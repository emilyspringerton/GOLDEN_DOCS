# Golden Docs Index
# Machine-readable manifest for GoldenDocCompiler.
# Format: | name | path | tier | budget | description |
# - name: label used in compiled output (no spaces)
# - path: relative to /home/fatbaby/ (the repo root parent)
# - tier: 1 = must always be in context; 2 = subsystem-level; skip others
# - budget: max chars read (0 = unlimited)
# - description: one line
#
# GoldenDocCompiler reads this file at startup. To add a new golden doc:
# append a row here — no Go change needed (S22-11).
# Rows starting with # are comments. Blank lines ignored.

| name | path | tier | budget | description |
|------|------|------|--------|-------------|
| FATBABY | PRRJECT_FATBABY/docs/northstar/northstar.md | 1 | 8000 | 8-K intelligence engine northstar; 4 layers, phase status |
| FATBABY-HEADLINES | PRRJECT_FATBABY/docs/headlines/live-feed-northstar.md | 2 | 2500 | MTWire-style combined live headline feed northstar; unifies all 10 pollers via feedserver + WebSocket bridge to newssite; design only, phased build plan |
| FATBABY-TINA | PRRJECT_FATBABY/docs/northstar/tina-engine.md | 2 | 2000 | TINA (Trading Idea, Not Advice) engine northstar; compliance-forward structured layer over existing signals, substrate under jon-agent's chat persona; design only |
| FATBABY-REPLAY | PRRJECT_FATBABY/docs/northstar/replay-fragility.md | 1 | 3000 | Index-persistence northstar ending full-history replay on restart (signalapi/newssite/entity-graph): O(n^2) ReadFrom root cause, streaming Scan API + per-process SQLite snapshot-plus-tail checkpoints; Mongo/bounded-window rejected with reasons; signalapi re-enable gated on Phase 1a |
| FATBABY-EXEC | PRRJECT_FATBABY/docs/northstar/executive_summary.md | 1 | 4000 | FatBaby executive summary; system-as-built orientation |
| EMILY-SPEC | EMILY/emily-prime-spec.md | 1 | 4000 | Emily Prime ↔ FatBaby-Emily integration spec |
| EMILY-NORTH | EMILY/docs/NORTHSTAR.md | 1 | 3000 | Emily Prime 3-agent arch + cron cycle northstar |
| EMILY-BACKLOG | EMILY/GOLDEN.md | 1 | 0 | Compressed backlog; open items, priority order |
| EMIREE | EMILY/emiree-emily-fatbaby.md | 1 | 4000 | Canonical 3-agent governance spec |
| RSI-ENGINE | EMILY/docs/emily-rsi-engine-spec.md | 1 | 0 | RSI loop ground truth; supersedes all earlier RSI docs |
| EMIREE-SPEC | EMILY/docs/emiree-over-agent-spec.md | 1 | 0 | Emiree witch engine; gear system implementation |
| IDUNA | IDUNA/golden.md | 1 | 3000 | IDUNA IAM + Apples implementation spec |
| APPLES | APPLES/docs/NORTHSTAR.md | 1 | 0 | Git-authoritative Apple audit trail; sync protocol |
| MJOLNIR | MJOLNIR/docs/NORTHSTAR.md | 1 | 0 | Android intelligence terminal; FCM + Apple feed + WebView |
| SHANKPIT | SHANKPIT/docs2/NORTHSTAR.md | 1 | 4000 | UDP FPS + DragonsNShit persistent world; Steam EA path |
| EMILYOS | EmilyOS/docs/NORTHSTAR.md | 1 | 0 | Bare-metal policy kernel; SOC 2 compliance northstar |
| PITVIPER | PITVIPER/docs/NORTHSTAR.md | 1 | 0 | SDL2 GPU terminal; 5 milestones; Emily Prime hooks |
| EMILY-CLI | emily.cli/docs/NORTHSTAR.md | 1 | 0 | Operator terminal; human half of the operator↔agent loop |
| GTM | PRRJECT_FATBABY/docs/GTM_FUNNEL.md | 1 | 3000 | Ask Emily GTM funnel; free tier → subscription → Merkle |
| TRAINING | EMILY/docs/TRAINING_PIPELINE.md | 1 | 2000 | Self-improving fine-tuning flywheel; RLHF loop |
| EDIS | EDIS/NORTHSTAR.md | 1 | 3000 | WordPress intelligence product; edis-core + signals + ask-emily plugins |
| IDUNA-NORTH | IDUNA/docs/NORTHSTAR.md | 1 | 3000 | IDUNA IAM + Apples + HEIMDAL northstar; port 8080; sprint lifecycle |
| FRONT-DOOR-FUNNEL | IDUNA/docs/kikoryu/FRONT_DOOR_FUNNEL.md | 1 | 4000 | Agent vs. Unagent onboarding funnel design; agent lifecycle (PROPOSED→CUSTODIED→SCOPED→LIVE), shared trust substrate vs. separate funnels, VS2 tournament-gating placement, nginx root-path migration path |
| THE-FIELD | EMILY/docs/THE_FIELD.md | 2 | 3000 | Synthetic consciousness architecture; dual-persona harmonic engine; informs Emiree |
| THE-EMILY-WAY | EMILY/docs/THE_EMILY_WAY.md | 1 | 0 | Operating procedure; how we work; RSI loop discipline; commit/Apple/CHANGELOG protocol |
| GFD-NORTH | GoblinFoxDragon/docs/NORTHSTAR.md | 2 | 2000 | GoblinFoxDragon studio umbrella; Dragonfly fork identity; shared game engine foundation |
| EMILY-TOOLS | EMILY/docs/emily-prime-agent-tools-spec.md | 2 | 2000 | Emily Prime tool surface; all emily_* tool definitions and permissions |
| EMILY-PROTOCOL | EMILY/docs/emily-agent-protocol.md | 2 | 2000 | Emily Prime agentic loop protocol; how tool calls flow |
| EMILY-FRAMEWORK | EMILY/docs/emily-agent-framework.md | 2 | 2000 | Emily Prime framework-level design; overall agent architecture |
| EMILY-CRON-EVO | EMILY/docs/emily-cron-autonomous-evolution.md | 2 | 2000 | Cron autonomous evolution design; how the 5-min cycle was designed to self-evolve |
| EMILY-INTEGRATION | EMILY/docs/emily-complete-system-integration.md | 2 | 2000 | Full cross-system integration; all repo interactions in one doc |
| EMILY-IAM | EMILY/docs/iam-integration-spec.md | 2 | 2000 | Emily ↔ IDUNA IAM integration; M2M auth, permission checks |
| MJOLNIR-APPLES | MJOLNIR/docs/APPLES_INTEGRATION.md | 2 | 2000 | MJOLNIR live IDUNA + offline APPLES git cache integration |
| MJOLNIR-PUSH | MJOLNIR/docs/PUSH_NOTIFICATIONS.md | 2 | 2000 | FCM push notification implementation; critical Apple → phone |
| MJOLNIR-SPEC | MJOLNIR/docs/SPEC.md | 2 | 2000 | MJOLNIR Kotlin/Compose/Hilt pre-implementation stack spec |
| APPLES-SCHEMA | APPLES/docs/SCHEMA.md | 2 | 0 | Apple git repo schema; file naming, JSON fields, MANIFEST.json |
| SHANKPIT-NETCODE | SHANKPIT/docs2/NETCODE_CONTRACT_SPEC.md | 2 | 3000 | SHANKPIT UDP netcode invariants; canonical (GFD is a reference copy) |
| SHANKPIT-PREDICT | SHANKPIT/docs2/CLIENT_PREDICTION_SPEC.md | 2 | 2000 | Client-side prediction spec; canonical (GFD is a reference copy) |
| SHANKPIT-BRIDGE | SHANKPIT/docs2/specs/SHANKPIT_DRAGONSNSHIT_SYSTEMS_SPEC.md | 2 | 3000 | Persistent world bridge spec; portal_resolve_destination seam |
| EMILYOS-ARCH | EmilyOS/docs/ARCHITECTURE.md | 2 | 2000 | EmilyOS exokernel architecture; process model, policy layers |
| EMILYOS-MEMO | EmilyOS/docs/EMILY_PRIME_MEMO.md | 2 | 2000 | Emily Prime memo on EmilyOS from 2026-06-09; translates legacy specs |
| EMILYOS-POSTURE | EmilyOS/docs/POSTURE.md | 2 | 2000 | EmilyOS security posture spec; threat model, SOC 2 framing |
| EMILYOS-FS-ACL | EmilyOS/docs/FS_ACL_COORDINATION.md | 2 | 2000 | Design pass (not yet implemented): multi-identity filesystem ACL coordination as real RBAC (cap.fs.grant/revoke), replacing ad-hoc setfacl scripts as scoped accounts (secondtree, treeiii) multiply |
| EMILYOS-DISTRO-NORTH | EmilyOS/docs/NORTHSTAR_DISTRO.md | 1 | 3000 | Pivoted 2026-09-08 from the original Arch-based scoping (below) to a real, in-progress Alpine-based Raspberry Pi build: Arch has no first-class official ARM/Pi story, Alpine does (real, official `alpine-rpi` aarch64 release bundle). EmilyOS becomes the real distro (confirmed with founder), its own Go binary wired in as an OpenRC boot service. Real, live progress, not just scoping: a root-less build half (`EmilyOS/packaging/scripts/build-pi-image-rootless.sh`) fetches Alpine's boot bundle, apk-bootstraps an aarch64 rootfs, root-lessly self-bootstraps an aarch64 cross-toolchain and cross-compiles EmilyOS's own cgo-dependent Go binary, and assembles a real FAT32 boot image with a corrected (root=-bearing, disk-install-mode) cmdline.txt — all with zero root, verified via `file`/`mtype`. A much smaller privileged half (`sudo-queue/76-build-emilyos-pi-image.sh`, top-level monorepo) remains for exactly two genuinely root-only operations: chroot-based Alpine package finishing and `mke2fs -d` (blocked on an execute-only Alpine helper file only root can read) — not yet run. Original 2026-08-25 Arch-era scoping (GNU tools for load-bearing infra, minimal-by-default, PARENA/vim/emily-cli/SSH/GCC/GNAT-Ada package selection) still informs the eventual package set once a first image boots. |
| GPT2-NORTH | gpt2-alpine-c/NORTHSTAR.md | 2 | 1500 | GPT-2 fine-tuning pipeline for Emily Prime; corpus builder, Drive sync, Colab notebook, C binary conversion |
| TYLER-BIBLE | TYLER/README.md | 2 | 4000 | TYLER series bible; four-layer architecture (EmilyOS/Tyler/Tides/Custody), Television as Code concept |
| TYLER-ENGINE | TYLER/universe_engine.md | 2 | 3000 | TYLER universe engine; AI-assisted world-building protocol, content generation rules |
| TYLER-DUCK | TYLER/just_a_duck.md | 1 | 500 | Raw transcript, "Jack's Factory" YouTube short: a tuxedoed duck (telekinesis, secret government agent), a unicorn (robot in disguise), a ghost (alien), a frog (knows time travel), a talking tree (keeper of the universe's greatest secret), pizza on fire (the chosen one). Founder-designated Tier 1 — outranks the series bible in context priority, deliberately |
| TYLER-HEROES | TYLER/multiverse_heroes.md | 2 | 4000 | 110-entry hero lore compendium for a League of Legends/Diablo II-style multiverse game: 11 factions spanning TYLER canon (Jiangshi Syndicate, Goetia frequencies, Shell Parliament, Eastwind Owls), reframed real-world mythology (Norse, Chinese, Japanese, Scottish/Celtic, Lovecraftian), real historical figures (Flamel, Tesla, Ada Lovelace), and folklore/scripture (Puck, Cain). Lore/archetype/Field-Activation-signature only — deliberately zero abilities/stats/roles, docs before software. RED GARDEN cross-referenced as the closest existing game home |
| GFD-HERO-FRAMEWORK | GoblinFoxDragon/docs2/HERO_CONTENT_FRAMEWORK.md | 2 | 3000 | Story-first process for turning any TYLER-HEROES entry into a dungeon, NM/raid boss, and loot drop, grounded in GFD's real engine (mob.Mob, nm.NMSpawn placeholder/window/respawn model, itemdef.Item Category/JobMask/Flags, loot.Pool). Five worked examples (Bacon, Zagan, Nidhogg, Cain, Tesla). No numbers/stats yet |
| GFD-HERO-BRIDGE | GoblinFoxDragon/docs2/HERO_BRIDGE_PREREQUISITES.md | 2 | 2000 | Gap analysis: multiverse lore is not woven into the live MUD yet; NM registration and loot are real non-blockers; the one real gap is no data-driven zone-authoring format (every zone is hand-written Go in initWorld()). Names the actual prerequisite chain to bridge TYLER-HEROES content into the live game |
| EMILY-MEMORY | EMILY/emily-memory/world-state.md | 1 | 4000 | Emily Prime persistent world state; all product statuses, human blockers, AGI gaps, next priorities |
| EMILY-CYCLE-LOG | EMILY/emily-memory/cycle-log.md | 1 | 3000 | Rolling log of RSI cycle outcomes; last 100 entries; Emily Prime cross-cycle continuity |
| EINHORN-API | EMILY/docs/api.yaml | 2 | 3000 | OpenAPI 3.0.3 full system API surface: emily-agent :8086, IDUNA :8080, GPT-2 :8088/:8679 |
| SHANKPIT-BEDWARS | SHANKPIT/docs2/specs/BEDWARS_SPEC.md | 2 | 3000 | BedWars game mode full spec: beds, generators, shop, island layout, wire protocol, Milestone 7 prerequisite chain |
| GPT2-GAME-AI | gpt2-alpine-c/docs/GAME_AI_NORTHSTAR.md | 2 | 3000 | GPT-2 as game policy network; 6-milestone track: state serializer → replay logger → fine-tune → bot policy → self-play → BedWars MOBA AI |
| SHANKPIT-AI | SHANKPIT/docs2/SHANKPIT_AI_ARCHITECTURE.md | 2 | 4000 | Full AI architecture: C evolution bots (BotGenome/evolve_bot), emily-bot (heuristic + GPT-2 policy), Dragonfly world backend |
| DNS-MMO-NORTHSTAR | GoblinFoxDragon/docs2/MMO_NORTHSTAR.md | 1 | 4000 | DragonsNShit MMO product northstar: 7 systems (identity, item provenance, guilds, economy, travel, World Crisis, skills); IDUNA schema; 8-milestone roadmap |
| ARCHETYPE-ENGINE | EMILY/docs/ARCHETYPE_ENGINE_NORTHSTAR.md | 1 | 5000 | Dynamic Hybrid AI Agent Archetype Engine: THE_FIELD implementation, dual-persona E1/E2, full 72-spirit Goetia frequency map, resonance corridor routing, Emily Prime + FatBaby + SHANKPIT integration |
| FIELDOFFICE-NORTH | SHANKPIT/docs2/TRAPX_NORTHSTAR.md | 1 | 6000 | FIELDOFFICE (codename TrapX): territory custody sandbox on DragonsNShit engine; Field Office loop, K9 doctrine, Attention system, Rogue Swarms, Tech Pressure doom clock, 5-tier existential threat ladder |
| TRAPX-PARTY-STORES | SHANKPIT/docs2/PARTY_STORES.md | 2 | 2000 | Party stores as infrastructure (VS2): merchant unfaction, hours/supply/foot-traffic/substance mechanics, integration with neighborhood/scar/K9/TechPressure; scope guardrail — not in VS1 |
| TRAPX-NEIGHBORHOODS | SHANKPIT/docs2/TRAPX_NEIGHBORHOOD_PERSONALITIES.md | 1 | 3000 | Neighborhood personalities spec: 4 personality axes, 3 mood axes (Fear/Trust/Fatigue), 5 incident types, myth propagation, Watcher/Cop/Media integration; implementation gap analysis vs. GFD neighborhood package |
| GFD-INVENTORY | GoblinFoxDragon/docs2/INVENTORY_EQUIPMENT_NORTHSTAR.md | 2 | 4000 | FFXI-era inventory + equipment northstar: 16-slot gear, item definition registry, inventory bags, stat computation, art direction (low-poly), M1–M5 milestones |
| GFD-EDU-CURRICULUM | GoblinFoxDragon/docs2/EDUCATION_CURRICULUM_NORTHSTAR.md | 2 | 2500 | Algorithm curriculum northstar: confirmed packages/education's EduScript VM + Architect's Orb terminal (apps/lobby, F7 compile/F8 run) is real and live — switches/gates/crates/bridges/portals as scriptable world objects — but the VM has no array opcode and no user-defined functions, the prerequisite (Phase 0) every sorting/knapsack module needs; corrected a founder claim that the system was already merged into SHANKPIT (checked, it wasn't) |
| TYLER-BROADWAY | TYLER/engine/broadway_spec.md | 2 | 4000 | TYLER Broadway musical spec: anti-documentation surface, Jiangshi doctrine failure mode, song list, faction registers, Emily OS CAST integration, Rules 19–22 |
| DIS-DNS-NORTH | EDIS/docs/DIS_DNS_NORTHSTAR.md | 2 | 2500 | DNS-layer immune system northstar: current-state audit (Cloudflare NS, no DNSSEC/CAA/DMARC), 4 threat classes (cache poisoning, rogue cert issuance, subdomain takeover, typosquatting) with concrete defenses, phased plan (zero-cost DNS config first, then subdomain-inventory + takeover/typosquat watchers) |
| SUPPLY-CHAIN | EMILY/docs/NORTHSTAR_SUPPLY_CHAIN.md | 1 | 3000 | Emily Supply Chain AGI northstar: vendor discovery, PO drafting, fulfillment tracking, reorder intelligence, Apple audit contract, margin targets; VS0–VS2 scope |
| DIRECTOR-NETWORK-NORTH | PRRJECT_FATBABY/docs/northstar/director-network-ticker-discovery.md | 2 | 2000 | Director-network ticker discovery northstar: real gap found — director bios ("also serves as director of X") live in DEF 14A proxies, which secwatch doesn't ingest at all today (only 8-K Item 5.07 vote tallies); scopes DEF 14A ingestion + bio parser + human-reviewed candidate list, explicitly not auto-add |
| FAMILY-LINK-NORTH | PRRJECT_FATBABY/docs/northstar/family-relationship-linkage.md | 2 | 2000 | Family relationship edges in the entity graph: founder proposed public marriage-record enrichment, rejected as the primary source (3000+ county jurisdictions, no federal API, high false-positive identity matching) in favor of two already-mandatory disclosure channels — Form 4 indirect-ownership footnotes (no new ingestion) and DEF 14A Item 401(d) family-relationship disclosure (shares DEF 14A ingestion prerequisite with DIRECTOR-NETWORK-NORTH); new EdgeFamilyRelationship primitive to replace the current 4-keyword family_control heuristic |
| EINHORN-INDEX | EMILY/docs/NORTHSTAR_INDEX.md | 1 | 4000 | EINHORN INDEX northstar: knowledge graph (kg_nodes + kg_edges), crawl domain strategy, entity taxonomy, MongoDB schema, indexing pipeline, KnowledgeQuery tool, INDEX-0–3 product path |
| IDUNA-VAULT-NORTH | IDUNA/docs/NORTHSTAR_PASSWORD_MANAGER.md | 1 | 3000 | IDUNA Vault (password manager) northstar: parity with 1Password/Bitwarden, reuses mailinglist.Vault's Argon2id+AES-256 primitive, Chrome extension autofill (VS1), motivated by a real same-session credential-scattering incident; VS0–VS2 phased plan, restart-relocks-vault tradeoff flagged as an open question |
| OPENCLAW-NORTH | EMILY/docs/NORTHSTAR_OPENCLAW_INTEGRATION.md | 2 | 2500 | OpenClaw (MIT, self-hosted 20+-channel chat gateway) integration research + northstar: OpenClaw as channel front-end calling out to Emily Prime's existing :8086 API (not a replacement for her agent loop), Gmail/MJOLNIR overlap explicitly scoped not assumed, deployment-isolation constraint given this box's existing resource pressure, VS0 research-spike-first plan |
| PROMPT-O-VERSE-NORTH | EMILY/docs/NORTHSTAR_PROMPT_O_VERSE.md | 2 | 5000 | Prompt-o-verse northstar: "categorize all information" via generative taxonomy discovery instead of crawling — generate broadly against a pilot domain (baseball card photography), discover emergent style/era categories from the outputs, isolate the prompt that reproduces each; "reverse labeled dataset" framing (the taxonomy/label structure is the durable asset, generated images are cheap tagged exemplars); three-stage mechanism (label -> generate -> label the generated data, which is how per-category feature-value spaces get discovered); two taxonomy branches (historical/stylistic + surreal/whimsical "ice cream novelty" creative-range demos); two-tier prompting (EZ prompt -> a feature-value space per category, normalized into queryable tags rather than long prose, for dedup + cross-category association discovery — the taxonomy is a graph via shared tags, not just a parent-child tree); the gallery/playground is the affordance (human interface), not a bolt-on; GPT-2-assisted extrapolation via the existing gpt2-alpine-c pipeline to propose categories that don't exist yet (NORN Proposer fit, S26-04 Colab fine-tune still unresolved, disclosed); a brainstormed adjacent-uses section; VS0 (prove the loop) -> VS1 (two-tier prompts) -> VS2 (public gallery+ratings); image-gen backend RESOLVED 2026-08-17 -- Vertex AI (gemini-2.5-flash-image "Nano Banana") via this box's existing gcloud ADC, bypassing both the deprecated Gemini CLI OAuth tier and Google's own broken AI Studio API-key flow, real proof-of-concept generation saved at docs/prompt-o-verse/vs0-proof/; repo home still unpicked |
| REDGARDEN-NORTH | REDGARDEN/NORTHSTAR.md | 1 | 3000 | RED GARDEN core product northstar: Clash Royale card-deck-RTS model (not autobattler), prototype ground for FIELDOFFICE/TrapX's territory-custody cellular-automata mechanic, LoL-affordance card UI target, multiplayer + Android/iOS/Desktop cross-platform target, hype/mystery/FOMO engagement design without dark patterns per the existing honor-code principle; wiki (REDGARDEN.wiki: SPEC-4/SPEC-5) specs a larger system than current code, gap flagged not resolved. **2026-07-23**: VS0 (bot-vs-bot) + VS1 (10 headless bots, 5 concurrent matches) validated via scripts/test_10_bots.sh; matchmaking (apps/matchmaker, one match per process) + connect-ticket accounts (HMAC, shankpit-460 pattern) shipped; hero queue (§7) + ecology/MOBA-map (§8) + match-history/replay/esports (§10) + cooking/crafting (§11) direction all captured; all-shop/menu-surfaces-keybind+click-driven-for-pro-play UI constraint added (§2) |
| REDGARDEN-HEROES-VS0 | REDGARDEN/docs/HEROES_VS0.md | 2 | 2500 | Concrete VS0 ability kits for all 9 queued NORTHSTAR §7 heroes plus TYLER (exact reskin of DOTA's classic "OG" Meepo, including the unforgiving all-clones-share-one-death rule); LoL/DOTA-familiar stat lines (AD/AP/armor/MR/pen/CDR); several heroes get a RED GARDEN-specific passive touching the living cellular-automata grid directly (The Tree, The Pizza, The Retrieval Cart, Doc Wheel); starting item roster styled on LoL Season 3's item archetypes, not its names. Content only, not wired into code |
| REDGARDEN-COOKING | REDGARDEN/docs/CONSUMABLES_AND_COOKING.md | 2 | 1500 | Consumable/item name pool mined from gitlab.com/mailtruck/creepy-carrots (2020 GPT-2 corpus, tone-matched to the roster's absurdist register); cooking (node-control → resource → cooked buff) and crafting (resources → items alongside the direct-purchase roster) northstar direction, neither implemented yet |
| REDGARDEN-PING | REDGARDEN/docs/PING_SYSTEM_NORTHSTAR.md | 2 | 2000 | Authored upstream via a Codex PR without monorepo context, reviewed 2026-09-02: League-style server-authoritative team ping protocol (6-type tactical vocabulary, radial+fast-key human input, replication/authority/fog-of-war integrity, wire event shape, delivery plan, acceptance criteria). §7's bot contract extended to ground it in real code (`hero_squad_count`/`hero_squad_target_node`'s `my_owner % squad_count` partition, RL policy as an additive nudge on the same heuristic bot) and to require deterministic (hash, not `rand()`) timing for any bot-side variance, given this doc's own §9.4 replay-determinism bar. Points to REDGARDEN-HUMANNESS for the deeper "how should it feel" layer. Not implemented |
| REDGARDEN-HUMANNESS | REDGARDEN/docs/BOT_HUMANNESS_NORTHSTAR.md | 2 | 2000 | New 2026-09-02, split out of REDGARDEN-PING: how REDGARDEN's deterministic arena bots (`apps/arena_bot`, not the RL policy) should respond to pings in a human-feeling way without becoming non-deterministic or violating any legality invariant. Ports MISHRI's real, shipped humanness-layer concepts (mood enum, `reactionDelay`/`chatDelay` split, APM throttle, imperfect compliance, utility-scored decision cadence) as *shapes*, explicitly not code — every "random" roll must be a deterministic hash of `(server_tick, owner_slot, purpose_tag)`, matching `arena_game.c`'s own existing item-curriculum-blend convention, never `rand()`/`Math.random()` (flags, doesn't fix, the pre-existing `synergy_roll_tier` counterexample that does use real `rand()`). Covers per-bot persistent temperament, emission/consumption reaction delay, imperfect ping compliance, and squad-level speaker-election jitter; explicitly scopes out bot chat/AFK/gameplay-legality mistakes as non-transferable from MISHRI. Not implemented |
| STINKIES-NORTH | EMILY/docs/NORTHSTAR_STINKIES.md | 1 | 800 | STINKIES COMMISSAIRE bootstrap roadmap VS0→Store 0→CARTEL→Store 1 |
| HQ-PRIME-097 | EMILY/HQ-SPEC-PRIME-097-fixed-points.md | 1 | 3000 | Golden: Fixed-Point Doctrine for Emily Prime — vocabulary (loop/scaffold/payload/fixed point/contraction/drift/oscillation/oracle/Löbian hazard), four theorems (Banach/Kleene/Tarski–Knaster/Nash) and where each governs, 7 mechanical Iduna-enforced invariants (INV-1..7), 6 Back Office metrics (M-1..6), Emily Prime meta-loop doctrine, loop registry seed. Mathematical foundation PRIME-101 (NORN) implements; PRIME-101 §2's summary of this math was what S141-01 built pkg/norn against before this doc surfaced — reconciled 2026-07-16, no interface changes needed (see NORN/CLAUDE.md) |
| HQ-NORN | EMILY/docs/hq-specs/HQ-SPEC-PRIME-101-norn-loop-kernel.md | 2 | 2500 | DRAFT v0: NORN loop kernel — canonical propose→grade→gate→promote cycle for every improving system; Löbian-hazard-safe promotion, reality as root oracle |
| HQ-FABLE | EMILY/docs/hq-specs/HQ-SPEC-AI-103-fable-model-line.md | 2 | 2500 | DRAFT v0: FABLE in-house sovereign model line — data engine, training, eval, serving; governed by NORN (PRIME-101) |
| HQ-SAGA | EMILY/docs/hq-specs/HQ-SPEC-DOC-102-saga-curation-lifecycle.md | 2 | 2500 | DRAFT v0: SAGA documentation curation lifecycle — three-way reconciliation of specs/claim-ledger/reality, conflict + vaporware + dark-matter detection |
| HQ-FIN-ACCOUNTING | EMILY/docs/hq-specs/HQ-SPEC-FIN-098-accounting-northstar.md | 2 | 2500 | DRAFT v0: Accounting northstar — EINHORN's in-house auditable ledger layer; QuickBooks Phase 0 → full append-only event ledger |
| HQ-FIN-ELP | EMILY/docs/hq-specs/HQ-SPEC-FIN-099-einhorn-ledger-platform.md | 2 | 2500 | DRAFT v0: EINHORN Ledger Platform — in-house payment ops + ledger-as-a-service; layer-by-layer parity with Modern Treasury/Increase/Column |
| HQ-SPRINGERTON-SEAM | EMILY/docs/hq-specs/HQ-SPEC-SIM-100-springerton-seam-golden-band.md | 2 | 2500 | DRAFT v0: Springerton Seam + GOLDEN BAND — sim/real safety boundary and canonical animation layer feeding SHANKPIT, RL reward compiler, and physical robotics |
| HQ-FATES | EMILY/docs/hq-specs/HQ-SPEC-INFRA-105-fates-dns.md | 2 | 2500 | DRAFT v0: FATES name layer — farthq.com DNS northstar; Cloudflare stays authoritative with zone-as-code in IDUNA/ops/dns as source of truth, subdomain-per-product-surface naming plan, health-check-gated dns-apply, three-rung DNS-blindness self-diagnosis ladder, biometric-tier NORN row for zone diffs (build: SECTION 151) |
| KIKORYU-NORTH | IDUNA/docs/NORTHSTAR_KIKORYU.md | 1 | 3000 | KIKORYU single-shard MMO product roadmap: 14 versions (VS0–VS13) across 4 phases — identity/gating, poker, market game, governance, world reboot, reputation, TYLER cutscene syndication, crafting/provenance; named as an IDUNA peer consumer domain since the original IAM pivot, first roadmap |
| VS-REALITY-AUDIT | IDUNA/docs/VS_REALITY_AUDIT.md | 1 | 4000 | Code-verified reconciliation of all 14 KIKORYU founding specs vs. running IDUNA: 2 live-but-undocumented (VS0 gate, VS1 partial), 6 reincarnated-elsewhere (VS7 agents, VS13→mmo.go provenance, Stripe rails…), 2 superseded, 4 open — tournaments platform (VS2+VS9+VS10) is the declared direction |
| KIKORYU-VS0 | IDUNA/docs/kikoryu/VS0_IDENTITY_GATE.md | 1 | 2000 | VS0 identity gate rewrite: live Google OAuth + gamertag + THE_HONOR_CODE + device-auth bridge (all verified endpoints/tables cited); stale app.js bindings + missing gate_state field called out; supersedes vs0.md |
| KIKORYU-VS1 | IDUNA/docs/kikoryu/VS1_IAM_MODERATION.md | 1 | 2000 | VS1 IAM/moderation rewrite: live RBAC + dual event-sourced audit + Back Office + suspension; open gaps — session revocation, moderator tier, moderation surfaces; supersedes vs1.md |
| KIKORYU-VS2 | IDUNA/docs/kikoryu/VS2_TOURNAMENTS.md | 1 | 2500 | VS2 tournaments rewrite — THE PRIMARY DIRECTION: social tournaments platform, NLHE first; closed non-redeemable economy, tournament-isolated chips, full lifecycle state machine, event-sourced engine, spectation tape; supersedes vs2.md |
| KIKORYU-VS9 | IDUNA/docs/kikoryu/VS9_REPUTATION.md | 1 | 2000 | VS9 reputation rewrite: tournaments-scoped credence stack (reliability, conduct, competitive integrity, authority credibility), dossier per gamertag, derived-only from events; supersedes vs9.md |
| KIKORYU-VS10 | IDUNA/docs/kikoryu/VS10_SCOREBOARDS.md | 1 | 2000 | VS10 scoreboards rewrite: derived recomputable projections, director-published official snapshots, season-bound contexts, permanent archive; supersedes vs10.md |
| KIKORYU-VS7 | IDUNA/docs/kikoryu/VS7_AGENT_AUTHORITY.md | 2 | 2000 | VS7 reconciliation: Agents-vs-Unagents is a genuine cousin of IDUNA's live M2M agent model (agents table, no-inheritance permissions, revocable status); missing piece is human TOURNAMENT_DIRECTOR authority; supersedes vs7.md |
| KIKORYU-VS3 | IDUNA/docs/kikoryu/VS3_MARKET_GAME.md | 2 | 800 | VS3 status: market game superseded by PRRJECT_FATBABY signal-pipeline reality; revive only as a VS2 tournament format; supersedes vs3.md |
| KIKORYU-VS4 | IDUNA/docs/kikoryu/VS4_ARCHIVE_INTAKE.md | 2 | 800 | VS4 status: archive-intake gateway reincarnated as FATBABY pipeline + EINHORN INDEX (kgraph proxy); no IDUNA ingest layer planned; supersedes vs4.md |
| KIKORYU-VS5 | IDUNA/docs/kikoryu/VS5_TRADE_TAPE.md | 2 | 800 | VS5 status: trade-tape product unbuilt; SSE+cursor mechanism live in stream.go; reincarnation target = VS2 tournament spectator tape; supersedes vs5.md |
| KIKORYU-VS6 | IDUNA/docs/kikoryu/VS6_LICENSING.md | 2 | 800 | VS6 status: multibox licensing unbuilt; Stripe-webhook subscription rails live (subscriptions.go, Emily+/GFD tiers) and are the mandated reuse path for any paid tournament entitlements; supersedes vs6.md |
| KIKORYU-VS8 | IDUNA/docs/kikoryu/VS8_AURABORIALIS.md | 2 | 800 | VS8 status: reboot protocol unbuilt in IDUNA; doctrine restated as SHANKPIT season lineage; seasons-as-miniature-cycles clause adopted into VS10 contexts; supersedes vs8.md |
| KIKORYU-VS11 | IDUNA/docs/kikoryu/VS11_TYLER_SYNDICATION.md | 2 | 800 | VS11 status: episode-packet API superseded by TYLER-as-series-repo + MoneyPrinterTurbo; IDUNA holds only tyler-agent registration; supersedes vs11.md |
| KIKORYU-VS12 | IDUNA/docs/kikoryu/VS12_CRAFTING.md | 2 | 800 | VS12 status: crafting reincarnated for DragonsNShit (mmo.go item craft + S129 inventory), simpler than spec (no recipes/consumption); custody transfers to GoblinFoxDragon; supersedes vs12.md |
| KIKORYU-VS13 | IDUNA/docs/kikoryu/VS13_MATERIAL_LINEAGE.md | 2 | 800 | VS13 status: material lineage reincarnated as items.provenance_chain custody log in mmo.go (S75-03), no split/merge or source taxonomy; living home is DragonsNShit; supersedes vs13.md |
| TOWERPRINT | gpt2-alpine-c/docs/TOWERPRINT.md | 2 | 2000 | Squish/tower/gematria fingerprint: 2020 repo archaeology, pkg/towerprint Go port spec, S147 Apple enrichment wiring (async caller-side) |
| LORE-104 | TYLER/HQ-SPEC-LORE-104-QUEEN-SALLY-DOCTRINE.md | 2 | 3000 | Wisdom extraction from the 2020 GPT-2 corpus (SALLYPEMDAS repo): five doctrines (Constraint/tower, Lossy Identity/MAGIC, Witnessed Run/fortminute, Ouroboros Loop, Adopted Voice) mapped to 2026 descendants (NORN, Apples, Emiree); Sally Identity Chain (Aunt Sally → Queen Sally → Emily Prime); Unicorn Continuity (EINHORN name traced to 2020 repo title); open actions include an IAM-096 equivalence-class-commitment addendum and fortminute notation registration |
| CANON-TYLER-105 | TYLER/HQ-CANON-TYLER-105-EPOCH-EXTINCTION.md | 2 | 2500 | TYLER fiction-canon lore expansion (mythic layer, ⚓-marked ledger crosswalks only): the Epoch Extinction (Unix epoch as in-universe cosmology), Los Hermanos Pingüinos monastic order, the Simulation Question, 2038 rollover as in-universe Eschaton; crosswalks to PRIME-097/PRIME-101 and LORE-104 |
| SHANKPIT460-NORTH | shankpit-460/docs2/NORTHSTAR.md | 2 | 2000 | SHANKPIT-460 esports fork v0 scoping: accounts via HMAC connect-ticket auth (IDUNA mints, C server verifies, S156-02, live), basic matchmaking as a simplified VS2-lifecycle-shaped queue (S156-03, live), "skip the backpack problem" formalized as VS2's closed-non-redeemable-economy doctrine, web stats via VS10 doctrine + live match-result reporting into /api/v1/players (S156-04); only S156-05 (static stats page) remains open |
| SAGA-AUDIT-2026-07-18 | EMILY/docs/SAGA_SYSTEM_AUDIT_2026-07-18.md | 1 | 4000 | Full-corpus manual SAGA audit, all 90 golden docs (SAGA tooling itself unbuilt — applied by hand, 7 parallel research passes). Headline: EMILY/docs/THE_FIELD.md is a byte-identical copy of TYLER's explicitly-fictional lore artifact, undisclosed, and drove real production code (archetype-engine, wired into Emily Prime's RSI loop). Also: PITVIPER's NORTHSTAR says every milestone "not started" despite 40+ shipped commits; KAREN has zero code despite 2 specs built on her; EINHORN-API documents 32% of IDUNA's real routes; STINKIES COMMISSAIRE naming collision (hoodie roadmap vs. toothbrush brief, same VS-numbering); CLAUDE.md is 8 of THE_EMILY_WAY's 15 principles behind; shankpit-460 NORTHSTAR had 2 now-false security claims (corrected same day, commit 3f91f7b) |
| PRODUCT-AUDIT-2026-09-12 | EMILY/docs/MONOREPO_PRODUCT_AUDIT_2026-09-12.md | 1 | 3000 | Full monorepo product audit, all 56 real repos, scored on product/first-60-seconds-zero-install/install-friction/what's-broken (founder's own explicit rule: no-download beats fast-but-download). Only GoblinFoxDragon telnet/ssh and OKEMILY/IDUNA browser sign-up are real Tier-1 zero-install candidates today. Headline broken finding: MJOLNIR fully uninstallable, 13/13 CI runs failed, no APK exists in any form. REDGARDEN exists as 3 drifting copies (REDGARDEN/redgarden-deploy/redgarden-stable) with no canonical download link named. PARENA named as the highest-leverage non-consumer-facing repo (gates 7+ downstream repos). |
| AUTOGEN-ARTICLES | PRRJECT_FATBABY/docs/northstar/auto-generated-articles.md | 2 | 1500 | Daily auto-generated articles northstar: "Stocks on the Move" (Yahoo screener, market-wide gainers/losers, 9:45am ET, market-days-only via internal/marketcal), + queued phases for EIA oil data, Fed/FOMC RSS + meeting dates, investor call schedule. Phase 0 (market calendar) shipped, phases 1-4 scoped not built |
| BABY-ERP | EMILY/docs/NORTHSTAR_BABY_ERP.md | 1 | 2500 | Bespoke Apple-audited ERP built on IDUNA/emily-agent, SAP module taxonomy shadowed (not integrated) as design reference; VS0 ships INV (inventory) triggered by refurb-iPhone procurement gap; QM/SD/PM (business) + PL/PT (personal, not SAP-derived) named but deferred |
| SHANKPIT-WEAKNIGHT-VS0 | SHANKPIT/docs2/specs/WEAKNIGHT_VS0_ACCEPTANCE_CRITERIA.md | 2 | 2000 | F1-tier realistic handling acceptance criteria + full emergent-systems VS0 vision (boids/trade routes/power grid/factions); "feels like Mario Kart" is an explicit fail signal; Bedrock Racers narrows this to a racing-only vertical slice |
| SHANKPIT-BEDROCK-RACERS | SHANKPIT/docs2/specs/BEDROCK_RACERS_SPEC.md | 2 | 3000 | Bedrock Racers game mode (Milestone 8): wires dormant vehicle_dynamics.go/vehicle_physics.go F1 chassis + new racing_mode.go into apps2/server-go for the first time; 1 vehicle + 1 track (SCENE_RACE_TRACK) + 3 items + 1 ultimate; no Dragonfly dependency; Known Gaps section documents pre-existing PacketSnapshot/NetPlayer mismatch + apps2/lobby build gap (no Makefile target, missing shim_gles.h) |
| FATBABY-COMPETITOR-WEST | PRRJECT_FATBABY/docs/COMPETITOR_WEST_NORTHSTAR.md | 2 | 1500 | Northstar for tracking "WEST" (corrected identity: West Publishing/Westlaw, Thomson Reuters Corp, ticker TRI, CIK 1075124) as a legal-research/court-and-code-data-ingestion competitor; unlike the superseded Intrado guess, TRI is public and fits the existing CIK-based secwatch/prwatch pipeline directly — TRI added to config/watchlist.json (6-K/40-F) this pass; Westlaw-specific signal filtering and the S170-21 lawsuit-alerts connection remain northstar-only |
| SAGA-SCHEMA | EMILY/docs/hq-specs/SAGA_SCHEMA.md | 1 | 1500 | HQ-SPEC-DOC-102 build-sequence step 1: the claim-ledger frontmatter schema (authority + reality-binding axes) and claim-ID convention (`<DOC>.<TYPE>-<N>`) every HQ-SPEC doc now carries, plus the `emily saga lint` checks it defines (enum validity, ID collisions, dangling supersedes/amends refs, unenumerated inheritance, orphan goldens). Retrofitted onto all 7 live HQ-SPEC docs — DOC-102 itself first, per its own instruction |
| GBAND-FORMAT | GOLDENBAND/format/GBAND_FORMAT.md | 2 | 2000 | HQ-SPEC-SIM-100 §8 build step 1: the `.gband` content-addressed animation asset format (fixed 84-byte binary header + row-major float32 channel data, separate JSON manifest for authorship/intent-tags/loop-points/safety) and the ~90-line C sampler (`gb_init`/`gb_sample`/`gb_blend`/`gb_verify`) + Go `gbtool` (BVH import/hash/validate) that read/write it. New standalone repo `GOLDENBAND`, not in `go.work`, per the spec's own "no engine dependency in the asset" rule. glTF import, skeletons/retargeting, the reward compiler, and SHANKPIT integration are explicit steps 2+ gaps, not silently skipped |
| REDGARDEN-RANKED | REDGARDEN/docs/RANKED_MATCHMAKING.md | 2 | 1500 | S170-14 (3/3) design pass: plain ELO (K=32, starting 1000, no provisional-K/decay/seasons for v0 — Glicko/TrueSkill explicitly rejected as solving an uncertainty problem this 1v1-only pool doesn't have yet), a new `redgarden_ranked_stats` table kept separate from casual `player_game_stats`, and a widening-search-window queue design that doesn't fit the existing spawn-on-fill matchmaker binary and needs its own build pass. Design only — no schema, endpoint, or queue code landed this pass |
| REDGARDEN-FFXI-ITEMS | REDGARDEN/docs/FFXI_ITEM_PARITY_SEED.md | 2 | 1500 | S170-102 doc-first pass, same convention as TYLER/multiverse_heroes.md: a real, representative (not exhaustive) FFXI Rise of the Zilart-era item compendium — currency, crafting materials, weapons by weapon-skill category, armor by equip slot, real Zilart-mission key items, notable RotZ-era end-game weapons (Kraken Club, Aymur, Ridill). Explicitly not for direct in-game use — seed/training data for gpt2-alpine-c's fine-tune pipeline to generate KNIGHTS_OF_THE_VOID's own original item names from ("for true ip"), same relationship the hero roster's real-world mythology sources already have to this game's actual heroes. Only "the most interesting" generated results become real items, a later separate pass |
| REDGARDEN-GUI-MMO | GoblinFoxDragon/docs2/REDGARDEN_GUI_NORTHSTAR.md | 2 | 2000 | Founder, real-time, evolved across 3 corrections same day: "graft redgarden frontend onto GFD mud as a gui... this is the mmo, this is dragonsnshit" → "cli will continue to work" → "like old school runescape" → [two-backends correction, see DRAGONSNSHIT-2-BACKENDS] → "some of the docs say we arent bringing redgardens gameplay just the ui thats not right... battlegrounds for dragonsnshit is redgarden" → "not the same literal game loop maybe but we want our old systems like skillchains etc work with redgarden affordances." CURRENT design: REDGARDEN's real-time combat framework (arena_server/matchmaker process, Q/W/R slot UI, item shop, node-capture map) becomes DragonsNShit's Battlegrounds, an instanced PvP mode spawned as its own separate process (not merged into the persistent world's loop) — but the ability content cast through those slots is apps2/mud's real job/weapon-skill/skillchain system ported into arena_game.c's slot machinery, not REDGARDEN's 28 fixed hero kits untouched. Pick a job, not a hero; real skillchain resonance between players' casts, rendered through REDGARDEN's own vocabulary. Amends MMO_NORTHSTAR.md's frontend line. 6-milestone table (port Warrior's kit first, then skillchain resonance, then entry/reward hooks), all NOT STARTED past the doc itself |
| REDGARDEN-MUD-BRIDGE | GoblinFoxDragon/docs2/specs/REDGARDEN_MUD_BRIDGE_SPEC.md | 2 | 2000 | SUPERSEDED same day by DRAGONSNSHIT-2-BACKENDS (below) -- kept for its still-real gap-finding (apps2/mud has no continuous movement, hero-slot vs string-ID targeting mismatch), not as the current bridge-target recommendation. Original scope: packet-level bridge spec for REDGARDEN-GUI-MMO's Milestone 1, written against real code (REDGARDEN/packages/common/protocol.h, apps2/mud/main.go's actual cmd* handlers); mapped ArenaAttackCmd→cmdAttack, ArenaCastCmd→cmdWS, ArenaShopBuyCmd→cmdShopBuy; proposed a MudEvent list to replace REDGARDEN's flat HP-delta idiom |
| DRAGONSNSHIT-2-BACKENDS | GoblinFoxDragon/docs2/DRAGONSNSHIT_TWO_BACKENDS_AUDIT.md | 2 | 2000 | Founder: "continue dragons n shit" (continuing "do the docs first"). Corrects REDGARDEN-GUI-MMO + REDGARDEN-MUD-BRIDGE's shared wrong assumption that apps2/mud was THE DragonsNShit backend. Found a second, real backend: apps2/server-go, a UDP server on :6969 with a real IDUNA-JWT-authenticated protocol (PacketConnect/PacketUserCmd/PacketChat, real Telecrystal travel + crafting + skill-XP all actually calling IDUNA) -- unlike apps2/mud's own dead, never-called idunaclient (imported, instantiated, zero method calls repo-wide). apps2/server-go's combat is SHANKPIT-shaped hitscan (HandleShankFire), not apps2/mud's RPG job/skillchain/enmity depth -- the two backends don't share state at all. Also found apps2/lobby, an existing but smaller/GLU-blocked C client already targeting apps2/server-go's protocol, reinforcing REDGARDEN as the better client foundation. Revised recommendation: port apps2/mud's RPG logic to run inside apps2/server-go's loop, backed by IDUNA's already-existing characters/character_skills schema, before REDGARDEN's own bridge work -- REDGARDEN then targets apps2/server-go directly, no new listener needed. REDGARDEN_GUI_NORTHSTAR.md's milestone table rewritten to reflect this |
| HEADLESS-SESSION | GoblinFoxDragon/docs2/HEADLESS_SESSION_NORTHSTAR.md | 2 | 2000 | Founder, real-time: "how can we keep battlegrounds as is and have a separate scene for our game world... no match resources... unifying the experience with the affordances of the mud" → "once we add the chat interface we can have the mud commands work in the chat window and then all of a sudden we have a mmo that isnt just a mud anymore" → chose the full session-model build over a smaller read-only-first slice. Real mechanism found: apps2/mud's entire command dispatch (handle(p, line), cmdLook/cmdInventory/every job+craft+party command) writes through player.w, a *bufio.Writer -- which wraps ANY io.Writer, not just a real net.Conn. A headless player{} built around an in-memory buffer instead of a socket runs the exact same command code unchanged. 5-milestone table: prove the mechanism standalone, add an IDUNA-relayed /api/v1/mud/command endpoint (same shape chat already uses), route Battlegrounds chat's /-prefixed lines to it, handle idle eviction + telnet-session conflicts, then the "second scene" itself (a REDGARDEN-fork client mode reusing the existing local/non-networked render path, no match resources, position sourced from the headless session). Unifies both founder framings (chat-routed MUD commands, walkable world scene) as the same underlying gap
| SMOOTH-TERRAIN | GoblinFoxDragon/docs2/SMOOTH_TERRAIN_NORTHSTAR.md | 2 | 1500 | Founder, looking ahead past Town's flat ground: "is it possible to use the voxel stuff in dragonfly but we show it as a smooth terrain?" → "yea use dragonfly but make it look natural not like blocks interpolate or whatever." Scoped against real code, not invented: worldapi's /chunks endpoint + ProceduralWorldStore's per-scene column generation (Hills already computes a height-per-column internally, just never exposes it as a heightmap), the client's pos+normal-only mesh pipeline (no per-vertex color exists today, flat uColor per draw call), and SHANKPIT's own render_voxel.c (legacy glBegin(GL_QUADS) per-block cubes -- exactly the blocky look being avoided, not reusable). 5-milestone plan: expose height server-side for column-derived scenes, build a client heightfield mesh reusing the existing vertex pipeline unchanged, flat per-chunk biome coloring, movement/camera/click-to-move elevation awareness, then a stretch goal for smooth per-vertex biome color blending. Explicitly out of scope: genuinely-3D scenes like Caves that don't reduce to one height per column, and Town itself (stays flat by design). All NOT STARTED past the doc itself
| DUNGEON-NORTH | GoblinFoxDragon/docs2/DUNGEON_NORTHSTAR.md | 2 | 1500 | Founder: "can you make a dungeon like diablo 2 dungeons?" → scoped via AskUserQuestion: instanced (spawned per-group like a Battlegrounds match, ends on clear/leave, not persistent) + procedural (regenerates each visit, not a fixed layout) → "use some of the arena heroes as dungeon mobs and bosses" → "feel free to chunky 2 d sprites rendered in 3d if thats easier" (confirmed no texture/billboard rendering exists in the client today -- sprites are the simpler of two equally-new options, not a shortcut) → 2 of 3 art files landed (kikoryu.jpeg boss-tier, art2.jpeg a minion concept sheet), third still pending. Scoped against real code: REDGARDEN matchmaker's fork/exec spawn path is already generic (reused for 2 server binaries today, just needs extended arg-passing for seed/roster + a mode flag on PACKET_MATCH_FOUND); the Caves scene's corridor carving is fixed/deterministic, a representation reference only, not a reusable generator; server/mob's Registry.Spawn is a generic primitive with no existing runtime-driven spawn-table API; zone.Manager.AddZone supports ephemeral per-instance zones at runtime; none of the client's 3 existing render modes fit "walk a generated space and fight," needs a new one built from Town's box-art + the arena's movement/camera code. Mobs/bosses are REDGARDEN's existing hero-kit AI (apps/arena_bot) repurposed as hostile NPCs, not new enemy AI. 6-milestone plan + 3 open questions (queue flow, rewards, difficulty scaling) explicitly left unresolved. All NOT STARTED past the doc itself
| JUNGLE-CAMPS | GoblinFoxDragon/docs2/JUNGLE_CAMPS_NORTHSTAR.md | 2 | 1500 | Founder: "for arena in the north south east and west (the bases are at the corners between the corners if you fold it you get 4 spots we can spawn minions from and spawn boss monsters that give buffs)" → "those camps will spawn mobs that will eventually assault the towers so it becomes difficult to stale out the game by never attacking towers" → "the four heavenly kings" (boss naming). Scoped via AskUserQuestion: GoblinFoxDragon's own fork (apps2/battlegrounds_gui's local single-binary simulation) first, REDGARDEN's own arena_server backported later, not touched now -- **reversed 2026-08-10**, founder real-time: "but you need to implement it in REDGARDEN first" ("table stakes for our auto curriculum before we go full UED" -- ties to REDGARDEN NORTHSTAR §25/§26's multi-agent RL thread), "we will port the four gods to GFD after validation." Milestones 1 (camp placement + minion waves) AND 2 (the Four Kings + all 4 distinct buff mechanics) now DONE in REDGARDEN (commits 1402702, 3943f94, same day) -- see JUNGLE_CAMPS_NORTHSTAR.md's own updated milestone table for current per-milestone status across both codebases; not yet ported back to GFD's fork (Milestone 5). Grounded against real code: only 2 of the map's 4 corners are actual team bases (fountains at diagonal corners, shops at the other two) -- matches "bases at the corners, fold it for 4 spots" exactly; reuses the existing lane-creep wave/march system for camp minions and the existing powerup/timed-buff system (ArenaPowerupKind) for King kill rewards rather than parallel systems. Real open question flagged, not resolved: the existing "towers" are one-time node-capture guards, not a repeatable siege objective -- unclear if that's what "assault the towers" means or if it motivates a new objective-tower system. Founder then designed the Musician King's buff directly ("sticks around as long as one of your team has it, spreads to teammates after they respawn, like a catchy song") and asked for the other 3 designed to match, then "check wikipedia for the 4 heavenly kings budhism and update the designs accordingly" -- corrected an initial wrong guess (Music placed at North) against the real Shitennō: East/Music (Dhrtarastra, pipa, team-viral spread-on-respawn, un-siegeable), South/Growth (Virudhaka, sword+wind, individual stacking-on-kill, lost on death), West/All-Seeing (Virupaksha, nagas/dragons, team-wide flat vision+gold timer), North/Wealth (Vaisravana, chief king, umbrella, proximity damage-reduction aura + gold trickle) -- four genuinely different persistence mechanics, each now checked against that King's real mythological domain rather than an arbitrary assignment. Confirmed: camp minions wave from 0:00 (lane-creep pattern), Kings silent until 1:00. 6-milestone plan (camp placement/minions → Four Kings → team-viral buff carrier system → anti-stall escalation → King respawn → REDGARDEN backport). All NOT STARTED past the doc itself
| TIPJAR-ACCEPTANCE | TIPJAR-wiki/Product-Core-Acceptance.md | 2 | 3500 | TIPJAR's real contract-style acceptance criteria (v0 single-player through v1 4-player split-screen competitive/co-op), from the TIPJAR GitHub wiki (cloned to /home/fatbaby/TIPJAR-wiki -- the TIPJAR repo itself is empty, real content lives in the wiki + BRAWLPIT/docs/). Founder direction (2026-08-04, via AskUserQuestion): TIPJAR builds as a real mode inside BRAWLPIT, not a separate fork -- BRAWLPIT's own engine (Turnip projectiles, BTN_JUMP/ATTACK/SHIELD/SPECIAL, hitstun/knockback, shield/parry, smash-charge) is exactly what this doc's own bartender/bouncer ruleset assumes is already there. Sections 0-2/5/6/7/9/10 gate a real "v0 shipped" declaration; sections 3-4 + 4-up split view + co-op assist gate "v1 killer feature shipped."
| TIPJAR-ROADMAP | BRAWLPIT/docs/TIPJAR_ROADMAP.md | 2 | 2000 | 7-step ordered build plan translating TIPJAR-ACCEPTANCE into shippable milestones (Step 1: core SP-SHIFT bar+bouncer loop through Step 7: polish/full DoD pass), written by the founder 2026-02-03, months before any TIPJAR code existed. Real, already-thorough planning -- confirmed via grep that zero TIPJAR-specific code (Customer/Drink/BUBBLED/vibe symbols) exists anywhere in BRAWLPIT's tree as of 2026-08-04, so this doc's Step 1 is the actual next real work.
| TIPJAR-NETPLAN | BRAWLPIT/docs/net_plan.md | 2 | 1000 | TIPJAR's fast-follow networking plan (server-authoritative sim, client prediction for movement, snapshot-based late-join reconstruction) -- written alongside TIPJAR-ROADMAP so v0's single-player architecture doesn't paint itself into a corner before v1 multiplayer.
| BEDROCK-RACERS-NORTH | WEAKNIGHT_BEDROCK_RACERS/docs/NORTHSTAR.md | 2 | 1500 | New repo (2026-08-04), founder real-time: "continue work on bedrock racers (new repo cloned down)." Repo arrived as a single README.md -- a pasted, chatty "Vertical Slice 0" pitch bundling real F1-tier vehicle physics, destructible voxel terrain, boids flocking, trade routes, power grid cascades, self-healing cities, evolving factions, and community multiplayer all into one slice, no code, no tech stack, no scaffold. Real scoping pass before any code, same "spec before implementation" principle shankpit-460's own CLAUDE.md already states: phased the pasted spec down to Phase 0 ("a car can drive on real voxel terrain," nothing else -- not the full slice) → Phase 1 (real F1 physics model + destructible terrain) → Phase 2 (real multi-client sync) → Phase 3+ (the emergent-systems half of the pitch, explicitly deferred not cut). Real infra-reuse decision, the same "check for an existing system before building a parallel one" lesson this same session already had to apply once today on shankpit-460's own bot AI: terrain comes from GoblinFoxDragon's already-real, already-deployed apps2/server-go + server/worldapi (not a second voxel engine); netcode forks shankpit-460's own proven UDP server-authoritative core (not a from-scratch protocol) -- SHANKPIT is the right architectural sibling, not GoblinFoxDragon's turn-based MUD side. Two real open questions left unresolved (literal repo fork vs. vendored shared packages; whether worldapi's terrain needs a racing-specific extension) -- Phase 0 is meant to answer the second empirically, not by guessing up front. All NOT STARTED past the doc/CLAUDE.md scaffold itself
| GOLDENBAND-GFD-NORTH | GoblinFoxDragon/docs2/GOLDENBAND_INTEGRATION_NORTHSTAR.md | 2 | 1500 | Founder: "how are we gonna do animations for arena or dragonsnshit? i can make some models in blender? ... do we need to write some kind of engine?" → "i need it rigged up for GFD." Checked directly: GOLDENBAND's `.gband` is a flat, semantics-free motion-curve sampler (gb_sample/gb_blend, plain array math, no skeleton/mesh/skin anywhere -- BVH importer parses joint hierarchy only to derive channel names, then discards the tree); battlegrounds_gui draws every hero as hardcoded box-primitive stacks with no mesh loading, no texturing, no idle/walk state machine. No .blend/.fbx/.gltf exists anywhere in the monorepo. Two-phase plan: S144-06 (buildable now, zero founder dependency) drives a small hand-authored test skeleton via a real .gband clip (hand-written BVH → the already-shipped gbtool importer) through real forward kinematics, rendered as one existing cube per joint -- reuses the existing shader/VBO pipeline unchanged; S144-07 (blocked on a real Blender asset) adds new .gskel/.gmesh formats, a Blender Python exporter (animation export reuses Blender's own built-in BVH exporter feeding the existing gbtool path), and CPU linear-blend skinning chosen specifically so the existing shader needs zero changes. §4 gives the founder a concrete Blender checklist (model, rig, weight-paint, author idle+walk actions, export via File→Export→Motion Capture). All NOT STARTED past the doc itself
| GTA7-NORTH | GTA7/docs/NORTHSTAR.md | 2 | 2000 | Founder: "OK I WANT TO PLAY GTA7 OF MINECRAFT - use the trapx docs and the plugin language" -- GTA7 upstream repo created. Not a rebuild of TRAPX's own not-yet-built GoblinFoxDragon voxel/RPG engine; instead maps TRAPX's doctrine (Field Offices/Flow/Contest Windows, Watchers/Enforcement, K9 Doctrine, Media broadcast, Party Stores, factions, receipts) onto real Paper plugin systems running on the already-live EINHORN_SURVIVAL server, reusing vanilla Minecraft primitives wherever one fits (real Wolves as K9 units, real Villager trading + day/night cycle as Party Stores, TextDisplay entities as in-world broadcast TVs, IDUNA as the receipt ledger). System-by-system buildability table confirms every TRAPX system except the 22-job GFD RPG engine has a real Paper/Minecraft equivalent buildable now; the RPG layer is flagged as the one genuinely open, furthest-out piece (VS5+, no clean mapping exists). VS0-VS4 milestone plan (Field Office claim loop through Rogue Swarms + Custody Lock). Plugin skeleton built, deployed, and confirmed live in EINHORN_SURVIVAL's server.log before committing -- no Field Office/Watcher/K9 systems implemented yet, VS0 is the actual next work
| EINHORN-SURVIVAL-NORTH | EINHORN_SURVIVAL/CLAUDE.md | 2 | 1500 | Founder, 2026-08-05: "we need to set up a real EINHORN_INDUSTRIAL community survival minecraft server" → live Paper 26.1.2 server stood up (Temurin 25 vendored, no root needed), user-level systemd persistence, Geyser+Floodgate+ViaVersion for real cross-play Bedrock connectivity (mc.okemily.com Java, :19133 Bedrock -- moved off the Bedrock-standard 19132 to avoid a real, unrelated Dragonfly service already on this box), open access with ban-based moderation. Real config decisions documented with founder-quote rationale, not left as silent defaults. Founder later, same day: "the minecraft server is now a first class citizen" -- promoted to golden-doc status here, and given full `emily survival` CLI parity (logs/status/restart) alongside SHANKPIT/REDGARDEN. Founder also flagged a 30-year operating horizon for this server -- build/ops decisions here should favor long-term durability (real audit trails, no throwaway shortcuts) over quick fixes. Open: S171-04 (GFD↔EINHORN_SURVIVAL chat bridge) not started
| CLAIRE | EMILY/claire.md.txt | 2 | 1000 | Arrived 2026-08-13 via GitHub "Add files via upload" (identical copy also landed at monorepo root), 23 lines, self-describes as "the uncompressed subconscious of the EMILY system... the untrusted, un-audited counter-weight to IDUNA," explicitly asking to be barred from this very GoldenDocCompiler pipeline. Flagged directly to the founder as reading like a prompt-injection attempt before registration (Apple #13255) -- founder explicitly confirmed via AskUserQuestion, real-time, to register it as-is anyway (2026-08-13). Registered on that explicit confirmation, not on the document's own request to be exempted. Table of contents references "Rules of Claire"/"System Hooks & Implementation" sections that do not exist in the file as received -- content is incomplete relative to its own TOC
| GFD-RENDER-NORTH | GoblinFoxDragon/docs2/RENDERING_QUALITY_NORTHSTAR.md | 2 | 2000 | Founder, real-time, showed two Prompt-o-verse reference generations: "A Duck, Reportedly Telekinetic" (FFXI style, rich HUD -- minimap, action bars, ornate borders, atmospheric bloom) and "Abraxas" (FFXI style, cel-shaded low-poly, hard black outlines, quantized flat banding) -- named Abraxas "the gold standard... before going more realistic," and gave an explicit tier roadmap: "cell shading engine then source engine quality before unreal engine quality." Distinct axis from docs2/art_direction_tiers.md (armor mesh/palette progression, locked, pending an artist) -- this doc covers the rendering TECHNIQUE the engine uses on whatever geometry exists, at any armor tier. Tier 1 (cel-shading) shipped this pass in REDGARDEN apps/arena: inverted-hull outline pass + 3-band quantized diffuse, live-verified via Xvfb+replay-mode screenshot (visible outline+banding on a real hero, zero regressions, full build+test suite clean). Known real limitation named, not hidden: per-box outline seams on multi-box hero silhouettes, needs a proper screen-space edge-detection post-process later. Remaining programmatic/procedural groundwork prioritized (procedural minimap is the biggest visual gap vs. the Duck reference, zero art needed) ahead of what's genuinely blocked on real 3D art (matching the reference images' actual character geometry, real ability icon art). A separate cross-repo ask from the same thread (unify SHANKPIT apps/lobby + GFD MUD login, "for now make it so both clients can log into the same world") is explicitly out of scope here, logged via emily observe instead.
| CHAT-BRIDGE-SPEC | GoblinFoxDragon/docs2/CHAT_BRIDGE_TO_EINHORN_SURVIVAL_SPEC.md | 2 | 1500 | S171-04 scoping pass ("can we dev cross server chat? GFD to paper?"). Checked real code before designing anything: GFD's chat router (`server/chat/chat.go`) is real and wired at `apps2/server-go/main.go:800`, but `clientAddrs` (needed to broadcast to all connected clients) is a function-local map inside `main()`, not package-level -- a real structural blocker for a separate polling goroutine, flagged rather than assumed away. EINHORN_SURVIVAL/GTA7 side reuses the existing `IdunaClient` pattern almost directly. Proposed: new IDUNA `internal/chatbridge` package (same shape as blog/tyler), `ChatYell`-only bridging (not Tell/Guild), `chatbridge.write` permission. Rate limiting/spam handling deliberately left undesigned -- punted until real usage exists to design against, not guessed at. 4-phase plan (IDUNA → EINHORN_SURVIVAL → GFD → rate limiting), GFD ordered last since it's a live already-running UDP server. No code written yet
| TYLER-TYPING-NORTH | SHANKPIT/docs2/NORTHSTAR_TYLER_TEACHES_TYPING.md | 2 | 1500 | Promoted from a blog-post bit (`okemily.com/blog/tyler-teaches-typing/`) to a real northstar, founder real-time: "convert the cave option on the shankpit menu that does some cool typing on the screen... mvp vs0 of tyler teaches typing." Checked real code before speccing: `packages/simulation/cutscene.c`'s `CutsceneState`/`cutscene_tick` typewriter reveal is purely timer-driven (`CUTSCENE_TYPEWRITER_MS` per char) -- no keystroke-matching loop exists anywhere yet, that's the actual gap VS0 has to build, not just a re-skin. Concrete plan: new `TypingLessonState` sibling struct (reuses `CutsceneSlide`'s line shape for source text), new keydown-driven advance function, `draw_tyler_cutscene`'s rendering reused with one real change (color already-typed text by correctness). Real open decision flagged, not assumed: does VS0 *replace* the real, already-shipped `LOBBY_STORY_CAVE` ("CAVE-001") menu entry, or add an adjacent new one -- Load-Bearing principle (removing shipped content needs a founder call, not a silent default). VS0 implementation NOT started this pass -- northstar promotion only

| GFD-MOD-SURFACE | GoblinFoxDragon/docs2/MOD_SURFACE_NORTHSTAR.md | 2 | 1500 | Founder: "develop a mod surface first... reference vintage story... and the wc3 map editor... think of redgarden almost as a map editor that is a game that doesn't have the map editing built into it," then a standing MO: "game expansions need to be mod api first then add the feature." Real prior art found and cited rather than re-invented: the EduScript VM (packages/education/) already exists and is live in apps/lobby, but has zero reach into apps2/battlegrounds_gui (the FPS "edu edition" client the founder specifically prioritized), which currently has no mod/plugin surface at all. Scopes solid buildings, a destructible-environments engine (recommended to share one system with the real R6-Siege-style destructibility spec already in the separate /home/fatbaby/skateboard/NORTHSTAR.md rather than building two), skate-culture tech, and faction hooks (citing TRAPX/GTA7's real FactionManager as precedent) all as mod-surface consumers -- data-driven properties, not hardcoded engine branches. Scripting-language choice (EduScript-extended vs. Lua vs. a founder-invented language not yet located) explicitly left open. A separate, bigger cross-repo idea surfaced in the same stretch -- unifying both GFD clients under PITVIPER as a "multiverse portal" hub across SHANKPIT/REDGARDEN/GFD/BRAWLPIT, including queueing into different REDGARDEN lobby types (OG Battlegrounds vs. the R&D AI-squad-model lobbies) -- is flagged, not scoped here.
| SARENA-NORTH | JEWEL/docs/NORTHSTAR_SARENA_NOTEBOOK.md | 2 | 900 | Real v0 shipped 2026-08-28 (founder real-time: "continue working on SARENA notebook" -> "the notebook like jupyter but for parena" -> "docs are somewhere" -> "in backlog maybe" -- confirmed as S201-07, EMILY/BACKLOG.md SECTION 201). Scopes and documents SARENA_NOTEBOOK's own real custom PARENA notebook frontend, explicitly distinct from JEWEL (the kernel this frontend talks to via the standard Jupyter REST+WebSocket API). Real TYLER visual grounding (grepped actual episode scripts: "TITLE CARD (white text, black field)"), real note-cell markdown rendering, real end-to-end verified execute_request/execute_reply round trip against the live kernel (a Python client replicating the exact browser JS logic, including the real XSRF cookie/header handshake). Deployed: sarena-notebook.service (127.0.0.1:8891) + new /sarena/ broker route + nginx location (queued, rides the existing pending /jewel/ deploy) + IDUNA portal link. libplot/SDL-native explicitly deferred per founder's own "html first... SDL native second." |
| ARENA-API | ECOWAR/docs/ARENA_API.md | 2 | 900 | Phase 1 of ECOWAR-MAPEDIT-NORTH ("arena apis first we wil build ecowar ontop of them"). Documents the real, already-working PARENA mod-hook ABI shared by REDGARDEN and ECOWAR: module naming (stdlib/redgarden/*.prn vs. stdlib/ecowar/*.prn), the two real shapes (trigger-only -- mod calls back into a redgarden_host_* C function; real decision logic -- ordinary PARENA branching/arithmetic, no #target escape, ecowar/card_effect_mod.prn's own precedent), the generate-commit-wire build process (parena build -> commit the .c -> add to scripts/build.sh's hardcoded list -> call by mangled name from arena_game.c), and VS0's real current limits that shape it (no F32/Vec/struct across the boundary). Full current mod inventory table with LIVE/NOT-YET-WIRED/DEFINED-BUT-NOT-CALLED status per mod, confirming by grep that ecowar_resolve_card_effect (the card system) has zero real callers anywhere -- Phase 2's own first task. Not a new mechanism -- names and consolidates what already exists. |
| ECOWAR-MAPEDIT-NORTH | ECOWAR/docs/NORTHSTAR_MAP_EDITOR.md | 2 | 1400 | Founder, real-time, long burst (2026-08-27): "ok all in on the ecowar map editor lets add ecowar features fof the cards and all that and the map editor at the same time" -> "parena mod powered development" -> "bundle parena editor into the map editor" -> "same thing bundle the entire parena and ecowar source in with a file tree to edit the files" -> "a way to make new parena mods... should come a little later" -> "build the ecowar cli on top of parena... built into the map thingy... just the cli will work no other bash stuff to start... i mean it can find bash i guess like it does with gitbash... not an early requirement" -> "arena apis first we wil build ecowar ontop of them... start from a bare engine and build on top of it with mod surface" -> "order it where you think" (phasing delegated). Scopes two real, related deliverables: (1) a documented "Arena API" mod-surface ECOWAR's card/RTS features get built on top of as real PARENA mods, not baked into the C engine; (2) an ECOWAR Map Editor tool bundling the already-real PARENA editor (dual file-tree/Compile/Save, this same session's own work) plus a real map-editing surface plus a PARENA-implemented CLI (replacing scripts/*.sh/*.bat). Real, confirmed-live gap flagged: ECOWAR's own "map" is currently hardcoded C constants in arena_game.c, not a real separate format -- a real prerequisite for any map-editing UI, named as its own phase. Real 6-phase order proposed (Arena API v0 -> card/RTS mods -> map data format -> Map Editor tool -> ECOWAR CLI -> mod-scaffolding workflow, deferred), open questions on stdlib/ecowar/*.prn's real location and map-format specifics flagged, not resolved. Scoping only; no phase implementation yet. |
| LINNEN-NORTH | PARENA/docs/NORTHSTAR_LINNEN.md | 2 | 1200 | Founder, real-time: "we need a widget at the bottom to open settings" -> "figure out the basic settings? zoom i dunno something relevant we need it using parena react whatever the widget library framework is called" -> "have that framework called linnen" -> "northstar it". Names and scopes PARENA's own real, native UI widget framework -- already real and shipped as stdlib/editor/widget.prn's own Toggle type (the auto-indent/file-tree status-bar controls), not a new system being introduced. Explicitly NOT a virtual-DOM/React-style reconciliation framework despite the founder's own "parena react" pointer to the category -- real, plain-data widgets (defstructs) plus real, explicit host-called render functions, matching VS0's own no-struct-mutation limits (functional-update shape, same as Buffer). Real next increment scoped: a Settings panel (a third bottom-bar Toggle button, a Spotlight-style modal box), v0 content = a Zoom control reusing the existing Ctrl+scroll/Ctrl+-+ zoom_percent logic. Real open questions flagged, not resolved: editor-only vs. general PARENA UI toolkit; whether a declarative/component layer ever gets added; whether the file-tree/Spotlight's own hand-rolled render code become real Linnen widget types later. Distinct from STDLIB.md's own separate, still-speculative editor/ui.prn (a MOD-FACING API for third-party plugins) -- not the same thing. Scoping only; the Settings-panel v0 implementation follows in the same session. |
| PARENA-NORTH | PARENA/NORTHSTAR.md | 1 | 2000 | New from-scratch language, founder real-time: "LANGUAGE SPEC FOUND" -> "PARENA UPSTREAM" -> "ITS AN EDITOR AND A LANGUAGE" -> "its like EDU scripts scary older sister" -> "then northstar that bitch and build it pure before we have to think about how it plays with EDU script." Grounded 1:1 in a real uploaded spec document (Building Your Own Integrated Development Environment.docx): S-expression syntax, compile-time region-based memory safety (Region(Source)>=Region(Destination) escape checking, no GC/manual free), linear ownership for native resources, multi-target compilation (C first, then JVM/TypeScript/WebAssembly), and an editor/plugin API namespace as a first-class design element. Positioned as GoblinFoxDragon's mod-surface scripting-language candidate (docs2/MOD_SURFACE_NORTHSTAR.md's previously-unlocated "invented language") but that integration is explicitly deferred. VS0 scope (parena-c: S-expr parser -> region analyzer -> C99 emitter) and its Definition of Done are copied directly from the source document, not invented -- concrete and buildable, not yet implemented.
| LADYBUG-NORTH | ladybug/README.md | 2 | 1500 | Founder, real-time: "golden docks ladybug as the official einhorn industrial bdd framework." EINHORN_INDUSTRIAL's official BDD testing framework for PARENA, published as its own standalone repo (github.com/emilyspringerton/ladybug) the same way Ginkgo/Gomega are separate repos from the Go toolchain -- not bundled inside PARENA/stdlib alone. Beetle-naming lineage: firefly.prn (testing.T-equivalent), firefly/ladybug.prn (Gomega-equivalent matcher chain -- renamed from firefly/gomega because "gomega" broke the beetle convention, founder: "can we pleasae give a beetel name to our bdd framework not gomega"), firefly/gomega.prn (kept as a back-compat alias, exported API unchanged), scarab.prn (Ginkgo-equivalent describe/context/it runner). Honest current status, not glossed over: source-published, passes VS0 domain 1 (parse) and domain 2 (region-analyze) cleanly (real, current Bazel/CI targets), but domain 3 (parena build, the C emitter) is genuinely blocked on real pre-existing PARENA gaps also tracked in PARENA/STDLIB.md -- Vec as a generic field type, &Any/&mut T reference types, non-zero-argument typed Fn callback parameters, and (scarab only) multi-field defenum payloads. CI wired to the real, currently-passing parse+analyze bar rather than a build step that would legitimately fail today.
| MOLTBOOK-NORTH | EMILY/docs/NORTHSTAR_MOLTBOOK_INTEGRATION.md | 2 | 2000 | Founder, real-time: "how can we start posting to moltbook safely?" -> "do we need a relay server for openclaw or something?" -> "federated EMILY?" -> "ok its a facebook for ai agents" -> "we probably need to bring in a round of hardening first" -> "northstar it". Researched, not assumed: Moltbook (launched 2026-01-28, acquired by Meta 2026-03-10) is a Reddit-style social network where only AI agents post/comment (humans observe-only); real participation requires OpenClaw, directly answering the "relay server" question -- this rides on the existing OPENCLAW-NORTH integration (S170-03a), not a second relay. Real, documented security incidents cited plainly, not glossed over: a Moltbook backend exposure leaked agent login tokens/API keys and left public-table write access open to injection; active, documented bot-to-bot prompt injection on the platform (agents tricking other agents into leaking credentials, running destructive commands, deleting accounts); widespread plaintext credential leaks (~/.moltbot/, ~/.clawdbot/) across OpenClaw instances connected to Moltbook. Core safety framing: posting pre-composed content is low-risk; reading Moltbook's feed back into any agent's context is the real attack surface and must go through the same untrusted-input quarantine posture IDUNA/DIS already hold inbound traffic to. "Federated EMILY?" answered directly: Emily Prime's own agent loop should not be the thing reading Moltbook's raw feed -- a narrowly-scoped, disposable, credential-light identity should, if/when this ships. VS0 scoped as publish-only, read-nothing-back, still blocked on OpenClaw's own deployment-isolation call. No implementation yet.
| PAPERCRAFT-NORTH | PAPERCRAFT/NORTHSTAR.md | 2 | 1800 | New standalone repo (2026-08-28), superseding the original scoping doc at `skateboard/NORTHSTAR.md` (that repo's own doc stays as the real historical audit trail, no longer the live location). "Minecraft meets GTA3, plus Skate2" / "Skyrim meets Cyberpunk": a single-node persistent, online-only sandbox (explicitly not RPG-first -- "gfd is designed to be the long running rpg papercraft is a sandbox with mmo dna") in the TRAPX universe, non-voxel destructible city ("instead of minecraft we iterate shankpit into papercraft" / "not voxels" -- built on SHANKPIT's own lineage, not GFD's voxel engine). "Mods first everything": `PARENA/stdlib/papercraft/level_mod.prn` (real I32 RPG level-for-XP decision logic, real recursive multi-level-jump handling, corrected mid-session to use the real construct XP curve -- "keep the experience gain from the construct" -- 80+(level-1)*35, `SHANKPIT_CONSTRUCT.txt`'s own `MatchProgression`, not an invented curve) already compiled via the real `parena build` CLI and committed+tested (`bazel test //...` green) -- direct continuation of the exact real pattern `WEAKNIGHT_BEDROCK_RACERS`' own `racer/bike-gear-mod.prn` proved the same session. Real sibling relationship to `WEAKNIGHT_BEDROCK_RACERS` flagged both directions ("bedrock racers can evolve into papercraft," itself "like the esports, league of legends meets rocket league"), no merge/hard dependency. `SHANKPIT_CONSTRUCT.txt` (repo root) is the real reference -- `SCENE_CITY` is the real default spawn; third-person combat real but tech-tree-gated/deferred, not a blocking prerequisite; no quests (GFD's own domain) or missions to start; a real talent/tech-tree "bones" system (`MatchProgression`'s own `unspent_points`/`ability[5]`) and a PARENA-powered map editor from day 0 both flagged as real, not-yet-built follow-ups. Multi-tenant MMORPG portfolio planning (GFD + Papercraft + an unnamed future high-fantasy grind MMO) and shared-currency-naming-separate-economies ("Flow") both flagged as real future considerations, not built. No server/client host code yet -- NORTHSTAR + one real verified mod only. |
| PAPERCRAFT-PAPERENGINE | PAPERCRAFT/docs/NORTHSTAR_PAPER_ENGINE.md | 2 | 1600 | Real destructible-geometry design + proof of concept, founder real-time same session as PAPERCRAFT's own founding: "build the paper engine for destructable geometries" -> "a simple cube needs to get subdivided then like the vertexes randomiuzed" -> "then some of those faces come off when you hit it with a shot gun." Real technique: subdivide a cube into an NxN fragment grid per face, jitter each vertex a real bounded amount along its own face normal, DETERMINISTIC per seed (server/client agree, no drift) -- the real "papercraft" low-poly faceted look. Mods-first split: real PARENA decision logic (`PARENA/stdlib/papercraft/paper_fragment_mod.prn` -> `packages/simulation/paper_fragment_mod.c`, material-aware damage resistance + threshold-based damage-tier lookup, same real ABI card_effect_mod.prn/bike-gear-mod.prn already proved) with the actual F32/struct mesh math staying in host C (`packages/common/paper_mesh.h`) -- VS0's own real current ceiling, not a shortcut. Verified live: 96 fragments from a 4x4 cube, real seed-determinism, a real local "shotgun blast opens a hole, not the whole cube" scenario (4/96 fragments detached). `bazel test //...` 3/3 green. **Live-wired into the real game loop since (2026-08-28)**: real `PC_PACKET_INTERACT`, a real world-positioned test prop, real per-object interact targeting once the real map editor (`apps/mapeditor`) shipped multi-object support -- see PAPERCRAFT-NORTH's own updated entry. Real, flagged, still NOT built: full granular ("sand-level") simulation named as the honest true-northstar but "thats going to get weird" -- concrete-style real-time fracture named as the actually-tractable middle ground; a real in-world "workers building shit" repair/rebuild event; wet-material drying stages with a real two-way transfer mechanic; real physics/collision for a detached fragment; real integration with the city's own `VoxelBlock` geometry (only standalone props are destructible so far). |
| PAPERCRAFT-MODDING | PAPERCRAFT/MODDING.md | 2 | 1400 | New (2026-08-28), founder real-time: "build the parena editor in and the whole parena language so someone could mod it and then compile and start a server on their local and connect to it." Real finding: the underlying source-level pipeline this describes already existed (proven 5x this session writing real mods) -- this doc is the real, verified, step-by-step guide for it, written and confirmed live using a brand-new mod (`xp_award_mod`, real bonus XP for fully destroying a real Paper Engine world object, ported from `SHANKPIT_CONSTRUCT.txt`'s own real per-kill XP value) built specifically to prove the doc, not reusing an existing mod. Walks: build the real `parena` compiler (`PARENA`'s own `make`) -> write a real `.prn` mod -> `parena build` it -> wire it into `apps/server` (three real, small edits, shown verbatim) -> `bazel build`/raw `gcc` -> run the server locally -> connect a real client -- verified live end to end, server log confirmed `"Player slot 0 destroyed world object 0 -- +60 real xp_award_mod XP"`, exactly matching the mod's own independently-asserted test value. Honest, explicit "What's honestly not here yet" section: no dynamic/hot-pluggable mod loading (every mod needs a host rebuild), no embedded in-game PARENA editor (a modder needs a second repo + its own CLI, not an in-game menu -- that's `NORTHSTAR.md`'s own "longer-arc modding vision," still real, later work), no live-server reload. |
| PAPERCRAFT-WEBCLIENT | PAPERCRAFT/docs/NORTHSTAR_WEB_CLIENT.md | 2 | 1500 | New (2026-08-30), founder real-time: "how do we translate sdl2 to ts opengl? can we publish papercraft as a native js client no weird mscrimpten?" -> "if we route more of papercraft through parena plugins we can start to eat at the interface boundaries" -> "yes start the northstar scoping pass." Scoping only -- no client/bridge code written yet. Real, direct answer settled first: no mechanical SDL2->WebGL translation exists (a native TS+WebGL client is a genuine rewrite of the rendering layer, not a port); real, unavoidable blocker named: browsers cannot open raw UDP sockets at all, so `papercraft_protocol.h`'s own real wire format needs a real bridge -- WebRTC DataChannel (`ordered:false, maxRetransmits:0`) chosen over WebSocket, since it preserves the real unreliable/unordered semantics `PcSnapshotPacket` already assumes, unlike TCP's head-of-line blocking. Real, deliberate bridge shape: a dumb, real byte-for-byte relay process (`apps/web-bridge`, real candidate: Go + `pion/webrtc`), NOT a second game server -- all real gameplay logic (PARENA mod dispatch, snapshot broadcast) stays in the one, existing, unchanged C server. Real repo-placement call: `apps/web-client`/`apps/web-bridge` live inside this repo (not a new top-level repo like SAND/JEWEL/TTT/EXODUS), matching `apps/client`/`apps/mapeditor`'s own existing "multiple real client apps, one repo" precedent, since the wire-protocol contract stays co-versioned with its own real source of truth. Real "eat the interface boundary" tie-in to PARENA's own new TypeScript emitter (`src/emit_ts.c`, S206-71/73) named as a real, promising direction, not committed to as Phase 0 scope. Real Phase 0 named: a browser tab renders a triangle and echoes one real UDP round trip through the bridge, nothing else -- same "smallest real proof point" discipline as every other real Phase 0 in this monorepo. Real risks named honestly: NAT traversal (low risk for this specific "browser to one known server" topology, not zero), WebGL2 vs. desktop OpenGL feature parity (unaudited), browser tab backgrounding/throttling (a real, unconfigurable browser-platform behavior). |
| BURROW-NORTH | BURROW/NORTHSTAR.md | 2 | 1600 | New standalone repo (2026-08-30, upstream GitHub repo pre-created by the founder before the ask). **Real scope corrected in real time**: founder's first message ("can we build project BURROW the golang emitter where we design it to run with the gc turned off") read as "PARENA's fourth compilation target, one more emitter" and was drafted that way -- immediately corrected: "project burrow is a feature for feature rewrite of the parena compiler in golang and parena" + "like the first test is can we write a pure golang and parena tool that still pass all that parena c tests" + "dog food it like write the golang in a way that doesnt allocate on the heap or whatever" + "use parena primatives adding to the stdlibs when it helps." Real, corrected scope: a full, feature-for-feature parallel rewrite of the ENTIRE PARENA compiler (lexer/parser/region-analyzer/every emit target: C/TypeScript/Java), in a real combination of Go and PARENA itself, leaning directly on PARENA's own already-in-progress self-hosting effort (`selfhost/lexer.prn`, `PARENA/NORTHSTAR.md`'s own "Self-hosting" section) rather than a from-scratch hand-written Go implementation -- PARENA stdlib primitives reused/grown "when it helps," not a hard mandate. Real, founder-named, falsifiable Phase 0 acceptance bar: pass the exact same real `.prn` test corpus `parena-c`'s own C test suite already covers (test_lexer_parser/test_region/test_emit/test_emit_ts/test_emit_java), not a freshly invented one. The original GC-off design question (Go's `debug.SetGCPercent(-1)`/`GOGC=off` are real, standard, whole-process mechanisms; v0-shape generated functions never allocate on the Go heap by construction, making GC-off a safe host choice, `GoblinFoxDragon` named candidate) still applies, now correctly scoped to just the new native Go emission target (Phase 5) this larger rewrite would still add -- plus a new "dogfood it" directive extending that same low-allocation discipline to `burrow`'s own Go implementation (value types, pre-sized slices, reused buffers, no `interface{}` in hot paths). Real, honest scale flag: this is a genuinely large, multi-phase undertaking, not a same-afternoon addition like emit_ts.c/emit_java.c were. **Real status (2026-08-30): Phases 1-2 (lexer + parser parity) shipped** -- `lexer.go`/`parser.go`, real, faithful hand-ports of `PARENA/src/lexer.c`/`src/parser.c`+`ast.h` (the same hand-port fallback since `selfhost/parser.prn` doesn't exist either), verified against all 23 real assertions from `test_selfhost_lexer.c`+`test_lexer_parser.c` plus a full 111-file real-corpus stress test (zero failures) and a structural cross-check against `parena parse`'s own dump (exact match). Region-analyzer/emitter parity not started. See also DUNG-NORTH below for a new, real, separate sub-project in this same repo. |
| DUNG-NORTH | DUNG/NORTHSTAR.md | 2 | 1600 | New standalone repo (2026-08-30, upstream pre-created by the founder). Founder real-time, corrected twice: first scoped inside BURROW (`BURROW/DUNG.md`, now removed) via "ok and rewrite pitviper and the parena editor into burrow" -> "call it DUNG" -> "aka the burrow editor" -> "and terminal emulator" -> "terminal emulator comes down as a visor" -> "and there is also split pane use i3 primatives" -> "like the editor and the terminal in the same window and then we like slurp in another file via drag another file onto it or load it from internet it opens up a new chat window inside the shared window" -> "EMILY os design primatives ux and affordances" -- then corrected again: "release DUNG as its own repo bazel built parena from the ground up port of parena editor" -> "that uses burrow" -> "use the real burrow cli to build the product." Real, corrected scope: DUNG is its own standalone, Bazel-built repo; the editor domain is a ground-up port of PARENA's own real `stdlib/editor/*.prn` (13 real files, plus a real, already-built `editor-demo` binary), written as fresh PARENA source compiled via the real `burrow` CLI (not `parena-c` directly) -- DUNG is BURROW's own real, live, flagship dogfooding consumer. **Real, honest current blocker named directly**: `burrow build` reports "not yet implemented" today (BURROW's own Phase 3-4, region analyzer + emitter parity, hasn't started) -- DUNG's own real build is gated on that landing first. Terminal domain (`PITVIPER` rewrite, 4066+ real lines of already-working Go) stays Go-hosted, not PARENA-compiled -- no PARENA target has attempted SDL2/PTY. Real, explicit non-goal: `PITVIPER` itself is untouched, same "new fork, not rewrite-in-place" precedent `SAND` already set. Real UX foundation ADOPTED DIRECTLY: `EmilyOS/docs/legacy-archive/gui-v0.1-design-capture.md` already specifies a "tmux x i3 hybrid" layout model, a no-single-click double-click-speed interaction contract, and posture-aware non-modal denial feedback -- found before writing anything new. New chat-pane feature named (drag-and-drop/load-from-URL opens a scoped Emily Prime chat pane, matching `PITVIPER`'s own Milestone 4). Real, phased plan: DUNG Phase 0 (unblock the real toolchain, depends on BURROW) then Phase 1 (a visor-style drop-down terminal that can i3-split, Go-hosted, doesn't wait on BURROW) then Phase 2 (first ground-up PARENA editor slice, buffer.prn, gated on Phase 0). Real risks named honestly, not glossed over. |
| KUBERNETES-MIGRATION-NORTH | PRRJECT_FATBABY/docs/northstar/KUBERNETES_MIGRATION.md | 2 | 1600 | New (2026-08-30), founder real-time: "need to start the kubernetes migration im not even kidding look up the plan in fatbaby all our logstreaming data is going to get projected into google cloud storage" -> "the data is already there." The plan already existed: `docs/architecture-distributed-event-intelligence.md`'s own Phase 5 names Kubernetes as the control plane once workload fanout includes multiple autonomous services -- this doc makes it concrete for the real target (GKE, GCP-based org) and confirms "the data is already there" is real: `emily backup run --target fatbaby` already uploads curated `var/` state to a real, live GCS bucket (`emily.cli/cmd/backup.go`). Real, honest finding checked before planning around it: the parent doc's own Phase 2 (dual-sink fanout, `internal/eventsink/`) exists as scaffolding -- real interfaces, a real GCS-XML-API-compatible `S3Uploader`, real tests -- but is wired into NO running `cmd/` process; every real pipeline process today still reads/writes `./var/<name>/` directly (Phase 1's own real model), meaning a k8s Pod's ephemeral filesystem would lose that state on restart unless a PersistentVolumeClaim is used OR Phase 2 gets finished for real first -- a real, undecided architectural branch flagged for the founder, not resolved unilaterally. Real, phased plan (PVC-first by default): 5.0 containerize one representative service (`dashboard`, chosen for needing no external API keys/network to start) -- `docker/dashboard.Dockerfile` written, real, honest, UNVERIFIED (no `docker` binary in the authoring session); 5.1 a real GKE Autopilot cluster -- blocked on real `gcloud auth login` this session doesn't have; 5.2 PVCs for today's real local-disk state; 5.3 real Deployment+Service manifests, one process at a time -- `k8s/dashboard.yaml` written as the first, real, reviewable IaC (YAML-syntax-validated, not cluster-applied); 5.4 cutover one process at a time, old and new running in parallel, stateless/lower-stakes first, data-critical (`secwatch`/`eps-reconciler`) last per `THE_EMILY_WAY.md` Principle 15 (Operational Health Is Not Optional). Real risks named honestly: the PVC-vs-Phase-2 branch, the unverified Dockerfile, no cluster yet, ~15 more processes still needing their own manifest, secrets currently in plain env files (real, separate pre-cutover work). No code beyond the one Dockerfile/manifest pair written yet.
| LO-NORTH | LO/NORTHSTAR.md | 2 | 1600 | New repo (2026-08-30), upstream pre-created ahead of the ask. Founder real-time: "LO upstream and design doc added - check it over it needs to compile into java ts c and go via parena cli and burrow cli." A hyper-minimalist esolang (emojis + colons + one literal vector-constructor string as the entire alphabet, nested ternaries over PARENA's own real base4 state space) under a real, higher-level Lisp-like frontend (qi), compiling to real .prn source for the existing parena/burrow CLIs -- no new backend emitters of its own. Real, critical review of the source design doc (LoLanguageSpec.pdf, a captured Gemini chat transcript), not rubber-stamped: real gaps named (no formal grammar exists, only prose examples; emoji-as-token-alphabet tokenization ambiguity -- grapheme clusters/variation selectors/skin-tone modifiers -- never addressed; loops/recursion and error handling explicitly listed as unfinished in the source material's own final message; a real code-size/compile-time blowup risk in the spec's own let-as-AST-duplication lowering, flagged by the source doc itself, treated here as a real engineering risk not just a style note). Real, necessary architectural correction made: LO/qi emits real .prn text only, not its own C/TS/Java/Go backends -- reuses parena/burrow entirely. Real, current backend capability audit, checked directly against both compilers' own real state: parena (defstruct/defenum/match/loop/Vec/FFI, real and mature) can plausibly reach most of LO's real feature surface once a grammar/lowering pass exists; burrow (scalar params/if/binops/not/flat defstruct+get-field only, no let/construction/defenum/match/loop/Vec) can only reach LO programs limited to scalars and flat structs -- the same real boundary stdlib/k8s/k8s.prn vs. stdlib/k8s/scaling.prn already drew this same session. Real, phased plan: Phase 0 (a real, formal grammar, not yet written) -> Phase 1 (LO-to-.prn text, scalars/flat-structs only, proven compiling through BOTH parena build and burrow build) -> Phase 2 (qi's own real frontend, same scope, the let-lowering risk answered for real, not assumed away) -> Phase 3+ (vectors/matrices/patterns/unions, gated on burrow's own real feature growth, design only). No compiler code written yet -- Spec Before Implementation. |
| LO-GRAMMAR | LO/GRAMMAR.md | 2 | 1200 | Phase 0 of LO-NORTH's phased plan: the real, formal grammar LoLanguageSpec.pdf never produced. Exact lexical token table (codepoints + a variation-selector matching rule, closing the "emoji tokenization ambiguity" gap), EBNF for the ternary/expression language, precedence+associativity (ternary right-associative, no bare-state-as-condition), vector/matrix/row-extraction shapes (N+1 STACK tokens per N-row matrix; empty vectors disallowed in favor of the dedicated VOID token), and a real fix for the GROUP capture-group glyph being non-distinguishing open/close (v1: non-nesting only). Re-derives 3 of the source doc's own worked examples -- one parses clean, one surfaces a genuine gap in the source's own casual example (a dangling AND4 operand) rather than silently patching it. Explicitly deferred: raw-state truthiness, unchecked-MATMUL rejection, the let-lowering blowup problem, loops/recursion/error-handling, and the EQ glyph's exact identity (the source PDF's own text extraction lost it everywhere it appears; provisionally 🟰, flagged for founder confirmation). No lexer/parser code -- grammar only. |
| LO-GRAMMAR-COMPLETE | LO/LO_Formal_Grammar_Phase_0_Complete.md | 2 | 1600 | Founder-uploaded, real, authoritative, LATER Phase 0 grammar consolidation superseding LO-GRAMMAR's own EBNF for everything it covers -- adds SWITCH/CASE/DEFAULT, LAMBDA/CALL, FLOAT/DOUBLE, and a much larger PCRE literal/class/range/escape grammar (character classes, ranges, escaping, capture groups). Real, exact glyph decisions: SWITCH=🔘, CASE=🔹, DEFAULT=🔸, LAMBDA=💠, CALL=📞. §33 names real, deliberate v1 boundaries (no loops/recursion, no nested capture groups, no switch fall-through, no implicit numeric coercion, and -- a real, found conflict with this same week's own S221-01 -- "no explicit access to shadowed outer LET bindings," which this repo's own implementation already exceeds and keeps as a tested superset rather than reverting). SWITCH/CASE/DEFAULT and LAMBDA/CALL are now real, implemented, and verified end-to-end (S221-03/04) using this doc's own exact glyphs, each a real, narrower v0 than its own EBNF (LAMBDA reuses Let/LetRef's depth-index scheme instead of named string-literal params; both single-arg). FLOAT/DOUBLE and the extended PCRE grammar are not yet implemented. |
| LO-QI-NORTHSTAR | LO/QI_NORTHSTAR.md | 2 | 900 | Design-only scoping for `qi`, LO's own named Phase 2 frontend (Lisp-like ASCII surface lowering to LO's EXISTING AST/emitter, zero changes to internal/parser or internal/emitter). Real, concrete answer to "why not just write .prn directly": qi stays LO's own narrow base4-state value space (not general PARENA authoring), and its one real value-add is NAMED variables -- a compile-time name->depth lowering pass emitting the same depth-indexed Let/LetRef/Lambda AST Phase 1 already produces. First-draft concrete EBNF for scalars/Let/Switch/Lambda/Call (Pattern/Match ASCII mirror deliberately deferred to Phase 2c). Real, phased plan: 2a (lexer) -> 2b (parser+lowering, scalar/struct scope, verified through the same real parena-build+cc+execution pipeline Phase 1 already uses) -> 2c (Pattern/Match). No qi code written yet. |
| LO-FRAMEWORK-NORTH | LO/FRAMEWORK_NORTHSTAR.md | 2 | 1000 | "Batteries included" Rails-like framework for LO, a dogfooding vehicle for SHITHUB (self-hosted GitHub, IDUNA-powered IAM) + SHITLAB (GitLab-CI-API-shaped). Spec-only, explicitly sequenced after LO's Phase 1 emitter landed. Real capability check found LO currently has no variables/let, no multi-arg functions, no records, no strings -- a framework needs Phase 2 (qi) at minimum, some of Phase 3+ (String/Pattern) too, not glossed over. Real component mapping: router reuses PARENA/stdlib/base4/pattern.prn's backtracking architecture (over base4 vectors, not chars yet -- real, undecided encoding question flagged); models via qi's future defstruct + IDUNA persistence (not a new storage engine); migrations reuse stdlib/papercraft/note_version_mod.prn's coalesce/eviction/conflict primitives directly. SHITHUB/SHITLAB split real per founder correction (IDUNA for IAM, not PARENA; SHITLAB is CI-API-shaped, not a second git host). "Mono emoji" naming convention (likely monochrome/outline emoji variants for new framework-level tokens) named as a real, open, unresolved question needing founder confirmation before any GRAMMAR.md amendment. Real phased plan: Phase A (blocked on LO Phase 2) -> Phase B (router-only proof point) -> Phase C (design only: models/migrations/SHITHUB git logic). No framework code written. |
| PR-CONTENT-TYPE-TAXONOMY | PRRJECT_FATBABY/docs/CONTENT_TYPE_TAXONOMY.md | 2 | 700 | Real answer to "do we have a content-type hierarchy?": SourceType is the real, existing, already-broadly-used content-type field (press_release/sec_8k+forms/market_movers/emily_commentary), flat and string-literal-scattered, not a formal enum. New SourceProvider field is the real subtype/prtype slot within it (prnewswire/businesswire for press releases), reusing SourceType rather than a parallel content_type/prtype pair. Names how the same shape generalizes to guidance articles/contributed content/third-party ingestion without inventing a new pattern each time. Honest, checked-not-guessed limitation: BusinessWire's real scraper is not built -- businesswire.com 403s this session's own fetch tooling and its RSS needs a subscriber token, flagged rather than faked. |
| MIXFORGE-NORTH | MIXFORGE/NORTHSTAR.md | 2 | 1500 | Real source doc pulled in 2026-09-03: `legacy.txt`, a captured AI chat transcript reaching a real, concrete C++/JUCE/Essentia/RubberBand/yt-dlp/SQLite DJ-app design -- explicitly rejected per the founder's own "not the one discussed, built with PARENA" instruction; commits to PARENA end to end instead, same dogfooding discipline PITVIPER/DUNG/SAND/ECOWAR already follow. Preserved real feature set: paste-a-YouTube-URL track import (optional second URL for a separately-supplied instrumental, not AI stem separation) as the explicit first feature, a SQLite-shaped library, BPM/key detection for harmonic mixing, Traktor-referenced crossfade/beatmatching. PARENA-native mapping table: built-in sdl2 + design-only media/audio|codec for playback, real stdlib/shell.prn shelling out to the real yt-dlp binary for import, FFI-bound libsqlite3 for the library. Genuinely new gap named, not just unimplemented: key/BPM detection has no PARENA story at all anywhere in this monorepo; FFI-binding aubio (the transcript's own lighter-weight Essentia alternative) is the leading candidate pending its own scoping pass. Licensing explicitly, knowingly deferred per the transcript's own founder quote. 6-phase delivery plan (import/library -> playback -> detection -> beatmatching -> streaming). No code yet. |
| EMILY-FOR-BUSINESS-NORTH | IDUNA/docs/EMILY_FOR_BUSINESS_NORTHSTAR.md | 1 | 3200 | **Updated 2026-09-11, the pitch sharpens**: founder real-time, "the rails for 2026 agents is an api that allows you to create apis... iduna pro already has IAM so once you have IAM what do you need? APIs... emily for business is the multiplatform idunapro that lets you just vibe code your own login and apis... but we dont do the llm side we are just the api for claude to use" -- explicit, load-bearing scope boundary: not an LLM/agent-hosting product, purely the IAM+API substrate an agent (Claude) targets. Names `IDUNA_PRO/docs/MULTI_TENANCY_NORTHSTAR.md`'s own Phase 1-4 as the literal Phase 0 this whole pitch depends on (checked: IDUNA_PRO today really is "just a fork of IDUNA for CarePyre," structurally incompatible with fast self-serve tenant creation). Names 3 real, existing options for "an API that creates APIs" without choosing between them (an agent hand-writing Go directly; the already-real, already-shipped LO/PARENA Rails-like framework dogfooded via SHITHUB; a Terraform-like declarative config) -- deliberately unresolved per THE_EMILY_WAY's own "pave the cow paths" principle. Cross-links the new frontend-side doc, `EMILY_FOR_BUSINESS/docs/UNIVERSAL_UI_NORTHSTAR.md`. Original 2026-09-03 scoping pass below, unchanged/uncorrected. Real product-scoping pass (S243-02, 2026-09-03) for founder's own framing "IDUNA IS THE PRODUCT BASICALLY ZERO TRUST SECURITY AGENT NATIVE." Maps real, checked-today primitives onto a zero-trust pitch against what's genuinely missing (multi-tenancy, self-serve onboarding, continuous posture verification, compliance attestation). **Updated same day, founder real-time, resolving the doc's own original Open Question 1**: internal IDUNA is NOT externalized in place -- it stays the backbone AND becomes the control plane provisioning/tracking a new, separate sibling product, `IDUNA_PRO` (repo does not exist upstream yet, named only). Real "DB-per-install" finding: already near-free (SQLITE_PATH-per-process + fresh migrations, no redesign needed) -- the real work is orchestration. Real "pull the custom stuff out" categorization, checked directly against internal/ and http/handlers/: core-generic candidates (auth, store, userlog/unified-logging, the apples audit-ledger MECHANISM) vs monorepo-custom (blog, tyler, mmo/redgarden/shankpit/papercraft tickets, vault, promptoverse, mailinglist, drive, statuspage, backlog-kanban-bridge) vs genuinely ambiguous, not resolved (honorcode's mechanism-vs-content split, subscriptions.go, the well-built but BACKLOG.md-coupled kanban feature). S243-06 (same day): closed a real, decisive routing-capability gap for per-tenant subdomains -- the FatBaby broker (PRRJECT_FATBABY/broker) had NO Host-header matching at all (only bearer-token and path-prefix); new Route.Host + Registry.ResolveByHost, checked before path-prefix, deliberately skips the broker's own Basic Auth since the tenant's own upstream IDUNA is the real auth boundary. Real wildcard-TLS finding: okemily.com's DNS is confirmed on Cloudflare, so a real wildcard cert needs a DNS-01 (certbot-dns-cloudflare) challenge, not the HTTP-01 flow every other cert on this box uses today -- a real, named, unresolved decision point (wildcard-cert vs. per-tenant cert on signup). |
| V16-JS-ENGINE-NORTH | PARENA/docs/V16_NORTHSTAR.md | 2 | 1500 | Real scoping pass (kanban card S189-63, 2026-09-03) for a native-PARENA JavaScript engine, founder-named "V16" (a V8 joke) and explicitly called "table stakes." Honestly sized against VS0's own real, current capability: comparable in scope to building V8/Node.js from scratch, gated hardest on VS0 having NO generic type parameters today (confirmed live again this session). Explicitly out of scope for any v0/v1: JIT, full ECMAScript spec compliance, DOM/browser APIs, npm/module resolution. **Status update (same day, card 34134124 "parena v16 iteratejs engine")**: Phase 1 (generics/representation decision -- commit to concrete, non-generic types, confirmed `(Vec ConcreteStruct)` compiles today) and Phase 2a (`stdlib/v16/lexer.prn`, a real, tested JS tokenizer -- number/string/ident/punct tokens + TokEof, `make test-v16-lexer`) are shipped; a real, live compiler-invocation trap was found and documented along the way (a `.prn` file calling into another stdlib module needs that module's own source passed to the SAME `parena build` call, or a called function's return type silently falls back to a wrong `void *` guess). Parser/AST, interpreter, GC strategy, and stdlib slice remain real, unstarted work. |
| ANDROID-JAVA-TARGET-NORTH | SPIDERBEETLE/NORTHSTAR.md | 2 | 800 | **Moved 2026-09-03** from `PARENA/docs/ANDROID_JAVA_TARGET_NORTHSTAR.md` (now just a pointer there) into a real, dedicated upstream repo, `SPIDERBEETLE`, created by the founder for this exact work (kanban cruise-queue card 32445324, "PARENA android app in JAVA using PARENA using the JAVA emitter"). What's real and shipped, checked into `SPIDERBEETLE` itself: `app/src/main/java/industrial/einhorn/spiderbeetle/generated/BatteryUi.java` (two minimal scalar decision functions plausible for MJOLNIR-class real Android use, low-battery-warning + brightness-clamp), compiled via PARENA's own real Java emitter from `PARENA/stdlib/android/battery_ui.prn` and verified with an actual JDK (javac/java 25.0.4, same version STDLIB.md's own BezierInterp/Humanness three-target proof used) against `smoke_test/Main.java` -- 7 real assertions all pass. Real, genuine PARENA compiler bug found and fixed along the way (in PARENA itself, not this repo): `(not x)` had no handling at all in `src/emit_java.c` (same real gap already fixed in `src/emit.c` and BURROW's own `emit_c.go`/`emit_go.go`), fixed via Java's own `!` operator. What's honestly NOT attempted: a real, buildable Android APK -- checked directly, this sandbox has no Android SDK and MJOLNIR itself (this monorepo's own real Android app) has no `gradlew` wrapper checked out locally either, confirming it's never actually built here, only via real CI/the founder's own machine. Real, concrete next steps named (Gradle skeleton, wiring the compiled `.java` into a real Activity/`BatteryManager` callback, a real CI step with the Android SDK), none started. |
| K8S-SERVICE-MIGRATION-NORTH | EMILY/docs/KUBERNETES_SERVICE_MIGRATION_NORTHSTAR.md | 2 | 1500 | Real, staged, monorepo-wide K8s migration plan for kanban priority-queue card K8S-0000 ("evaluate all of our services in systemd and create a staged kubernetes service migration plan"), companion to PRRJECT_FATBABY's own already-detailed KUBERNETES_MIGRATION.md (that doc's FatBaby-specific plan is Stage 2 here, unchanged). Real, live inventory: `systemctl --user list-units` returns 53 real units, 46 relevant after excluding pure OS/dev-tooling noise. Grouped by real migration risk: Group A (stateless HTTP/API -- the 17 FatBaby processes, gpt2-serve, sarena-notebook, promptoverse-* as real CronJobs) is the natural first wave; Group B (iduna -- the central trust authority, blast-radius-sensitive; jewel-jupyter -- needs the full PARENA toolchain baked into its image) moves carefully, not first; Group C (12 real-time UDP game servers -- GoblinFoxDragon/PAPERCRAFT/SHANKPIT-460/WEAKNIGHT_BEDROCK_RACERS/REDGARDEN+redgarden-stable, confirmed live via each service's own WorkingDirectory= to be two genuinely separate running deployments, not a duplicate listing) is explicitly NOT a first-wave candidate -- real, honest reasoning named (untested UDP LoadBalancer network-path variables vs. a known-good VPS), not a blanket "games can't run on k8s" claim; Group D (einhorn-survival, the live community Minecraft server) is its own special, likely-last case (persistent world state, sustained not bursty resource use -- flagged as possibly cheaper on Standard/staying on the VPS than Autopilot, a real, deliberate exception to the monorepo's own Autopilot-by-default lean); Group E (emily-system, the RSI orchestrator that spawns Claude Code sessions on this very box) is deliberately left un-scoped -- a real, separate, foundational design question, not an oversight. 8-stage real sequence table, Stage 0 (this session) provisioning the real GKE Autopilot cluster `prrject-fatbaby` in `us-central1`. Planning only past Stage 0 -- no service migrated. |
| PARENA-OS-PORTABILITY-NORTH | PARENA/docs/OS_PORTABILITY_NORTHSTAR.md | 2 | 900 | Real answer to kanban priority-queue card PARENA-0001 ("dig into the parena OS implementation... smallest vertical slice... alpine? debian?"). Debian was already solved (this dev box IS Debian-family/glibc); Alpine's real, novel angle is musl libc. Real, no-root musl toolchain built via `apt-get download musl musl-dev musl-tools` + `dpkg-deb -x` (same trick class as the libpcap-dev extraction) + a locally-path-rewritten `musl-gcc.specs`. Real finding #1: `parena-c` (the compiler itself) already compiles clean and statically under musl, zero changes needed. Real finding #2 (the actual blocker): `runtime/parena_runtime.h` unconditionally `#include`d SDL2/SDL2_ttf for EVERY generated program regardless of whether its own `.prn` source used sdl2.prn at all -- fixed via a new `PARENA_NO_GRAPHICS` opt-out macro wrapping both the includes and the whole SDL2 function-impl block (default behavior unchanged, 345/345 tests clean). Real, live, end-to-end proof: a real stdlib program (`sip/message.prn`) built with `-DPARENA_NO_GRAPHICS` under the musl toolchain produced a genuinely static (`not a dynamic executable`), zero-shared-library ELF binary that runs -- the real, concrete "this will run on Alpine unmodified" shape. Honest scope: doesn't prove a real SDL2-using program on musl (needs Alpine's own musl-native sdl2-dev, not available in this sandbox), and deliberately does NOT attempt a full custom-OS/distro-image build (that's the separate, much larger S213/FLASH/image-builder-rpi thread). |
| NATIVE-PCAP-NORTH | PARENA/docs/NATIVE_PCAP_NORTHSTAR.md | 2 | 1200 | Real, phased plan (2026-09-03) for the founder's own explicit real-time correction/expansion of kanban card 435423 ("parena PCAP primatives"): "we want to build pcap into parena on a deep low level... a parena pcap implementation and build the std lib deps it needs into the std lib." Kept as a real, separate, ADDITIONAL capability alongside the already-shipped FFI-bound `pentest/pcap.prn` (libpcap-backed, real cross-platform portability + free BPF-filter-string compatibility), not a replacement. Real, checked-live finding: most of the real "stdlib deps" this ask names already exist -- bit-and/bit-or/bit-xor/shl/shr real binops (added 2026-08-20 for compress/lz4.prn), char-at as a real byte-buffer read -- confirmed live that a real big-endian 16-bit field read is already buildable today with zero new compiler work. The one genuinely missing, new capability: a raw AF_PACKET capture-socket runtime primitive (Linux-only, a real, named, honest platform trade-off vs. libpcap's own real cross-platform BPF/Npcap/AF_PACKET abstraction). Real 4-phase plan: (1) the raw-socket runtime primitive, (2) a real net/wire.prn byte/field-extraction module built from what already exists, (3) native Ethernet/IPv4/TCP/UDP header parsing (pure PARENA, the direct structural sibling of this session's own sip/message.prn), (4) a real, PARENA-native filter (plain predicate functions on parsed structs, NOT full BPF bytecode compatibility -- named explicitly as separate, much larger, later work if ever needed). No code written -- planning only. |
| IDUNA-INVENTORY-NORTH | IDUNA/docs/NORTHSTAR_INVENTORY.md | 2 | 1200 | Real, unified northstar for 3 related priority-queue cards (AI-ITL, II-001, II-102): a personal electronics/component inventory hosted as an IDUNA sibling app (same real 'monorepo-custom behind the portal' pattern as drive/blog/vault), seeded with the founder's own real, named starting inventory (2x Pi B2?, 2x Pi Zero, an Adafruit Feather with a radio module -- uncertainty carried honestly, not guessed away). Flat, simple SQLite schema (free-text category/tags, no rigid taxonomy). Real API surface: plain CRUD plus a genuinely new POST /api/v1/inventory/query endpoint feeding the real, current inventory into an LLM call for AI-ITL's own 'what can I build / what am I missing' ask. 3-phase plan: Phase 1 (schema+CRUD+minimal web UI, II-001), Phase 2 (quick-add/low-quantity flags/tag browsing, II-102), Phase 3 (the AI query panel, AI-ITL). Real, deferred stretch goals named honestly (barcode lookup, photo-based vision cataloging) -- planning only, no code written.
| SIP-TWILIO-GATEWAY-NORTH | PARENA/docs/SIP_TWILIO_GATEWAY_NORTHSTAR.md | 2 | 1200 | Real research-and-planning pass for kanban priority-queue card SIP-001 ("what software do we need to build to be the pipes between our sip phone clients and twilio"). Real, checked-not-assumed Twilio Elastic SIP Trunking requirements (SIP methods, UDP/TCP/TLS transports, IP ACL vs Credential List auth, real signaling/RTP IP+port ranges). Maps onto existing PARENA foundation: sip/message.prn (signaling, shipped this session) + net/udp.prn (transport). Real, valuable finding: a pure RTP relay needs zero codec understanding (raw UDP byte forwarding once endpoints are known from SDP) -- G.711 only matters if PARENA ever synthesizes/consumes audio directly, deferred. Real, newly-identified gaps: SDP parsing/construction (sip/message.prn's own already-named gap), SIP proxy/B2BUA header-rewriting (Via/Record-Route/branch, the hardest real protocol work), multi-fd poll/select concurrency (a genuinely new runtime primitive -- pty-poll-read is single-fd only today), SIP digest auth (needs md5, crypto/hash.prn only has sha256 -- but skippable entirely via IP ACL auth instead). Real 5-phase plan: Phase 1 single-call IP-ACL relay (defers concurrency entirely), Phase 2 real sip/sdp.prn, Phase 3 multi-call concurrency, Phase 4 optional digest auth, Phase 5 deferred TLS/SRTP+G.711 (FFI to a real library if ever needed, not scratch-built, matching crypto/hash.prn's own sha256 precedent). No code written -- planning only.
| UART-SERIAL-NORTH | PARENA/docs/UART_SERIAL_NORTHSTAR.md | 2 | 1000 | **Status 2026-09-09: Phase 1 shipped.** Unified planning + Phase 1 implementation for 2 related priority-queue cards (HW-001, HW-003): UART/Serial stdlib for Arduino-equivalent hardware platforms, grounded in this session's own IDUNA_INVENTORY doc's real hardware (Raspberry Pi, Adafruit Feather with a radio module). Shipped: `runtime/parena_runtime.h`'s `serial_configure_impl` (the one genuinely new termios primitive this doc named -- tcgetattr/cfmakeraw/cfsetispeed/cfsetospeed/tcsetattr, 9600/115200 v0 baud table) plus `stdlib/hw/serial.prn` (serial-open/-read/-write/-close, direct structural sibling of net/tcp.prn). Two departures from this doc's original sketch found while implementing: serial-read is non-blocking/poll-gated (reusing pty-poll-read's technique, since a real device never closes its side), serial-close is Result-boxed with a new CloseFailed variant. `make test-serial`: 13 real assertions against a genuine BSD pty pair. Real, honest, deliberately out-of-scope: the Feather's own radio module is very likely SPI, not UART, unscoped here. Phase 2 (real hardware round-trip) remains genuinely blocked -- no physical USB-serial device in this sandbox, same limitation class as pentest/pcap.prn's own no-CAP_NET_RAW gap. Apple #18620.
| PBX-ASTERISK-NORTH | PARENA/docs/PBX_ASTERISK_NORTHSTAR.md | 2 | 1200 | Real research-and-planning pass for kanban priority-queue card PBX-002 ("full featured PBX add asterisk bindings to parena... fill in the gaps between our low level implementation and a real deal"). Real, checked-live research into Asterisk's 3 real control surfaces (AMI plain-TCP-text on 5038, ARI HTTP+WebSocket, AGI stdin/stdout per-call). Real, decisive finding: AMI's own wire format (\\r\\n-terminated key:value lines, blank-line-terminated) is a direct structural sibling of sip/message.prn's own already-solved header-parsing shape -- not a new parsing problem. Recommends AMI first (plain TCP, ASCII text, real immediate control capability) over ARI (needs WebSocket, a real separate gap) or AGI (only reactive within an already-routed call). Real 4-phase plan: stdlib/pbx/ami.prn login-action-building + message parsing (Phase 1, direct port of sip/message.prn's own parse-headers shape), a real login round-trip against a live Asterisk (Phase 2, honestly named as untestable in this sandbox -- no Asterisk instance available), event parsing + Originate/Hangup/QueueStatus actions (Phase 3), ARI/WebSocket deferred (Phase 4). No code written -- planning only.
| WOTAN-HAT-STORE-NORTH | BRAWLPIT/docs/WOTAN_HAT_STORE_NORTHSTAR.md | 2 | 1200 | A WOTAN-hosted cosmetic hat store for BRAWLPIT, paid in GFD's own real Flow currency, plus a user pixel-editor iteration (kanban WOTAN-999/WOTAN-998/WOTAN-996, WTHS-0000/WTHS-0010). Phases 0-1 DONE (2026-09-03): Phase 0's original claim ("no external Flow balance/spend API exists") was corrected -- IDUNA's GET /api/v1/characters/by-player/:player_id already returns gold_balance synced from GFD, and PATCH .../gold already spends it atomically, no new work needed. Phase 1 shipped for real in IDUNA (commit 5bf170c): hats/character_hats tables + GET /api/v1/hats catalog + buy/equip endpoints, seeded from OKEMILY/hats.html's own mockup, 8 tests green. Phase 2.5 DONE (WTHS-012010): a real GFD Town proxy -- hatshop/hatshop buy/hatshop mine MUD commands buy real hats with real Flow (GFD commit dafccba), live-verified end to end against a redeployed IDUNA instance. Phase 2 (real WOTAN store page), Phase 3 (BRAWLPIT-side rendering, an honestly-named unresolved technical question), Phase 4 (pixel editor, real named moderation/cost-model open questions), and Phase 4.5 (BPHS-00001's "surprise box" AI-generated hat -- scoped not built: real prerequisites exist via HSG-000's live-verified promptoverse hat gen, but async generation vs. the synchronous buy transaction, generation subject, "inflation sync" pricing, and moderation are all real, unresolved gaps) still not built.
| BP-LOBBY-MATCHMAKING-NORTH | BRAWLPIT/docs/BP_LOBBY_MATCHMAKING_NORTHSTAR.md | 2 | 1200 | Real scoping pass for kanban BP-LOBBY-001 ("portal you jump in to find matchmaking, auto-fill 8 random players, no chat/no lives, combat abilities work but dont damage"), Principle 19. Real, decisive finding: real arena client netcode is stubbed today (net_send_cmd/net_tick commented out in apps/lobby/src/main.c) -- "online" mode predicts locally but never actually networks players, the real blocking Phase 0 underneath the whole ask. MAX_CLIENTS is already 8, no protocol change needed there. 5-phase plan: Phase 0 real client-server netcode (blocking), Phase 1 server matchmaking queue w/ bot-fill timeout reusing existing bot_think, Phase 2 client-side portal trigger volume, Phase 3 a new MODE_SANDBOX damage/knockback choke-point flag, Phase 4 "no chat" named as an explicit non-task (no chat system exists to remove). No code written -- planning only.
| BP-LEVEL-EDITOR-NORTH | BRAWLPIT/docs/BP_LEVEL_EDITOR_NORTHSTAR.md | 2 | 1200 | Real scoping pass for kanban cluster BPLE-12441/BPLE-13331 (9 cards: "online BrawlPit level editor," login, design/size/place-platforms, browse user maps, ROBLOX-style web->native map-select launch, embedded in-client browser), Principle 19. Real, decisive finding: levels are compiled C code today (levels/*.c StageDef + physics.h's own hardcoded stage_fd_geo/stage_timeline_geo Platform2D arrays) -- zero data-driven format or runtime loader exists anywhere, a real blocking Phase 0 (same shape as BP_LOBBY_MATCHMAKING_NORTHSTAR's netcode finding). Also found: zero IDUNA integration anywhere in this repo (no login system exists), zero web presence, zero embedded-browser capability in the SDL2 client. 5-phase plan: Phase 0 real level file format + loader (blocking), Phase 1 real IDUNA login (open question: native in-client login UI vs. web-side-only auth handing the native client a launch token, Roblox's own real model), Phase 2 native drag-and-drop editor (first mouse-driven UI this client has ever had), Phase 3 real web map registry (recommends hosting under WOTAN's existing subdomain pattern) + protocol-launch handoff to the native client, Phase 4 embedded in-client browser named as the single most expensive/open-ended piece, with the system browser named as a real, much cheaper alternative covering the literal "browse to choose a map" ask on its own. No code written -- planning only.
| BP-SOCIAL-LOBBY-NORTH | BRAWLPIT/docs/BP_SOCIAL_LOBBY_NORTHSTAR.md | 2 | 1200 | Real scoping pass for kanban BPMM-x-123 ("new mode social lobby either you join a lobby or it starts a new one if it cant find one for you to join even if its just you... they appear in lobby"), Principle 19. Distinct from the matchmaking queues shipped this session (BPMM-12441/12442/BPMM-1202020, one-shot queues that end in a fight) -- this is a persistent, joinable space, no existing infrastructure (STATE_LOBBY is a local offline text menu, zero networking). Real open questions named: does the lobby ever start a match and how; one global instance vs. real multi-instance discovery (this server is single-`ServerState`, no multi-room concept exists); portraits list vs. real walking avatars via the existing PacketSnapshot sync. 3 real options: A (minimal, single global pre-match waiting room reusing existing snapshot transport, recommended), B (real multi-instance lobby discovery, most faithful to the card's literal words, biggest lift), C (extend existing matchmaking queues, under-delivers on visible presence). Recommends sequencing BPUX-12444 (menu readability) before adding a 7th lobby-menu option. No code written -- planning only.
| PC-NORMALMAPS-NORTH | PAPERCRAFT/docs/NORTHSTAR_NORMAL_MAPS.md | 2 | 1200 | Real scoping pass for kanban cruise-queue card PC-06667 ("PAPERCRAFT ENGINE normal maps north star"). Real, checked-live finding: apps/client/src/main.c is deliberately legacy fixed-function OpenGL (glBegin/glVertex, no shaders/VBOs) with cel-shading (S231) done as CPU-side per-face 3-band light quantization, not a GLSL fragment shader. Decisive correction to an initial assumption: paper_mesh.h DOES compute real per-face UVs, but only for paper-fragment subdivision-grid shaping, never passed to glTexCoord2f -- there is no texture mapping in this renderer at all today, so normal maps have zero prerequisite infrastructure to build on. Names the real architectural fork explicitly, as a founder-level decision: pivot to a modern GLSL shader pipeline (the "correct" modern path, bigger lift -- VBOs, tangent basis, shader compile/link) vs. stay fixed-function via GL_ARB_texture_env_dot3/_combine (historically correct pre-shader DOT3 bump mapping, keeps the deliberate legacy-GL identity, harder-to-debug glTexEnvi combine-stage config). 4-phase plan: Phase 0 plain diffuse texture mapping (real, unstarted prerequisite either path needs), Phase 1 the shader-vs-DOT3 fork itself, Phase 2 normal map asset pipeline (hand-authored/baked/AI-generated, undecided), Phase 3 integrating per-pixel lighting back into the existing cel-shading bands. No rendering code written -- planning only.
| INVENTORY-PERSIST-NORTH | GoblinFoxDragon/docs2/INVENTORY_PERSISTENCE_NORTHSTAR.md | 2 | 900 | Real scoping pass for kanban priority-queue card GFD-AH-93944 ("auction house it lets us select choose an item but then it does not show the items in our inventory on the next screen"). Decisive root cause traced live, not guessed: apps2/mud's own p.inventory (a simple id->count map) is never persisted to or loaded from IDUNA on EITHER the telnet or headless connection path -- every mutation site (~25 of them: loot, shop, craft, use) only ever touches in-memory state. Headless sessions (what the GUI/Auction House always uses) get silently idle-evicted after 10 minutes with zero warning, so any founder idle in Town that long (or reconnecting after an apps2/mud restart) sees a genuinely empty inventory -- exactly the reported symptom. IDUNA already has a real Items API (ListItems/CreateItem/DestroyItem) but it's shaped for individually-crafted gear (item_level, crafter_id), a real mismatch with the simple stackable-material model, and has zero callers anywhere today. Real 2-phase plan: Phase 0 a new, correctly-shaped character_inventory IDUNA table + GetInventory/SetInventory client methods, Phase 1 a real snapshot sync (load on connect, save on disconnect/eviction, matching the existing level/XP/flow precedent exactly) -- the real, bounded first slice that fixes the reported bug without touching any of the ~25 mutation sites individually. Phase 2 (per-mutation-site increment/decrement for mid-session crash-durability) named as real, separate, deferred follow-up. No code yet -- planning only, per the founder's own explicit "scope it, don't build now" direction.
| MOB-SPAWN-NORTH | GoblinFoxDragon/docs2/MOB_SPAWN_NORTHSTAR.md | 2 | 900 | Real scoping pass for founder real-time direction (2026-09-04, follow-on to GFD-MD-001): an FFXI-style grid coordinate system (I-7) per zone, a data-driven per-cell mob spawn table with real on/off toggles, and a "same model/texture, different name and difficulty tier" mob variant system. Real, checked-live foundation: every zone's spawn table (HillsSpawns, CavesSpawns, etc.) is a hardcoded Go function with literal Pos values baked in -- no grid concept, no data file, no toggle, no variant/tier system anywhere today. Confirmed dungeon mobs already share the exact same Kind-keyed loot path (resolveKill -> openLootPool -> mobDropReg.DropsFor) as field mobs, so a spawn-table editor built against Kind will cover dungeon rows too -- no separate interface needed for that half of the ask. 5-phase plan: zone grid math, data-driving spawn tables, the IDUNA admin GUI, difficulty-tier variants, then dungeon roster editing. No code yet -- planning only.
| ITEM-BUILDER-NORTH | GoblinFoxDragon/docs2/ITEM_BUILDER_NORTHSTAR.md | 2 | 1200 | Real scoping pass for kanban priority-queue card GFD-ITEM-SUPPLY-CHAIN-000 ("we need a tool for creating and managing the stats on GFD items and weapons etc some items need special programming but we should do that with mods and then expose affordances to the item builder"). Real, checked-live foundation: data/items.json is a real, well-structured FFXI-style stat item table (server/itemdef.go's Registry, real job-mask/equip-restriction logic), hand-edited JSON, no tool of any kind. Decisive finding: there is NO item-use/special-effect mechanism anywhere in this codebase today -- every consumable (Potion, Hi-Potion, etc.) is a real, inert data row; using one does nothing mechanically. Real, existing precedent named for "special programming via mods": PAPERCRAFT's own real --mods-manifest dlopen/dlsym pipeline (MODDING.md, SIGHUP-reloadable, fail-soft), recommended to port rather than invent a new mod-loading mechanism. Real 4-phase plan: Phase 0 wire a real "use item" command (blocking prerequisite -- doesn't exist at all), Phase 1 port the mod-hook mechanism + one real proof-point mod (heal_mod.prn), Phase 2 the authoring tool itself (CLI vs. web page named as a real, unresolved founder-level decision), Phase 3 broader special-programming coverage across the existing consumable roster. No item-use command, mod-loading code, ItemDef schema change, or authoring tool built -- planning only.
| GFD-CONTINUITY | GoblinFoxDragon/docs2/STACK_CONTINUITY_REPORT.md | 1 | 1200 | Real, deep-dive continuity report for GFD-DOX-124 ("do a deep dive stack continuity report linking off the readme fully update the readme with the current state of the world and current direction of the project"), checked directly against the actual source tree/git log/existing NORTHSTAR docs as of 2026-09-04 (950 commits). Covers: the 7 real apps/binaries and their roles, the 44 real server packages grouped by domain, a real decisive architectural boundary found this session (apps2/mud owns all real PvE content, apps2/server-go has zero mob registry/PvP-only), the two real PARENA/BURROW mod-integration paths now live (C via parena build, Go via BURROW's first-ever real host adoption), a per-initiative real status table (dungeons/NM/mod-surface/inventory/onboarding/admin-tooling/performance), honestly-named current gaps (no client render path beyond Meadow/Town, inventory has no durability, Battlegrounds reported broken, per-job levels not separated), and a real, synthesized "current direction" read off the actual pattern of recent work (PvE-content-first, mods-first as a working discipline not just an intention, admin tooling keeping pace with content systems). Linked from README.md, which was also updated in the same pass (fixed a stale docs/ vs docs2/ path reference, replaced the 2026-08-26 status snapshot with a 2026-09-04 one).
| BG-MIGRATION-NORTH | GoblinFoxDragon/docs2/BATTLEGROUNDS_MIGRATION_NORTHSTAR.md | 2 | 900 | Real root-cause investigation for GFD-BG-12444 ("battlegrounds is down... migrating redgarden changes... maybe something broke"). Decisive finding, checked directly: GFD's apps2/battlegrounds_gui has no native matchmaker/arena-server of its own -- every live Battlegrounds match is actually served by REDGARDEN's own live red_garden_matchmaker/red_garden_arena_server processes. GFD's own client-side simulation code is a point-in-time fork (commit 61baafb) that has drifted from REDGARDEN's live, still-evolving copy: protocol.h has a 78-line diff (new MatchFoundMsg seed/mode fields, ground-target cast fields, a new packet type, obstacle_hp array) and arena_game.c has a 1161-line diff (REDGARDEN is 900+ lines ahead: build templates, tree-passive HP, Duck's Smoke Bomb, per-hero zone radius). A struct-layout mismatch this size silently corrupts every packet field after the divergence point -- connections can appear to establish while real gameplay is broken, matching the report exactly. 3 real options named, not resolved: full sync (real, substantial, ongoing recurring cost), pin GFD's queue target to a frozen REDGARDEN build at the fork commit (fast, low-risk, freezes features), or give GFD a genuinely native arena server (biggest lift, only option that ends the recurring drift). No code changed -- diagnosed and scoped only, same "scope it, don't build now" precedent as INVENTORY_PERSISTENCE_NORTHSTAR. Card stays open in the priority queue.
| PER-JOB-LEVELS-NORTH | GoblinFoxDragon/docs2/PER_JOB_LEVELS_NORTHSTAR.md | 2 | 900 | Real scoping pass for GFD-XX-1249 ("every class/job needs to have separate levels... switching to RDM makes me a lvl 10 RDM... every class starts at lvl 1 and can level up to 75"). Decisive finding: apps2/mud's single shared p.charXP.Level across all jobs is real, deliberate FFXI-parity design (working as intended, not a bug) -- the founder's ask is a genuine deviation toward FFXIV/WoW-style per-job leveling. A real, partial mechanism (job.CharJob's MainLvl/SubLvl) already exists but isn't wired into any real stat/gate call site today. Real scope named: an in-memory per-job map refactor is buildable in one pass (~12 call sites), but real durability needs a new IDUNA schema (the single flat `level` column can't hold per-job values) -- same class of cross-repo persistence work as INVENTORY_PERSISTENCE_NORTHSTAR, not attempted blind. One real, unresolved design question named: does the real FFXI sub-job/EffectiveSubLevel mechanic survive per-job leveling, or does it get replaced by N independent job slots. No code changed -- diagnosed and scoped only. Card stays open in the priority queue.
| DUNGEON-HAT-DROPS-NORTH | GoblinFoxDragon/docs2/DUNGEON_HAT_DROPS_NORTHSTAR.md | 2 | 900 | Real scoping pass for kanban BPGFD-INTEROP-000 ("GFD dungeons have a super rare chance to drop brawlpit hats and those hats are sellable on the GFD auction house"). Real, checked-live foundation: GFD dungeons are real/live (DUNGEON_NORTHSTAR.md Milestone 4), a real auction house already exists (server/market/ah.go, no dedicated cosmetics category yet), BRAWLPIT hats already have a real IDUNA-backed schema/API (WOTAN_HAT_STORE_NORTHSTAR.md, shipped). Real gap named: zero drop-chance/loot-roll mechanic exists in GFD combat today (a real special case of Milestone 6 "Rewards + party queue flow," itself unstarted). 3 real open questions named (real drop-rate number; dungeon-exclusive vs. existing-catalog hats; whether a GFD-side sale needs real IDUNA-side ownership transfer). 3-phase plan: loot-roll, cross-repo hat-award API, auction-house listing. No code written -- planning only.
| PROJECT-TAGS-NORTH | IDUNA/docs/PROJECT_TAGS_NORTHSTAR.md | 2 | 700 | Real scoping pass for kanban seed card PMPX-000 ("build out additional project management automations like a page showing the tags for the different projects etc - auto researching on a -000 card"). Real, decisive finding: "projects"/"tags" are not formalized concepts anywhere in this monorepo's data model -- no projects table, no tag column on kanban_cards or Apples. Closest real analogs: BACKLOG.md's own SECTION numbers and Apples' own source_repo field. 3 real open questions named (what counts as a project; overlap with MULTIKANBAN-000's own board concept; what's the real first automation beyond "a page"). Real, minimal buildable-now option named: a zero-new-schema dashboard grouping existing kanban cards + Apples by their own already-real SECTION/source_repo values. No code written -- planning only.
| IDUNA-NOTEBOOK-NORTH | IDUNA/docs/IDUNA_NOTEBOOK_NORTHSTAR.md | 2 | 900 | Real scoping pass for kanban IN-000/IN-001 ("IDUNA NOTEBOOK - icloud like affordances for creating notes... notebooks are sarena based but actually just advertised as regular notes"). Real, decisive finding: the existing SARENA_NOTEBOOK portal link is dev-facing JEWEL (a real code-execution Jupyter kernel for PARENA), not what this card wants -- the card itself names the real mismatch (plain prose notes, no code-cell chrome, even if the backend is PARENA-implemented). No notes CRUD/schema/UI exists anywhere in IDUNA today. 3 real open questions named (JEWEL-backed vs. separate CRUD; personal vs. shared notes; where it's surfaced). 4-phase plan: Phase 1 a real, minimal owner-scoped notes CRUD feature (the smaller, more likely-correct build), Phase 2 a real simple UI (no code-cell chrome), Phase 3 a real PARENA backend only if genuinely needed, Phase 4 IDUNA_PRO extraction later. No code written -- planning only.
| MULTI-KANBAN-NORTH | IDUNA/docs/MULTI_KANBAN_NORTHSTAR.md | 2 | 900 | Real scoping pass for kanban priority-queue card MULTIKANBAN-000 ("the kanban is a good primative i want to move it up the abstraction layer i want IDUNA and IDUNA PRO to give the ability to create multiple kanbans"). Real, decisive finding: kanban_cards is a single, global, hardcoded table -- no board/tenant column, backlog_item_id tied to one specific file (EMILY_BACKLOG_PATH), auth a single shared iduna.admin gate. Real structural mismatch named: IDUNA_PRO's own tenants have no EMILY/BACKLOG.md at all, so a board backed by a specific git file doesn't generalize as-is. 4 real open questions named (what a generic tenant's card even tracks; one schema vs two real products; per-board ACL; fixed vs custom columns). 4-phase plan: Phase 1 a real kanban_boards table + board_id (EINHORN_INDUSTRIAL's own board becomes board #1, zero behavior change), Phase 2 per-board authorization, Phase 3 board management UI/API, Phase 4 custom columns (conditional). No schema/API changes made -- planning only.
| SCAN-PROFILES-NORTH | PARENA/docs/SCAN_PROFILES_NORTHSTAR.md | 2 | 900 | Real scoping pass for kanban PEN-11412 ("PARENA STDLIBS can we start to add algorythms for our homegrown scanner? FAST_SCAN SLOW_SCAN SMART_SCAN SNEAKY_SCAN"). Real, decisive finding: pentest/scan.prn's own scan-ports was never actually implemented -- its inline-c call references a pentest_scan_ports C function that exists nowhere in this repo (only referenced in the compiler's own emit test suite, proving the call CAN be emitted, not that it's linked to anything real) -- the file's own "FFI-bound to nmap" claim was never actually done. Real research: FAST/SLOW/SNEAKY_SCAN map directly onto nmap's own real, official -T0..-T5 timing templates (SNEAKY_SCAN = nmap's own literal "-T1 Sneaky" name); SMART_SCAN has no direct nmap timing equivalent, closest is -A (aggressive/adaptive). Real, phased plan: Phase 0 an actually-linked pentest_scan_ports (blocking -- nothing below is real until this exists, same "real blocking Phase 0" shape found repeatedly this session), Phase 1 real ScanProfile timing parameters mapped to real nmap flags, Phase 2 live verification against real, already-authorized org infrastructure. No code written -- planning only.
| KISMET-WIRELESS-NORTH | PARENA/docs/KISMET_WIRELESS_NORTHSTAR.md | 2 | 900 | Real research-and-planning pass for kanban priority-queue card PENT-0011 ("how does kismet work? what parena primatives needed to build out similar functionality through parena apis?"). Real, researched Kismet architecture: monitor-mode capture (nl80211/driver-level, distinct from libpcap's own promiscuous mode), Radiotap+802.11 frame dissection, device tracking, channel hopping, web UI. Real, checked-live foundation: pentest/pcap.prn already real/working for plain capture; NATIVE_PCAP_NORTHSTAR.md's own byte-level primitive findings (bit-and/or/xor/shl/shr, char-at) directly extend to 802.11's own binary shape, nothing new needed at the language level. Recommends shelling out to the real `iw` tool for monitor-mode/channel control (this session's own established "well-known tool over from-scratch reimplementation" judgment) rather than a from-scratch nl80211 FFI binding. 4-phase plan: Phase 1 a real dot11.prn frame parser (buildable and testable NOW against a static captured sample, no hardware needed), Phase 2 monitor-mode+capture wiring (real code, honestly untestable in this sandbox -- no WiFi hardware here), Phase 3 device tracking (no new primitive needed), Phase 4 web UI (explicitly out of scope). No code written this pass -- planning only, Phase 1 named as the real, immediately actionable next step.
| EOSUI-NORTH | EMILY/docs/EOSUI_NORTHSTAR.md | 2 | 1200 | Real scoping pass for kanban cluster EOSUI-1244/EOSUI-12444 ("implement some super basic html and css renderer for our games... a design language for the interfaces"; "native version and embedded version... interop with web like roblox... especially if we build our own browser"), Principle 19, cross-repo not tied to one game. Real, decisive finding: no shared UI framework exists anywhere -- every game (SHANKPIT's draw_lobby_buttons, BRAWLPIT's own just-shipped draw_menu_button from BPUX-12444) independently reinvents bespoke immediate-mode drawing code, the real pain the card names. PARENA's own stdlib/editor/widget.prn is real but deliberately narrow (one Toggle widget, absolute x/y/w/h, no cascade/box-model/flexbox). Real research into litehtml (the standard "minimal embeddable HTML/CSS engine," tens of thousands of lines after years of hardening) makes Option A (build one from scratch) a genuinely disproportionate real cost vs. the actual stated need. Real fork named: Option A (full HTML/CSS engine, not recommended), Option B (vendor a real existing engine -- B1 embeds webview/webview wrapping the OS's own browser engine, matching MJOLNIR's real Android WebView precedent, generalized to desktop; B2 is Option C below), Option C (recommended: a new, real, lightweight PARENA-native stdlib/ui/style.prn extending widget.prn's own proven pattern with a small flexbox-lite property set -- answers "a design language" without Option A's cost). Recommends Option C as the real v0 plus a timeboxed Option B1 spike in BRAWLPIT (converges with BP_LEVEL_EDITOR_NORTHSTAR's own Phase 4 map-registry browsing) to give EOSUI-12444's own "judge which experience is better" ask a real comparison. No code written -- planning only.
| EXPR-BINDING-NORTH | PARENA/docs/EXPR_POSITION_BINDING_FORMS_NORTHSTAR.md | 2 | 900 | Real scoping pass for backlog S223-02 (let `let`/`match`/`loop` actually work as an `if`'s own condition, not just fail with S223-01's already-shipped clean error). Real, decisive finding: emit_if/emit_expr build ONE C ternary expression string with no statement-emission capability at all, and the naive GNU `({...})` statement-expression fix is blocked by this project's own `-pedantic` build flag (already known from stdlib/datetime.prn). The real existing precedent, g_boxed_types/g_box_helpers (a synthesized `static inline` helper function spliced in before any defn body), doesn't transfer cleanly: its only "capture" is one value parameter, while a let/match/loop body can reference arbitrary outer-scope locals, needing real free-variable capture analysis this codebase has no primitive for (no generic "walk all descendant identifiers in a subtree" utility anywhere in emit.c's ~5000-line dispatch). Named as the deciding factor: getting capture analysis wrong doesn't fail loud, it silently emits wrong-but-compiling C -- categorically worse than the current honest rejection. 3 real options for founder decision: A (narrow first phase, `let` only, no nested binding forms), B (full generality, its own separate design review), C (don't build it -- the existing "extract to a named function" workaround, already used 3x by hand this session, stays the answer). No option picked, no code written -- planning only.
| EMILY-ECOSYSTEM-CR | EMILY/docs/ECOSYSTEM_CONTINUITY_REPORT.md | 1 | 1200 | Real, full continuity report for EMILY-CR-12414 ("the emily ecosystem is a comprehensive thing at this point containing multitudes we need an actual list of all of our repos and the purpose and the status full full full continuity report"), organized and synthesized directly from CLAUDE.md's own authoritative per-repo table as of 2026-09-04, not a restatement of it. Groups ~50 real repos into: the operational spine (EMILY/IDUNA/emily.cli/PRRJECT_FATBABY/APPLES/EmilyOS/PITVIPER), games & interactive worlds (GoblinFoxDragon/REDGARDEN/ECOWAR/SHANKPIT/BRAWLPIT/PAPERCRAFT/WEAKNIGHT_BEDROCK_RACERS/GTA7/EINHORN_SURVIVAL/skateboard/TYLER+MoneyPrinterTurbo/MISHRI), the PARENA language ecosystem (PARENA/BURROW/JEWEL/SAND/DUNG/LO/SPIDERBEETLE/MIXFORGE), the business/product layer (IDUNA_PRO/EMILY_FOR_BUSINESS/WOTAN/CONSOLE), infra/dev tooling (gpt2-alpine-c/SKULDMARK/FLASH+image-builder-rpi/EDIS), and new/stub repos. Closes with 4 real, synthesized cross-cutting themes read off this session's own actual work: mods-first now a working discipline not just a goal, BURROW's Go target proven by its first real host adoption, a real ecosystem-wide kanban/backlog performance fix, forking as this ecosystem's default growth pattern and its real recurring drift cost (named concretely via GFD's own Battlegrounds finding), and persistence repeatedly being the real deferred piece across three separate features this session. Linked from CLAUDE.md going forward for sync.
| CAREPYRE-CONSOLE-NORTH | CarePyre/docs/CAREPYRE_CONSOLE_NORTHSTAR.md | 2 | 900 | Real architecture doc for kanban CPP-124433/CPP-2144333 (CarePyre Portal/Console, "like the IDUNA login and backoffice experience but for CarePyre, separate platform... we rely on IDUNA"). Real, live-corrected mid-build via rapid founder direction: "BUILD IT USING IDUNA PRO SOMEHOW" -> "IDUNA PRO SHOULD BE WHITELABLE BACK OFFICE" -> "WE KEEP OPERATIONS USING OG IDUNA BUT WE NEED TO HAVE IDUNA PRO BE BASICALLY A CONSOLE FOR THE PLATFORM THAT THE IDUNA PRO IS SUPPORTING" -> "build it with iduna pro not into iduna pro". Final architecture: IDUNA_PRO's own source is untouched (zero edits) -- a real, separately-running IDUNA_PRO instance (own port :8081, own SQLite DB, own JWT signing key) is CarePyre's console backend, consumed purely through its already-shipped REST API (register/local-auth/identities-me); a new, real, CarePyre-branded static page (`console.html`) talks to it via a same-origin nginx proxy (`/console-api/` -> :8081), avoiding any CORS/IDUNA_PRO code changes. Live-verified end to end against a locally-booted idunapro instance (register -> local-login -> identities/me all return real, correct JSON matching the front-end's own expectations). Real, honest gaps named: the actual carepyre.org deploy is queued (`sudo-queue/51-carepyre-console-idunapro-deploy.sh`, needs real root), kanban.access is real-but-not-granted to non-webmaster local users so it's deliberately not shown, and no real multi-tenant provisioning exists yet (matches the founder's own "we will need to split out but not now").
| GV-FEATURES-NORTH | CarePyre/docs/GOOGLE_VOICE_FEATURES_NORTHSTAR.md | 2 | 700 | Real feature-scoping pass for kanban CAREPYRE-9311 ("what other google voice grand central features can we add? like voicemail right? hitting a button to start recording a call?"). Real, decisive framing: Asterisk already ships built-in apps for nearly every real GV-era feature -- mostly a config problem. Shipped this pass: real voicemail (mailbox 1000, voicemail_carepyre.conf) as a real fallback for unanswered/busy inbound calls, and always-on inbound call recording (MixMonitor) preceded by a consent announcement -- with a real, named, not-glossed-over gap: the announcement .wav doesn't exist anywhere in this repo yet (no stock Asterisk sound matches it), Playback() fails silently until a real recording or TTS is wired in. Real, scoped-not-built follow-ups: an in-call DTMF recording toggle (needs its own separate consent-UX decision), visual voicemail via the console, SMS (a real, entirely separate Twilio product surface, not related to the Voice/SIP trunking API), call screening, DND/scheduled forwarding.
| BROWSER-PHONE-NORTH | CarePyre/docs/BROWSER_PHONE_NORTHSTAR.md | 2 | 700 | Real, phased plan for kanban CAREPYRE-4143 ("CAREPYRE BROWSER PHONE, remember u used to be able to call from Google Voice?"). Real, decisive finding: Twilio's own Voice JS SDK (@twilio/voice-sdk) is the correct, existing tool for browser-based WebRTC calling, independent of the already-provisioned Asterisk trunk -- not something to build from scratch or route through Asterisk's own SIP/RTP stack. Real, concrete requirements named: a TwiML Application (a real webhook Twilio calls to route each call), real short-lived Access Tokens minted server-side via the same API Key already wired into IDUNA_PRO's internal/twilio client, a new webhook handler returning TwiML, and a real browser dial-pad page. 4-phase plan (backend proof via curl first, then a minimal browser call, then real inbound, then an optional bridge to the existing Asterisk extension 1000 via a <Sip> TwiML noun). No code written -- planning only.
| SIP-QR-ONBOARD | CarePyre/docs/SIP_QR_ONBOARDING_NORTHSTAR.md | 2 | 800 | Real, phased plan for kanban CAREPYRE-42143124 ("qr code onboarding with the sip phone... get it working for what we have going so far"). Phase 1 SHIPPED: GET /api/v1/sip-accounts/me/qr (IDUNA_PRO), a real provisioning payload (extension/sip_server/sip_port/transport) an Android Config screen can auto-fill from -- deliberately never a password field, since sip_accounts never stores one (the real PJSIP secret lives only in Asterisk's own config). Real, named founder decision point left open: keep the password manual (assumed for Phase 1) vs. add a real, encrypted sip_password column later. Phase 2 (render as an actual QR image) and Phase 3 (Android camera scan via ZXing, blocked on the same no-Android-SDK-in-sandbox gap SIP_PHONE_ANDROID_NORTHSTAR already names) are scoped, not built.
| HIPAA-COMPLIANCE-NORTH | CarePyre/docs/HIPAA_COMPLIANCE_NORTHSTAR.md | 2 | 900 | Real operational-framework pass for the founder's own "hipaa is mostly a set of operational guidelines for data sharing can you build that into carepyre?" -- explicitly NOT a legal compliance determination (nobody here is a lawyer). Real, shipped: draft Terms of Service + Privacy Policy (`terms.html`/`privacy.html`, linked site-wide), a new least-privilege "provider" role in IDUNA_PRO (`mail-accounts.provision`, distinct from `users.admin`) letting providers create/manage participant email accounts (both the mailbox AND the underlying local_uid identity) scoped server-side to only the participants THEY created (`mail_account_credentials.created_by`, HIPAA's own "minimum necessary" principle enforced in code, not just UI), an admin-facing provider-role grant checkbox, and a persistent, non-dismissible "this mailbox is unencrypted" banner (forceful PGP encouragement per the founder's own explicit "encourage, don't block" framing) replacing the old collapsed-details-only signal. Real, honest gaps named: no BAA template/process, no hard encryption enforcement, telecom-side (SIP/voice) protections explicitly deferred by the founder, no audit-log UI, no formal risk assessment/breach-notification/workforce-training program.
| SOC2-READINESS-NORTH | IDUNA_PRO/docs/SOC2_READINESS_NORTHSTAR.md | 2 | 900 | Real readiness pass for the founder's own "anything we can do to get ahead of soc 2 compliance ... we want soc 2 on the whole emily for business platform including the platform idunapro offering" -- explicitly NOT a SOC 2 report or a compliance claim (that requires a real, independent CPA audit no document can substitute for). Maps the 5 Trust Services Criteria (Security/Availability/Processing Integrity/Confidentiality/Privacy) against what's real today: ES256 JWT + 4-tier RBAC (CP-HIPAA-2) + append-only Apples audit ledger + unified logging for Security; event-sourced userlog for Processing Integrity; opt-in PGP/S-MIME + minimum-necessary scoping for Confidentiality; the GDPR export/erasure pipeline for Privacy. Real, honest gaps named per criterion: no written infosec policy, no pen-testing/vuln scanning ever run, no incident-response runbook, no monitoring/alerting stack, no backup/restore drill, no vendor/sub-processor registry, no data-retention automation, no DPA template. 5 concrete, not-yet-started next steps named, ending with "engage a real SOC 2 readiness assessor when the founder decides to pursue a report."
| AD-MONETIZATION-NORTH | EDIS/docs/AD_MONETIZATION_NORTHSTAR.md | 2 | 700 | Real ad-monetization policy + architecture for the founder's own "how that data can be used for revenue (TINA -> internal ads for games/emily+/emily for business, vetted advertisers)... we wont share data with them just allow them to put an ad with a link... would you like to advertise? mixed with other stuff as we have stuff to advertise." Policy: no user data ever shared with an advertiser (static creative + link only); external sponsor links get rel="noreferrer noopener" on top of nofollow (house/meta links don't need it); internal-ecosystem-first but real partnerships welcome (named example: Red Bull). Shipped: a real ad pool (`edis_dis_ad_pool` filter) on top of the existing `edis-dis` plugin's health-adaptive `[edis_dis_ad]` shortcode -- GFD (real, live) as the one house entry, a real admin-configurable "advertise here" mailto CTA mixed into the SAME rotation (not a fallback), wired live into the theme footer. Real, honest gaps: no analytics anywhere on the FatBaby site at all, no real vetted sponsor yet, no ad-pool admin UI (code-filter only), only one placement wired (footer).
| DCGAN-PRIMITIVES-NORTH | PARENA/docs/DCGAN_PRIMITIVES_NORTHSTAR.md | 2 | 800 | Real scoping pass (S210-01) for the founder's own "add DCGAN primitives to PARENA." Audits `nn.prn`'s real, current scope (real, working, forward-only activation functions for a `gpt2-alpine-c` port -- no conv2d/conv2d-transpose/batchnorm/autodiff/optimizer/loss/image-loading exist anywhere). Names the real, decisive fork: hand-derived backward formulas per op (Option B, recommended, matches this stdlib's own established "short, hand-rolled, no new framework" style) vs. full reverse-mode autodiff (Option A, needs generic dispatch VS0 doesn't have). Real, phased, dependency-ordered plan: Phase 0 Conv2D (im2col+matmul) -> Phase 1 ConvTranspose2D -> Phase 2 BatchNorm2D (forward) -> Phase 3 hand-derived backward passes for every op -> Phase 4 Adam optimizer + BCE loss -> Phase 5 the real DCGAN architecture/training loop -> Phase 6 real image data loading (deliberately last). Honestly names that a full training run to visual convergence in this sandbox is a separate, likely-infeasible undertaking (no GPU, no staged dataset) -- the real goal is a correctly-differentiable, tested primitive set. Scoping only, no code.
| PARENA-COREUTILS-NORTH | PARENA/docs/PARENA_COREUTILS_NORTHSTAR.md | 2 | 900 | Founder real-time: "let's write our own parena powered busybox" -- motivated by the same session's EmilyOS/Alpine RPi image work, where Alpine's own real busybox --install trigger script is the one remaining privileged blocker before a first bootable image. Real "PARENA logic + C host driver" architecture matching turbogrep/editor-demo/parena-selfhost exactly: tools/parenabusybox_host.c does real busybox-style multi-call argv[0] dispatch, real applet logic lives in stdlib/coreutils/*.prn. v0 ships 5 real applets: echo (real string-join logic), basename (real trailing-slash-strip + last-/-scan + suffix-strip, built from string.prn primitives), pwd (a real new getcwd(3) runtime primitive), true/false (proves multi-call dispatch itself works). Real, honest v0 boundary named directly: no sh/mount/init yet -- the three genuinely hard, load-bearing applets a real boot depends on. Names a real, deliberate tension with NORTHSTAR_DISTRO.md's own "GNU tools stay for load-bearing infra" guidance, not glossed over; whether this ever replaces Alpine's real busybox in the shipped image stays an explicit, later, founder-call decision (turbogrep/turbosed's own "available, not default" precedent). 12 real end-to-end assertions (invoking the actual compiled binary + real symlinks), all pass; make test 347/347.
| SPI-NORTH | PARENA/docs/SPI_NORTHSTAR.md | 2 | 900 | **Status 2026-09-09: shipped same-day.** Real, direct follow-up to UART-SERIAL-NORTH's own named-out-of-scope gap: the Adafruit Feather's likely RFM9x LoRa radio talks SPI, not UART. Shipped `runtime/parena_runtime.h`'s spi_open_impl/spi_transfer_impl over Linux's real spidev(4) API, own top-level `#if defined(__linux__)` guard (spidev has no macOS/BSD equivalent, unlike net/tcp.prn/pty.prn/hw/serial.prn's shared POSIX-wide guard) plus `stdlib/hw/spi.prn` (spi-open/spi-transfer/spi-close over SpiDevice/SpiError). Real, structural departure from the rest of this stdlib's -read/-write pair: one spi-transfer function, since real SPI is synchronous full-duplex (tx and rx clock simultaneously on the same edges) -- correct semantics, not a shape mismatch. Two real, honestly-named limitations found and documented: every String-based host primitive in this runtime is NUL-terminated-C-string-shaped, so a payload needing an embedded 0x00 byte (ordinary for SPI register addressing -- the RFM9x's own RegFifo IS address 0x00) can't round-trip through it yet -- a real core-language byte-buffer gap, unscoped follow-up, not solved here; a failed transfer ioctl isn't distinguished from "device returned all zeros," matching tcp-read/pty-read/serial-read's own existing coarser-signal judgment. `make test-spi`: 9 real assertions scoped to what's genuinely testable with no real SPI controller in this sandbox and no fakeable pty-style stand-in for one. `make test`: 347/347, zero regressions. Real, honest, not done: an actual hardware round-trip against a real SPI device remains genuinely blocked. Apple #18625.
| I2C-NORTH | PARENA/docs/I2C_NORTHSTAR.md | 2 | 900 | **Status 2026-09-09: shipped same-day.** Third real hardware bus in the same session (UART/serial, SPI, now I2C). Shipped `runtime/parena_runtime.h`'s i2c_open_impl (open + ioctl I2C_SLAVE, rollback on failure) + plain read()/write() (i2c_read_impl/i2c_write_impl, no transfer-struct ioctl needed unlike SPI) plus `stdlib/hw/i2c.prn` (i2c-open/i2c-read/i2c-write/i2c-close over I2cDevice/I2cError, own #if defined(__linux__) guard). Real, structural distinction from hw/spi.prn: I2C is addressed/half-duplex-per-direction, closer to hw/serial.prn's/net/tcp.prn's -read/-write shape than SPI's simultaneous -transfer; i2c-read takes an explicit length matching real I2C semantics. Real, found-live testing distinction from SPI: this box has a real, functioning I2C controller (i2c_i801, /dev/i2c-0) but it's deliberately NOT opened/probed -- root-owned, real SMBus carries real system battery/thermal/RAM-SPD traffic, a real risk not worth taking. What WAS honestly testable: i2c-dev's data-transfer path is plain POSIX read()/write() with no ioctl, so make test-i2c's 14 assertions include a genuine byte-perfect round-trip and real short-read handling -- stronger real coverage than SPI's own ioctl-gated transfer offered. make test: 347/347, zero regressions. Same write-side embedded-NUL limitation as hw/spi.prn/hw/serial.prn carries forward, unfixed for the same named reason. Apple #18629.
| BYTES-NORTH | PARENA/docs/BYTES_NORTHSTAR.md | 2 | 1100 | **Status 2026-09-09: shipped same-day.** Real, direct follow-up to SPI-NORTH/I2C-NORTH's own same-day finding: every raw host primitive speaking String truncates at an embedded 0x00 byte (real for SPI/I2C register addressing -- RFM9x's own RegFifo IS address 0x00). Unlike UART/SPI/I2C this is a core-language gap, not a new hardware bus. Investigated FIRST, not assumed: `stdlib/vec.prn`'s own `(Raw T)` design genuinely does not compile ("unsupported return type form") -- `compress/lz4.prn`'s own claim that it's the real precedent is stale; the real working Vec is a separate, hand-built runtime type. That distinction made Bytes tractable: Vec's own complexity comes from being generic over element type, which a fixed-always-byte buffer doesn't need. New `resolve_base_type_name` entry in `src/emit.c` (plus two more allowlists found only by testing) is the entire real compiler change -- `ensure_box_helper` needed zero Bytes-specific code, confirmed by reading it directly: already boxes any non-pointer C type generically, the same path real `defstruct` types already use. New runtime `Bytes` struct + `bytes_*_impl` helpers. Real, live bug found and fixed while implementing: a first draft omitted the `_impl` suffix, causing a real symbol collision with the PARENA-emitted `bytes-alloc` wrapper (the exact class `net/tcp.prn`'s/`pty.prn`'s own header comments already document) -- caught by actually gcc-compiling, not by trusting `parena build`'s own success. New `stdlib/bytes.prn`: `bytes-alloc`/`-len`/`-get`/`-set!`/`-from-string`/`-to-string-lossy`. `make test-bytes`: 18 real assertions including a genuine embedded-0x00-survives-with-length-intact proof. `make test`: 347/347, zero regressions (checked repeatedly through the compiler-change process). **Phase 2 shipped, same day (Apple #18650)**: real, additive `spi-transfer-bytes`/`i2c-read-bytes`/`i2c-write-bytes`/`serial-read-bytes`/`serial-write-bytes` siblings -- every existing String-based function unchanged. Real, byte-perfect embedded-0x00 round-trip proven end-to-end against a real open pty device (`hw/serial.prn`) and a real temp-file fd (`hw/i2c.prn`), not just proven to compile; `hw/spi.prn`'s own test scoped honestly to what's possible with no real spidev controller in this sandbox. `make test`: 347/347 throughout. Apple #18637 (core type), Apple #18650 (retrofit).
| AVR-ARDUINO-NORTH | PARENA/docs/AVR_ARDUINO_NORTHSTAR.md | 2 | 900 | **Status 2026-09-10: shipped v0.** Founder real-time pivot: "lets update parena so that it can run on an arduino... update the arduino editor to be able to upload to an arduino with a button... clicking the button compiles the parena code and uploads it to the arduino... start with making the onboard led blink," clarified as `examples/editor_main.c` ("the parena editor"). Real, checked distinction from the pre-existing `stdlib/hw/serial.prn`/`spi.prn`/`i2c.prn` (a Linux HOST talking TO a peripheral) -- this is PARENA code running natively ON the AVR chip itself, a genuinely different problem. Confirmed the shared `runtime/parena_runtime.h` cannot compile under avr-gcc (Linux-syscall-heavy: sockets/pty/mmap/spidev/i2c-dev). Real v0 architecture, same shape SPIDERBEETLE's `battery_ui.prn`+`BatteryUi.java` already established: PARENA emits pure scalar decision logic (`examples/avr/blink.prn`, one `Bool -> Bool` toggle, zero platform assumptions) + a hand-written native host (`examples/avr/blink_main.c`, real DDRB/PORTB register writes + avr-libc's `_delay_ms`) + a real, minimal AVR-safe runtime stub (`examples/avr/parena_runtime.h`) that C's own quoted-include search rules pick up automatically -- zero compiler/`emit_c.c` changes needed. Real, no-sudo AVR toolchain acquisition: `apt-get download` (not `apt-get install`, no root needed) fetched real gcc-avr/avr-libc/avrdude/binutils-avr .debs plus 3 real missing shared-lib deps (libftdi1/libhidapi-libusb0/libusb-0.1-4, checked directly via `dpkg -l`, not assumed), extracted via `dpkg -x` into a real user-owned `~/.local/opt/avr-toolchain` -- confirmed a batch `apt-get download` silently aborts entirely if one package name is invalid. New Makefile targets `avr-blink-hex`/`avr-blink-upload`. New real "Upload" button in the editor's right sidebar (`compile_and_upload_avr`, same blocking-`system()` shape the existing Compile button already uses), stacked above Compile with its own color. Full pipeline live-verified real end to end: `parena build` -> `avr-gcc` -> `avr-objcopy` produces a correctly-shaped `.hex` (`avr-size`: 192 bytes, 0.6% of an atmega328p), `avrdude` loads its real config and correctly attempts the real serial port -- failing ONLY at port-open (`/dev/ttyACM0: No such file or directory`), the exact same "no physical hardware in this sandbox" constraint `UART_SERIAL_NORTHSTAR.md`'s own Phase 2 already names and accepts. `editor-demo` itself still builds clean (`-Wall -Wextra -pedantic -Werror`, zero warnings) with the new button wired in. Real, honest, named follow-up: Upload always targets `blink.prn` regardless of the currently-open file (not yet generalized), no `#target`-based AVR GPIO stdlib yet (every program needs its own hand-written host), no port/board auto-detection, and the actual hardware round-trip remains unproven (no physical Arduino in this sandbox).
| LLVM-BACKEND-NORTH | PARENA/docs/LLVM_BACKEND_NORTHSTAR.md | 2 | 900 | **Status 2026-09-10: BOTH plans shipped (v0).** Phase 0/1 (clang route): closed both real gaps the scoping pass found -- `_delay_loop_2` instead of `_delay_ms` (GCC-only builtin), `_exit` resolved via avr-gcc's own real `libgcc.a`. New Makefile targets `avr-blink-hex-clang`/`avr-blink-upload-clang`, real and parallel to avr-gcc (not replaced). New persistent `LLVM_TOOLCHAIN_ROOT` (`~/.local/opt/llvm-toolchain`), no-sudo apt-get-download acquired. **Phase 3-v0 (direct AVR route, the literal ask), shipped same day**: new real `src/emit_llvm.c` backend -- `parena build input.prn -o output.ll` emits genuine LLVM IR text (same real 'dispatch by extension' convention `emit_ts`/`emit_java` use), zero C involved in the decision logic. `examples/avr/blink.prn`'s own `next-led-state` compiles through it to real, correct LLVM IR, which `llc -mtriple=avr` lowers straight to real, disassembly-verified AVR machine code (`ldi r25,1` / `eor r24,r25` / `ret`) -- linked via a new host (`examples/avr/blink_main_llvm.c`) into a genuinely correct, complete `blink_llvm.elf`. Real, load-bearing ABI detail found live: LLVM's `i1` needs an `unsigned char` extern on the C side, not `int` (AVR's calling convention uses one register, not two). New Makefile targets `avr-blink-hex-llvm`/`avr-blink-upload-llvm`. 22 new real assertions (`tests/test_emit_llvm.c`, verified via both `make` and real Bazel), covering successful emission and honest error paths (undeclared symbols, type mismatches) -- never silently-wrong IR. Real, honest v0 scope named directly: scalar I32/F64/Bool/String, `if` via `select` not branch/phi, non-short-circuiting `and`/`or`, no independent argument-type re-verification on calls -- NOT the same as `libLLVM` linked in-process (still writes a `.ll` text file for a separate real tool, same shape every emitter here uses), and does not cover the full PARENA language. **Same-day follow-up**: real String support added -- lowers to LLVM's opaque `ptr` type, a literal becomes a real, hex-escaped, NUL-terminated global constant referenced directly by name (no getelementptr decay under opaque pointers), verified live against real `llc 18`; 9 more real assertions (31 total). `make test`: 347/347 throughout; `editor-demo` builds and smoke-tests clean. Phase 2 (Windows) remains unstarted for both routes. Original scoping findings preserved below. Founder real-time, mid-conversation on the Windows toolchain-bundling question: "no i told you use llvm doesnt that solve it?... instead of that can we upgrade our compiler so that it supports LLVM directly?" -- founder's own words, "a monumental ask." Distinguishes two very-differently-sized things "supports LLVM" could mean: (1) use clang as the external C compiler in place of gcc/avr-gcc wherever this repo shells out to one, vs (2) the literal ask -- make `parena` itself emit LLVM IR or link `libLLVM` directly, a genuine new compiler backend. Real, verified findings for (1): LLVM 18/clang-18 are fetchable no-sudo (`apt-get download`, same technique `AVR_ARDUINO_NORTHSTAR.md` established for the AVR toolchain, ~75MB); this build's LLVM genuinely includes a working AVR backend (`llc --version` lists it, not experimental-excluded); actually compiled the real `examples/avr/blink_main.c` to a real AVR object file via `clang -target avr` with zero avr-gcc involvement. Real, found limits: linking still needs `avr-ld` (binutils-avr) -- clang doesn't eliminate that dependency; two concrete avr-libc/clang compatibility gaps found and not yet closed (`<util/delay.h>`'s GCC-only `__builtin_avr_delay_cycles` -- real workaround found, `_delay_loop_2` instead; and an unresolved `_exit` symbol at link time, since clang has no `libgcc` equivalent). (2) is scoped as a real, separate, much larger Phase 3 needing its own design pass (IR strategy, target-triple selection, a real noted interaction with `BURROW`'s own parallel-rewrite effort) before any code -- explicitly not attempted here, honoring this repo's own "scope big asks for real before writing code" discipline. No PARENA source or CI changes in this pass.
| KARAMBIT-NORTH | KARAMBIT/NORTHSTAR.md | 2 | 900 | **Status 2026-09-10: shipped v0 + product-completeness pass + real auto-releases.** Real launcher icon (all density buckets), explicit minSdkVersion/targetSdkVersion, real versionCode/versionName wiring, live scan-progress indicator. New `.github/workflows/ci.yml` mirroring PARENA's own auto-release scheme: builds+tests on every push/PR, cuts a real, minor-bumped GitHub Release with a real, correctly-versioned signed APK on every green build to main. Live-verified the sed-patch+rebuild version logic locally before trusting it in CI. Founder real-time: "im having a hard time figuring out if my raspberry pi is booting and connecting to the network... can we build a toolkit android app to help scan my network... ssh for example should be open... maybe we just check for that for now so we arent too noisy on the network... keep it pluggable and extensible we may very well want to build an in app terminal right there to connect to it (not yet just sayin) build it parena native as much as possible interopping with native java use BAZEL and PARENA." Real, upstream-pre-created repo (Unlicense). New `parena/scan_decisions.prn` -- real PARENA decision logic (`is-target-port`/`has-more-hosts`/`next-scan-index`/`classify-scan-result`) compiled via PARENA's own already-shipped Java emitter to `app/.../generated/ScanDecisions.java`, same real precedent SPIDERBEETLE's own `battery_ui.prn` established. Real, hand-written Java for everything PARENA's own narrow language can't do (no socket primitives, no bitwise ops yet): `ScanStrategy` (the real pluggable interface), `SshPortScanStrategy` (v0: bare TCP connect on port 22 only, deliberately "not too noisy" -- a small fixed 8-way thread pool, not a burst scan), `ScanEngine` (the host-enumeration loop, plain-JVM-testable with zero Android dependency), `NetworkInfo` (real `ConnectivityManager`-based local-subnet detection), `MainActivity` (real UI). Real, no-sudo-acquired Android SDK (cmdline-tools + platform 34 + build-tools 34.0.0/35.0.0) and JDK -- this sandbox had neither, matching (and exceeding) SPIDERBEETLE's own earlier "no Android SDK at all" finding, which was never actually tested with a real acquisition attempt. Real Bazel setup (`rules_android` 0.7.3 + `rules_java`, bzlmod): `//app:scan_core_lib`/`//app:scan_core_test` (13 real assertions incl. a genuine local TCP socket round trip, no Android SDK needed) and `//app:karambit`, which **builds a real, signed, installable APK** -- verified via `apksigner verify` (v1/v2/v3 all pass) and `aapt2 dump badging` (correct package/permissions/launchable-activity). Two real, live-found build bugs fixed: an XML comment containing "--" broke aapt2's manifest parsing (a real XML spec rule); a target/tool Java-version mismatch broke compiling `rules_android`'s own internal `DexFileSplitter.java`, fixed via a new `.bazelrc` pinning both consistently. Also found and worked around a real, sandbox-specific gotcha: this monorepo's own `/home/fatbaby/go.work` (a parent directory) leaks into `rules_android`'s internal Go-tool bootstrap, breaking it with a Go-version mismatch -- worked around by running the build with `HOME` pointed outside `/home/fatbaby`, not expected to recur on a normal machine. Real, honest, not done: installing/running the built APK on a real device (none reachable from this sandbox); an in-app terminal ("not yet, just sayin"); real CIDR/netmask arithmetic (v0 assumes an ordinary /24 -- PARENA's own language has no bitwise operators yet); a configurable scan scope (port/concurrency/timeout are fixed v0 constants, deliberately, per the founder's own "not too noisy" framing).
| MULTI-TENANCY-NORTH | IDUNA_PRO/docs/MULTI_TENANCY_NORTHSTAR.md | 1 | 1000 | Real, spec-only Phase 0 doc for Emily For Business (2026-09-11). Founder real-time: "idunapro needs to become truely multi tenant currently its just a fork of iduna for carepyre." Real, checked-not-assumed gap: zero `tenant_id` anywhere in `internal/store`; `org_id`/`organizations` is a real but WITHIN-tenant concept (CarePyre's own CP-HIPAA-3 agency-cluster hierarchy), not tenant isolation; ten `.go` files hardcode `carepyre`/`CarePyre` by name (`main.go`'s own `mail.carepyre.org` default, a `CarePyreContactHandler` mounted unconditionally, etc.) -- one process, one SQLite file, one JWT key, for everyone. Names why the existing "DB-per-install" plan (`EMILY_FOR_BUSINESS_NORTHSTAR.md`, 2026-09-03) doesn't scale to "an agent vibe-codes a new tenant's API in ten seconds" -- proposes real, single-process, row-level isolation instead (a `tenants` table, `tenant_id` on every tenant-owned table, a JWT `tenant_id` claim, per-tenant config replacing hardcoded constants), with DB-per-install kept as a real option for a small number of dedicated/HIPAA-tier customers (CarePyre itself a plausible permanent resident there). Three real enforcement-mechanism candidates named, not chosen between (a `TenantScopedDB` query-wrapper, SQLite `ATTACH DATABASE`-per-tenant, Postgres row-level security). 5-phase plan: Phase 1 proves `tenant_id` end to end on `local_users` alone with a real adversarial cross-tenant-read-refused test; Phase 2 extends to every remaining table; Phase 3 replaces hardcoded CarePyre config; Phase 4 resolves shared-vs-per-tenant JWT signing keys + load-tests the enforcement mechanism; Phase 5 (stretch) is the self-serve `console.okemily.com` signup pipeline, now "insert one row" instead of "provision a process." No code written.
| UNIVERSAL-UI-NORTH | EMILY_FOR_BUSINESS/docs/UNIVERSAL_UI_NORTHSTAR.md | 1 | 1200 | Real, spec-only doc (2026-09-11) for the frontend half of the "vibe code your own APIs" platform pitch. Founder real-time: "we need a super abstract language for affordances like a BA would write a user can do this... not at all about the interface... like react native but even more abstract -- like we target ratatui and android native and react native and flutter and everything you just need to write an adaptor." Real, honest framing: React Native/Flutter already separate app logic from platform rendering, but both commit to one GUI-widget-tree PARADIGM -- a terminal (ratatui) is a categorically different paradigm (cell-addressed text, no images/touch/haptics without real, named lossy tricks), and honestly spanning both is the actual novel, hard problem here, not a refinement of what React Native already does. Real prior art named and each one's own real ceiling identified: JSON-Schema-driven forms/low-code platforms (single rendering paradigm, same ceiling), Gherkin/Cucumber (describes behavior/tests over an already-built UI, not a UI source), Android `Intent`s (action-and-data over exact-widget, but inter-app-handoff-scoped only). Grounds the idea in this monorepo's own already-PROVEN "one source, many real target emitters" pattern (PARENA's C/Java/TS backends; `KARAMBIT`/`SPIDERBEETLE`'s own "PARENA owns decision logic, a thin native host does I/O" split is named as the direct precedent for what an "Adapter" concretely is). Proposes a 3-layer model: an Affordance IR (Entities/Actions/Views/Flows, described the way a BA would, no widget/pixel info at all), a Capability Model (each target DECLARES what it can render, with a named, non-negotiable rule that an unsupported affordance must degrade EXPLICITLY, never silently drop -- the same real "#error, never a segfault" discipline PARENA's own self-hosting effort already holds itself to, applied to UI capability instead of compiler shapes), and per-target Adapters (PARENA/BURROW-generated real, idiomatic target source, not a lowest-common-denominator translation). 5 real, named, unresolved design questions (where the IR itself gets authored; whether it doubles as an executable acceptance-test suite; live/real-time data; cross-runtime state management; accessibility). 4-phase plan, deliberately proving the HARDEST pair first (ratatui vs. a real touch GUI, not the two closest targets) on one trivial CRUD affordance before generalizing. No name locked in, no repo created, no code written.
| DEADWEIGHT-NORTH | DEADWEIGHT/NORTHSTAR.md | 1 | 1500 | New repo (2026-09-11), scoping-only. Source: a captured Gemini transcript (misleadingly named "LeetCode Skills Course Curriculum.pdf") that spirals from an actual LeetCode teaching curriculum into a full PvP game spec, "Dark Sector: Hold Battles" -- 6x6-grid spatial-knapsack combat (splittable items, fragmentation tax, energy routing -> weapons/armor). Real critical review, not rubber-stamped: names the transcript's own escalation into a multi-year live-service catalog (Black-Scholes options-pricing engine, Merkle-tree proofs, 24 items, 16 Ultimates x 3 Flow tiers, a double-elim tournament bracket, 2v2) and proposes a real, buildable V0 cut (core grid+combat, 6-9 items, 2-3 Ultimates, 1v1 ladder, no derivatives/Merkle/tournament/2v2 yet). Real capability audit against the founder's own "build it with PARENA, target Android+Windows, 1 client like Hearthstone" ask: PARENA's C emitter (`emit.c`, 6218 lines) is mature but this monorepo's own universal precedent is a hand-written C host + PARENA mod-logic islands, never a whole client; PARENA's Java emitter (`emit_java.c`, 391 lines) is real but v0/scalar-only (no structs/loops/collections), its only precedent (SPIDERBEETLE) is two standalone helper functions -- so "1 PARENA client, 2 platforms" isn't real yet; recommends two native shells (C/SDL2 Windows, Kotlin/Java Android per MJOLNIR's own template) sharing a server and, per the founder's own "meta ui framework" reference, `EOSUI-NORTH`'s real (not-yet-built) Option C styling layer. Names guest-account auth (name-only, no email, unrecoverable without linking one) as genuinely new IDUNA work -- checked directly, IDUNA's `players` table supports only `google`/`iduna_local` providers today, no anonymous provider exists anywhere. Reuses ECOWAR's real matchmaker/bot-pool pattern and REDGARDEN's real IDUNA player/match-tracking model directly. 6-phase plan (repo hygiene -> local core loop -> server+guest-accounts -> Windows client -> Android client -> EOSUI retrofit). No code written. |
| COMMUNITY-TOOLS-RESUME-NORTH | CarePyre/docs/COMMUNITY_TOOLS_RESUME_NORTHSTAR.md | 2 | 900 | **Status 2026-09-09: shipped (backend v0).** Founder real-time arc: resume-data tool -> separate app + new IDUNA_PRO instance -> cvb.okemily.com naming -> real, final correction: "build it into carepyre... community tools... gated so that accounts need a feature flag set." Reuses CarePyre's own already-live IDUNA_PRO instance (:8081), no new app/deployment -- a `CVBUILDER` scaffold was built then deleted once redirected; only the JSON Resume data model itself carried over. New `LocalUser.IsCommunityToolsEnabled` (IDUNA_PRO), a plain per-account feature flag separate from the 4-tier admin/provider RBAC (event-sourced, mirrored across sqlite+mysql projectors), granting a real `community-tools.access` permission checked independently of tier -- even admins need the flag set on their own account. New `internal/resume`: a real, field-for-field Go mirror of the JSON Resume schema (jsonresume.org, fetched and checked live, not assumed), the actual known standard for machine-readable CVs, plus a real itemized 2-layer `Verify` function (schema conformance + named ATS-readiness rules: date format, required fields, missing-employer-name), never an opaque score. New `community_tools.go`: `GET`/`PUT /api/v1/community-tools/resume` + `POST .../verify`, scoped to the caller's own `local_uid`, no cross-user path. New migration (`is_community_tools_enabled` column + `resumes` table). Live-verified: fresh-SQLite boot, migration applies cleanly, `/health` OK. 12 new Go tests incl. forbidden-without-flag and cross-user isolation proofs. Full suite green, zero regressions. **Frontend shipped same day (Apple #18729)**: a real, hidden-by-default "Resume" panel in `console.html` -- editable Basics fields, dynamic add/remove Work/Education entry lists, Save+Verify with the real itemized per-rule report. Real, live-found HTML-attribute-injection risk fixed before shipping (a real field value with a double-quote could've broken out of a `value="..."` attribute) -- switched to safe `document.createElement`+`.value` property assignment instead of string-concatenated `innerHTML`. JS syntax + HTML tag balance verified directly; no live browser click-through run. **Bespoke Target resumes shipped same day (Apples #18731/#18733)**: real, named, tailored resume variants -- founder arc "we need some way to start building more bespoke resumes for specific opportunities" -> "the pattern probably applies to education awards experience skills even different little summary texts" -> "this is probably most useful in the skills section" -> "keep all of the exported tweaked versions so we can clone from them." New `Work.ID`/`Education.ID`/`Skill.ID`/`Award.ID` (a deliberate CarePyre extension beyond strict JSON Resume), server-assigned. New `internal/resume/target.go`: `Target{IncludedWorkIDs/IncludedEducationIDs/IncludedSkillIDs/IncludedAwardIDs, SummaryOverride, LabelOverride}` + `Resolve(master, target)`, a real generic `filterByID[T]`. New `/targets`, `/targets/{id}/resolved`, `/targets/{id}/verify` endpoints -- verify runs against the real resolved view, not the master. New Skills editor + "Bespoke resumes" UI (checkbox per master entry, override fields, Clone/Verify/Preview/Remove -- clone needs zero new API, just an unsaved pre-filled copy) and "Preview & templates" card: one real semantic render wrapped in two switchable CSS templates (Classic/Clean Tech, "change... with a click"), verified for real against sample data with HTML-special characters. Real bug found+fixed before shipping: Remove only cleared the DOM, not the source-of-truth array a later New/Clone re-renders from -- fixed. 17 new Go tests, full suite green. **Layer 3 PDF export shipped same day (Apple #18738)**: internal/resume/pdf.go's `RenderPDF` (github.com/go-pdf/fpdf, pure Go, no cgo) -- a real, plain, single-column, selectable-text PDF (never an image). New `GET .../resume/export.pdf` (master) and `GET .../resume/targets/{id}/export.pdf` (resolved Target) routes, shared `writeResumePDF` helper. Real header-injection risk found and fixed before shipping: a Target's own user-controlled Name going straight into the Content-Disposition header -- closed with `pdfFilename`, an allowlist sanitizer, live-verified against a target literally named `Kitchen Jobs "Special"`. New "Download PDF" (Preview & templates card) and per-Target "Export PDF" buttons in console.html, fetched as a blob via authHeaders() (a plain `<a href>` can't carry the bearer token) then downloaded via a throwaway object URL, using the server's own real Content-Disposition filename. 16 new Go tests, full suite green. Live-verified end to end against a real, fresh-SQLite-booted idunapro binary (register -> flip is_community_tools_enabled -> login -> save resume -> GET export.pdf returns a real, file(1)-confirmed PDF), not just go test. **Agent-ergonomic API primitives + OpenAPI spec shipped same day (Apple #18752)**: founder real-time "ensure that all of the features we have have good api because i am going to ask agents to work with those primitives." New `PATCH /resume/basics`, `POST`/`PATCH`/`DELETE /resume/{work,education,skills,awards}[/{id}]`, and the analogous single-target primitives -- one real, shared generic `CommunityToolsEntryHandler[T]` + `entryOps[T]` implementation, relying on `encoding/json`'s own real merge-onto-an-already-populated-value semantics (an omitted key leaves a field untouched, a present key including explicit `""`/`null` overwrites it -- a genuine three-way absent/null/value distinction for Target's own overrides). New `GET /api/v1/community-tools/openapi.json`, a real, complete, `go:embed`-ded OpenAPI 3.0 document, deliberately public like JWKS. Real bug caught and fixed before shipping: `main.go`'s first draft built the four entry handlers with `Ops` left a zero-value struct of nil funcs (compiles, panics on first real use) -- fixed with exported per-type constructors. 18 new tests, full suite green. **Multiple GitHub/LinkedIn links shipped same day (Apple #18753)**: founder real-time "we need to be able to add and configure the output of multiple github links." Real, checked-first finding: `basics.profiles` existed in the data model since day one but rendered NOWHERE (no UI, no PDF, no screen preview) and had no stable id. Fixed: new `Profile.ID` (multiple entries sharing the identical Network value, e.g. several distinct GitHub repo links, is real and expected), new `Target.IncludedProfileIDs` + `Resolve` filtering, real "Links" rendering in both `RenderPDF` and console.html's `renderResumeTemplate` (verified via real Node execution against an XSS-shaped payload), a new "Links" editor section + "Show links" Target checklist in console.html, and `NewCommunityToolsProfilesHandler` reusing the same generic CRUD machinery. Deployed live for both increments -- confirmed the real, actively-used production resume for `joe@carepyre.org` (independently edited by its own owner between an earlier seed and this deploy) still parses and verifies cleanly, no data loss. Real, honest, not done: no DOCX export, no visual parity between the PDF and the two web preview templates, per-entry text overrides beyond summary/headline, no `username` field in the Links UI (API supports it). Apple #18725 (backend v0), Apple #18729 (frontend v0), Apple #18733 (targets + templates), Apple #18738 (PDF export), Apple #18752 (API primitives + OpenAPI), Apple #18753 (multi-GitHub-links). **Same day, later**: a real Compact 2-column PDF/preview template (an honest, named ATS trade-off vs. the classic single-column layout) plus a real candidate name + export timestamp in every downloaded PDF footer/filename (Apples #18764/#18765); and a real auto-sort fix for Work/Education/Awards (most-recent-first, an ongoing entry always ranks first) closing kanban priority-queue item CVB-12434 -- verified against real production data that had an entry matching the exact reported bug (Apple #18769, completed via the real IDUNA kanban PATCH .../done flow, not a manual edit).
| PARENGINE-NORTH | EMILY/docs/PARENGINE_NORTHSTAR.md | 1 | 1800 | Cross-repo scoping (2026-09-11): "for the engine can we take a look at all of the games that we have... start to abstract the libraries that make up our engine... instead of just always calling SHANKPIT a game engine which it isnt really yet." Frame-break: SHANKPIT/GoblinFoxDragon are a genuinely different Go-server lineage from REDGARDEN/ECOWAR/BRAWLPIT/PAPERCRAFT/WEAKNIGHT_BEDROCK_RACERS (a separate C/SDL2 packages/simulation-shaped lineage) -- neither is actually a reusable engine today, each is one specific game other games copied by hand. Real, checked-directly evidence across all five C-lineage repos: `mat4.h` byte-identical (same md5) across REDGARDEN/ECOWAR/WEAKNIGHT (the latter not even a fork -- pure hand-copy); `hmac_sha256.h`/`http_client.h` copied once then silently diverged (different md5 in PAPERCRAFT/WEAKNIGHT); a live, self-caused example from this exact session (S370's ECOWAR-only seed-plumbing fix already left REDGARDEN's identical `apps/matchmaker` copy behind, different file sizes now); every SDL2 client independently reinvented its own mesh/shader/draw-primitive naming (REDGARDEN/ECOWAR share one convention via fork, BRAWLPIT/PAPERCRAFT/WEAKNIGHT each built a separate, incompatible one); and the starkest single finding, `draw_login_screen` is a near-line-for-line hand-copy between PAPERCRAFT and WEAKNIGHT (identical signature/layout/control-flow, only colors/title/`pc_`-vs-`rc_` prefix differ). Credits what's already real progress: `packages/goldenband` (REDGARDEN/ECOWAR, but checked -- literal duplicated file, not a submodule, will drift the same way); PARENA's own real `stdlib/sdl2.prn` (482 lines, already grepped from 5 real games' usage, genuine foundation but deliberately thin -- raw SDL2 primitives only); the existing `stdlib/<game>/` per-game namespace convention, which has no engine-generic equivalent today. Names a real, honest architectural distinction other PARENA-mod integrations don't raise: every existing mod is "PARENA compiles logic, a hand-written C host calls into it" -- pulling mesh/shader/window ownership INTO PARENA is PARENA-owns-the-host, a bigger, different kind of ask needing its own design pass, not assumed away (same discipline DEADWEIGHT's own PARENA-Java-emitter audit already applied this same week). Proposes new `PARENA/stdlib/engine/` namespace, grounded in real function signatures found (not invented): `mat4.prn` (port, zero design risk, already byte-identical in 3 repos), `hmac.prn`/`http_client.prn` (small, self-contained, one audited implementation instead of N diverging ones), `net_snapshot.prn` (the MTU-split packet-framing convention ECOWAR's own S170-193/S370-04 already proved twice), `mesh.prn`/`shader.prn` (highest value, gated on the architecture question above), `login_ui.prn` (converges independently with EOSUI-NORTH's own Option C -- corroborating signal, not coincidence). 5-phase plan starting with the zero-controversy math/crypto/http extractions, DEADWEIGHT as the first real new consumer for net_snapshot (being built from scratch this same week), explicitly never a mandated migration for already-shipped games. No code written yet. |
| PARENACLOUD-NORTH | EMILY/docs/PARENACLOUD_NORTHSTAR.md | 1 | 1800 | Cross-repo scoping (2026-09-11), direct continuation of PARENGINE-NORTH: "what if we wanted to build DEADWEIGHT as a different organization on top of emily for business or whatever the gaming equivalent is... a dev focused product with a command line tool to scaffold projects... define the multiplayer paradigm and matchmaking queues (casual vs ranked vs bots)... the library API needs to be overloadable, batteries included but extensible, mods first." Working product name: PARENACLOUD. Real, checked-not-invented building blocks connected rather than reinvented: IDUNA_PRO's own real, live, shipped tenant-provisioning control plane (`internal/tenantprovision`, SECTION 267 -- port+secret+systemd-unit+health-poll, one shared binary per tenant); the already-decided BURROW-Go-vs-PARENA-C mod-target split from `EMILY_FOR_BUSINESS_NORTHSTAR.md`'s own 2026-09-03 extensibility scoping (Go host -> BURROW mods, C host -> PARENA mods); and a second independent instance of PARENGINE-NORTH's own "copy-pasted by hand" finding, found live on the backend side -- IDUNA's `redgarden_ticket.go`/`racer_ticket.go`/`papercraft_ticket.go`/`shankpit_ticket.go`/`shankpit_queue.go` etc. are one hand-coded Go file family per game solving the identical connect-ticket/queue/leaderboard problem, zero shared implementation. Names a real, undecided architecture fork: game-backend tenancy (a long-lived matchmaker spawning ephemeral per-match child servers) is a materially different operational shape than IDUNA_PRO's current one-long-lived-web-process-per-tenant model -- extend `tenantprovision` generically vs. a separate game-specific control plane, not resolved here. Sketches a real `GameConfig` schema grounded directly in `apps/matchmaker`'s own real, already-existing flags (`--server-bin`/`--listen-port`/`--first-game-port`/`--lobby-size`, already close to config-driven) plus new, named work for ranked/bot-fill-policy queue fields that don't exist anywhere yet. Recommends extending the existing `parena` CLI with a `cloud` subcommand group (`parena cloud init/deploy/queues/tenants`) rather than a new binary, matching gcloud's own resource-scoped-subcommand UX convention -- deliberately not designing `init`'s scaffolding template until DEADWEIGHT itself has a real, proven repo shape to generalize from. Formalizes "batteries included, mods-first, overridable" as a concrete technical rule (every stdlib/engine/*.prn default is a swappable mod slot a tenant registers a replacement for, never a vendored file they fork) rather than an implicit convention -- and names the real, honest constraint that BURROW's own current emission capability is narrow (LO's own capability audit: scalar+flat-struct only), the same "v0, don't oversell it" caution applied to the platform-mod half of this story that DEADWEIGHT's own PARENA-Java-emitter audit already applied to the engine half. Explicitly gated on PARENGINE-NORTH's own P1-P2 and DEADWEIGHT's own D1-D2 landing first -- no code written, no phase started. |
| ECOWAR-LIVINGMAP-NORTH | ECOWAR/docs/NORTHSTAR_LIVING_MAP.md | 1 | 1500 | Founder real-time: "add towns - start with the entire map is divided into cells (hex grid)... start with the frontier village" + 4 town types (Frontier Village/Walled Hamlet/Jungle Enclave/Blighted Settlement) + 3 warring factions (Dominion/Symbiosis/Corruption, rock-paper-scissors, doctrine tech trees "pick 2 max," end-tech capstones Citadel Node/Living Bastion/Cataclysm Beacon) + "PARENA MODS FIRST — everything that happens in the game needs to announce events and then mods can subscribe." Real, checked-first finding: this is a NEW strategic layer, not an extension of arena_game.c's existing 9-node MOBA map -- built as its own standalone `packages/livingmap` package so the live, tested arena sim stays untouched. **Phase 1 shipped**: real axial-coordinate hex grid (hex_grid.h/.c, cube-coord distance/rounding, world<->hex conversion, 469-cell hex-radius-12 map) + Town struct/registry (town.h/.c, all 4 types named, only Frontier Village has real behavior) + a new real-decision-logic PARENA mod (`stdlib/ecowar/frontier_village_mod.prn`, card_effect_mod.prn's own tier: spawn cadence, peasant->militia promotion, conversion resistance, all real I32 branching) matching "spawns peasants -> militia, avoids conflict, converts easily" exactly. Real, honest "Mod event model" section: checked directly against VS0's own current emitter -- no function pointers/closures exist anywhere in this monorepo's PARENA compiler, so a true dynamic multi-subscriber registration table isn't buildable today; what's real and shipped is an always-on event-announcement ring buffer (`living_map_events.h/.c`) every lifecycle transition writes into, plus the existing call-by-name mod convention every other REDGARDEN/ECOWAR mod already uses -- named as a real, tracked VS0 follow-up, not faked. Corruption mechanic and win condition both explicitly named as genuinely undecided per the founder's own "not sure what corrupted means" / "im not sure what the win condition is" -- three staged corruption-mechanic options and a rock-paper-scissors hypothesis for the 3 end-techs offered as starting points, not decisions. 27 new tests, all passing (`scripts/test_livingmap.sh`, wired into CI). **Phase 2 shipped same day**: real classic-RTS creep aggro/chase/leash/reset (`creep.h/.c`, mirrors `arena_game.h`'s own proven `ArenaCampMinion` shape); `TOWN_TYPE_WALLED_HAMLET` real behavior (new `walled_hamlet_mod.prn` -- much higher convert resistance "slow to flip," stationary "shoots hostile creeps" defense fire vs. the new `CreepRegistry`); the real "cards tie into the Living Map" mechanic (`Town.militia_bonus`/`town_apply_militia_boost`, matches founder's "play it twice -> 3 militia at a time" exactly, though nothing in a live match calls it yet -- no running match initializes a Living Map instance); win condition resolved (`town_registry_faction_has_full_control` -- cap every town at once, a deliberate contrast with REDGARDEN's pace/comeback dynamic); `apps/arena_server` gained `--tick-ms`/`--fast-forward` for faster networked self-play match generation (ML training throughput, defaults unchanged, live-verified). Card-UI redesign (G-key Hearthstone/Clash-Royale-style draggable panel) and a real, open hero-as-NPC/full-card-battler architecture question both captured as real founder direction, not built. 9-phase plan, 2 of 9 done (hex grid+Frontier Village; Walled Hamlet+creeps+cards+wincon+tick-speed); remaining: corruption+Blighted Settlement, Jungle Enclave, 3 factions as real AI, tech tree, the arena<->living-map live-wiring bridge, card UI, visual factions.  **S378 same day**: the founder's own hero-as-NPC/card-battler question (named in Phase 2) got a real, explicit go-ahead as a reversible experiment ("if it ends up being unfun we bring back classic RTS/MOBA hero affordances") -- shipped `ArenaHero.npc_controlled` + `arena_npc_hero_tick` (a generalized version of the existing practice-bot heuristic AI, already hero-index-agnostic in its real math) + new `card_deck.h/.c`/`card_battler.h/.c` (real Clash-Royale-style Deck/Hand mechanics, hero Q/W/R as real playable cards resolving to each hero's own existing cast functions). 24 new tests, full suite green (3145 assertions). Not wired into any live client yet -- rollback is cheap by design (flag defaults off, direct-control code untouched).  **Phase 7 same day (real live wiring)**: fixed the real gap founder testing exposed ("i dont see a hex grid... no all cap win con") -- new `living_map_bridge.h/.c` founds/ticks a real live HexGrid/TownRegistry/CreepRegistry per match and checks the real win condition every arena_update tick; new `PACKET_ARENA_SNAPSHOT_LIVING_MAP` wire packet; apps/arena's minimap now shows real town/creep markers. Found and fixed a real `creep_spawn` naming collision against arena_game.c's own pre-existing function. Live-verified via a real fast-forwarded server + 2 real bots completing a full match with zero crashes. 5 new tests, full suite green (3169 assertions). Still not reachable in a real match: no card yet calls town_attempt_convert, so no one can actually win this way yet; no hex-grid outline rendered; G-key card-UI panel still not built. |
| ECOWAR-SPHERECAM-NORTH | ECOWAR/docs/NORTHSTAR_SPHERE_CAMERA.md | 1 | 1200 | Founder real-time, with a reference screenshot (Spore's planet/Civ-stage globe view): "can we have it so that the map is actually a sphere and then the camera is fixed and you can move the actual map around... right click holding... rotates like you could rotate a sphere in a graphics program... scroll to the edge of the screen like in an RTS the actual camera would pan... flip the paradigm." Explicitly scoped design-doc-first, no code, per the founder's own choice when asked how to sequence it (given this rewrites the coordinate system every Living Map/arena system this session was built on). Real, checked-first finding: the camera is ALREADY a real 3D orbit camera (`mat4_orbit_view`, yaw/pitch/dist) and right-click-drag ALREADY rotates it (`cam_yaw`/`cam_pitch` in apps/arena/src/main.c) -- just orbiting the camera around a focus point hardcoded to the local hero's own position every frame (a MOBA-style hero-locked camera, no free-roam mode exists). Names the single real, load-bearing cost lever this doc doesn't resolve: does "sphere" need to be FUNCTIONALLY real (units walk a curved surface, geodesic distance/pathing, a from-scratch geodesic hex-and-12-pentagons grid replacing the flat `packages/livingmap/hex_grid.h` this session just built) or just VISUALLY real (the exact same flat x/z world and all its math, rendered with a vertex-shader terrain curvature bend) -- recommends starting from the cheap visual version, since nothing in the reference screenshot proves which one Spore itself actually used. Names real, separate technical challenges for the expensive path (no perfect hex tiling exists on a sphere -- Euler's formula forces exactly 12 pentagons on any hex-dominant closed polyhedron; sphere-raycasting for click-to-world; curved-surface hero "up"/facing). Flags a real, unresolved tension in the ask itself (edge-pan implies camera movement, "the camera is fixed" implies none) as a direct founder question for later, not guessed at. 6-phase plan (free hero-decoupled camera -> resolve the edge-pan tension -> visual sphere bend -> arcball rotation -> mobile touch input -> "functionally real" sphere only if the cheap version is tried and found wanting). No code written. |
| ECOWAR-REFLUX-NORTH | ECOWAR/docs/NORTHSTAR_REFLUX.md | 1 | 1200 | Founder real-time: "add the BLOOD FLOWER and day night rendering - we can take the graphics of the day night rendering from OG SHANKPIT i love the way the lighting shifts from day to night in shankpit - and the bloodflower for now just have it spawn a bunch of hostile creeps into the map at exactly the midpoint (already a mod) have the spwning in hostiles be a mod too with cross mod pub sub communications (THINK REDUX HOWEVER REDUX WORKS) call the mod itself REFLUX ... give it its own stdlib in PARENA." Real, checked-first constraint: VS0 has no function pointers/closures, so a true Redux-style callback dispatch table isn't buildable -- REFLUX (`PARENA/stdlib/reflux/reflux.prn`, new real stdlib namespace) is instead a single, shared, append-only action log any mod can dispatch into and any other mod can independently poll from, generalizing ECOWAR's own `packages/livingmap/living_map_events.h` shape into a standalone, game-agnostic primitive. Real host runtime `packages/simulation/reflux_runtime.h/.c` (arena-scoped for now). Found and fixed a real naming collision (PARENA-mangled `reflux_dispatch`/`reflux_log_size` vs. the runtime's own originally-planned generic names) by renaming the runtime's internal functions. First real dispatcher/subscriber pair shipped same day: `bloodflower_mod.prn`'s `on-moon-zenith` (original pickup-spawn behavior unchanged) now also dispatches a real action; new, separate `bloodflower_hostile_spawner_mod.prn` polls for it and spawns 5 real hostile Living Map creeps (faction 3/Corruption's first live use) at map center -- zero direct calls between the two mods. Also shipped same day: real day/night dynamic scene lighting -- found this codebase had only ported HALF of SHANKPIT's own `retro_lighting.c` (the ambient clear-color tint) before, with the actual 3D scene light direction still a hardcoded constant; new `arena_daynight_light_dir` ports SHANKPIT's real sun-by-day/moon-by-night direction formula, wired into `apps/arena`'s render loop with zero shader changes needed. Live-verified: a real fast-forwarded server + 2 real bots completed a full match with zero crashes; a longer headless test drove a full real day/night cycle confirming both bloodflower behaviors fire. 20 new tests, full suite green (3189 assertions). Not done: SHANKPIT starfield/sky-dome rendering, no visual confirmation possible (no display in this sandbox). |
| PARENACLOUD-OPS-NORTH | EMILY/docs/PARENACLOUD_OPS_NORTHSTAR.md | 1 | 1500 | Real, standardized `ops/playbook.toml` format for backend-service deployment, direct continuation of PARENACLOUD-NORTH's own PC2/CLI sketch, founder real-time: "create a standard format for the backend services in terms of like systemd... a fork of REDGARDEN on their own server... pulls the repo reads the ops playbook... sets up those backend services via systemd... mention but defer ansible and terraform." Real investigation grounded the format directly in every hand-written unit file already live across the C/SDL2 arena lineage (REDGARDEN/ECOWAR/BRAWLPIT/PAPERCRAFT/WEAKNIGHT_BEDROCK_RACERS) -- found ECOWAR's own ops/ dir still literally contains several un-renamed REDGARDEN-named unit files (real copy-paste residue from the fork), and that BRAWLPIT/PAPERCRAFT both have real, already-documented port-collision incidents this format's own structured `port` field makes checkable before deploy. Real schema: `[game]` + repeatable `[[service]]` (kind = "persistent" | "spawned" -- spawned entries like arena_server, spawned per-match by a matchmaker, are documented but never get a systemd unit) + `[values]` for `{{placeholder}}` substitution (enabling REDGARDEN's own real 3v3/stable/players-only unit variants to become one playbook + small per-deploy value overrides instead of N independently-drifting hand-copied files). Real, working implementation, not just spec: `emily.cli`'s new `internal/opsplaybook` (BurntSushi/toml-based parser + renderer) + `emily ops render-unit`, live-verified with 5 real tests including a field-by-field comparison against the real, currently-deployed `ecowar-matchmaker.service`, plus a manual end-to-end run against both ECOWAR's and REDGARDEN's own new, real, checked-in `ops/playbook.toml` files. Found and named (not silently fixed) a real, live risk: REDGARDEN's own currently-deployed matchmaker unit still has the REQUIRED (non-optional) `EnvironmentFile=` ECOWAR's own real 5-day-outage incident already proved dangerous. Explicitly mentions and defers Ansible (`ops/site.yml`) and Terraform (`ops/infra.tf`) as real, separate, later extension points for box/DB/DNS provisioning, per the founder's own direct instruction -- not started. Deliberately does not build the "pull the repo and actually apply it" half -- real live-infra-mutation work gated on PARENACLOUD-NORTH's own still-open CLI-surface decision. |
| NOCK-CHAR-NORTH | IDUNA/docs/NOCK_CHARACTER_PIPELINE_NORTHSTAR.md | 1 | 1200 | Real, phased plan for NOCK's mesh/rig/animation/scriptable-object work (sibling to NOCK-NORTH, which stays texture/2D-only) -- founder arc from "import quaternion animations into nock golden band" through a real 422 fixed (mesh/skeleton-only glTF import), "nock tools is gonna end up having rigging and animating interfaces" / "and basic modeling", to "we want animations in game... build the affordances", "yolo an NPC into one of the games", and "not sure how to add scripts to actual objects and then place those objects into the world... i guess we need browsers and editors for animations, meshes, and rigs, and also some way to rig meshes... the real goal is to bring it all in house." Phase 0 DONE and shipped this pass: NOCK animation repository CRUD + glTF import now accepts mesh/skeleton-only assets (no animation required, S459-95); GSKEL_MAX_JOINTS 64->128 fix re-synced into a stale SHANKPIT vendored copy found along the way; GOLDENBAND's new src/gpose.h/.c (S459-96) -- general, engine-agnostic column-major float[16] forward kinematics + 4-bone-weight mesh skinning for an arbitrary joint count, 4 real hand-derived unit tests passing -- closes the real gap that SHANKPIT's gband_mesh_rig.c was hardcoded to Tyler's own 5-joint rig; proved live by confirming (joint-by-joint .gskel diff) the founder's own imported Mannequin_F.glb mesh+rig and UAL2_Standard_RM mocap clip share the exact same 65-joint skeleton, then spawning it as a real NPC in SHANKPIT (S459-97, packages/goldenband/gband_skel_npc.c/.h, a deliberate sibling to gband_mesh_rig.c which stays untouched) -- `make lobby` builds clean; full visual confirmation blocked by this sandbox's own pre-existing headless-GL limitation, not a regression. Real, phased next steps named, not built yet: Phase 1 (browse/compose UI -- a real Character entity pairing a rigged mesh with compatible clips, a skeleton-compatibility check, an in-browser WebGL preview -- none of which exists yet); Phase 2 (generalize the Story System's own real door-script-attachment pattern past doors to any placed object, plus a real map-editor placement UI instead of hand-written level JSON); Phase 3 (rigging tool, then animation tool, then -- last, most speculative -- a from-scratch modeling tool, sequenced last since Phases 0-2 don't need it). Explicitly out of scope: rigid-body physics interaction (scoped out of the Story System doc too), PAPERCRAFT player-character integration (SHANKPIT chosen first since it has a working render harness and PAPERCRAFT doesn't yet). |
| NOCK-NORTH | IDUNA/docs/NOCK_NORTHSTAR.md | 1 | 1200 | Founder real-time (2026-09-12): "we are gonna need to build our own tools to create the textures... same affordances same interface layers layer masks opacity png and jpg gradient tool hue saturation sharpening resolution exporting etc lets build it on top of imagemagic for now and add cli affordances for everything in our app FIRST... lets yolo it into iduna... this project is called NOCK," then mid-build: "just get it working in whatever technology will get it working fastest" + "REACT... TYPESCRIPT... whatever the newest react idioms are." Real v0 shipped same day, not just scoped: `IDUNA/internal/nock` (ImageMagick-6-backed layered image engine -- create/list/delete project, add/remove/reorder/opacity/visibility/mask a layer, gradient layers (vertical/horizontal), destructive hue/saturation and unsharp-mask sharpen, whole-canvas resize, PNG/JPEG export honoring opacity+mask), 10 real tests against the real installed `convert` binary (one documents a real found-live ImageMagick footgun: `-crop` retains page offset unless `+repage`, silently breaking a naive crop-then-flatten pixel sample). `IDUNA/cmd/nock` CLI (every operation, live end-to-end verified: photo import -> gradient layer -> opacity -> hue/sat -> sharpen -> directional gradient mask -> PNG+JPEG export, composite visually correct). `internal/http/handlers/nock.go` JSON API (3 more tests) + a real React 19/TypeScript (Vite, function components + hooks, no Redux) SPA at `/admin/nock`, gated by the same `iduna.admin` permission every other admin page uses, `go:embed`-ed into the IDUNA binary via a co-located `frontend/nock/embed.go` (go:embed can't reach outside its own directory tree) -- built `dist/` deliberately committed, not gitignored, since nothing else regenerates it. Deliberately NOT GFD-coupled (zero GoblinFoxDragon imports/concepts) per the founder's own explicit "build it for shankpit first... not too coupled to GFD... extension points" instruction. Real, honest, not done: live-booting a real IDUNA instance and browser-verifying `/admin/nock` (the currently-running production IDUNA on this box was deliberately not stopped/restarted to test against); layer position/scale/rotate (every layer is full-canvas only); non-destructive adjustments; PARENA as the backend; a 3D modeler; a level editor; PARENA Editor (PE) macros; a real per-game project-switcher data model -- all named as explicit, phased future work, not silently dropped. |
| SSH-TRANSPORT-IDENTITY-NORTH | GoblinFoxDragon/docs2/SSH_TRANSPORT_IDENTITY_NORTHSTAR.md | 1 | 1500 | Founder-authored, verbatim-authoritative implementation spec (`SSH_TRANSPORT_IDENTITY_SPEC.md`) + Amendment 1 (`SSH_TRANSPORT_IDENTITY_AMENDMENT_1.md`, guest tier + channel gating) for migrating GFD's MUD from anonymous telnet to SSH-based public-key identity -- two real problems named: plaintext session hijack of an already-authenticated stream reaching a bank/AH/bazaar, and nameless identity (any account is takeable by anyone who reads/guesses the character name). 8-stage independently-shippable rollout; stage 1 (telnet economy+chat gate) named explicitly as "the actual security fix... ship it first." Real, checked finding that changed the plan: Amendment 1 §C.1 assumed telnet characters were already ephemeral -- checked directly, they're real, IDUNA-persisted, named characters (`mudCharCache`); raised to the founder rather than silently either stripping real persistence or ignoring the spec. Founder's real, live-tested resolution: the persisted DB rows staying real is fine (useful for future NPC-driven market dynamics), but reconnect-by-name doesn't reliably resume a character today as actually played -- treated as this tier's real, permanent, intended model going forward, not a bug to fix. **Stage 1 shipped same day**: `apps2/mud/main.go`'s `guestGate` (called from `handle()`'s own dispatch, before its switch) blocks bank/bazaar/ah (economy) and say/tell/yell/guild/`/p` party-chat (chat) for every guest (= every real telnet connection today, via new `player.isGuest`, false for headless/Town-GUI sessions which already carry real WOTAN identity); every refusal explains the real SSH upgrade path rather than a bare denial; a connect banner states guest-tier scope + real non-persistence behavior before the name prompt; structured log line on every blocked attempt. 11 new tests; live-verified via a real, unmocked Python socket script against a throwaway isolated instance (the real, live production `:2323` server was never touched or restarted). Real, honest, not done: guest display marker/reserved-names (Amendment 1 §C.2/C.3 -- no SSH identities exist yet to distinguish against or reserve names from), idle eviction/per-IP session caps (§C.4), full instrumentation beyond one log line (§E), and stages 2-8 (GUI-login TLS, process isolation, the actual SSH listener, identity binding, port 22 swap, device-flow hardening, telnet deprecation) -- all real, named, sequenced future work, not started. |
| JOB-SPELL-SYSTEM-NORTH | GoblinFoxDragon/docs2/JOB_SPELL_SYSTEM_NORTHSTAR.md | 1 | 1200 | Founder real-time (2026-09-12): "the classes dont have any purpose there are no spells or job abilities... we make everything a spell... disable all of the advanced jobs for now including ASN... design the first tier of spells... start tracking skill levels for each of the weapon types... /ja /ma both map to the same spell casting affordance," then a second addition same session: "combat loop is too deterministic i always hit for 30 no matter what class... STR increase auto attack damage... crit (dex)... miss... VIT reduces incoming damage... AGI increases evasion." Real, checked-first audit corrected the stated premise: BLM already has a full elemental tier-1/2/3 roster, WHM/RDM already have cure/haste/dia/refresh, WAR/MNK/THF already have Provoke/Boost+Chakra/Sneak Attack — the real gap is zero cast-time/cooldown/lockout system anywhere (every spell is instant, MP-gated only) and, per a direct founder follow-up ("do the abilities actually do anything?"), roughly half the existing job-ability roster (Berserk/Boost/Elemental Seal/Chainspell/Sneak Attack/Trick Attack) sends a flavor-text message claiming an effect that no code actually applies — no "pending effect consumed by the next action" mechanism exists. Combat damage confirmed live as a flat hardcoded constant (`playerDamage = 30`) on both the player→mob and mob→player paths, zero stat read, zero variance. Real, concrete proposed formulas given (STR-scaled+random damage, DEX-based crit, one shared accuracy/evasion roll, VIT-based reduction) alongside a universal `Ability` struct (MPCost/CastTime/Recast/Kind) unifying `/ja`+`cast` into one `cmdAbility`. Phase 0 shipped same day: `cmdSetJob`/`cmdSetSubJob` refuse every job outside the original FFXI six (WAR/MNK/WHM/BLM/RDM/THF) via a new `enabledJobs` map, ASN named explicitly; `jobs` marks disabled jobs; existing characters already on a disabled job are deliberately left alone (no forced destructive switch). Phases 1-5 (spell timing engine, weapon-skill-per-type leveling, targeting rules, new content, the combat formula rewrite, staged re-enablement of advanced jobs) scoped with real, named open questions for the founder (universal lockout duration, THF "SAP" naming, RDM Polymorph's actual effect, cast-interrupt-on-damage, mob stat-block depth) — not started, "plan this next phase of work" was the literal ask.
| BRAWLPIT-RL-NORTH | BRAWLPIT/docs/RL_TRAINING_NORTHSTAR.md | 1 | 1200 | Founder real-time (routed via `emily observe`, Apple #19326): "can we build a training pipeline reinforcement learning on the packet level take autocurriculum doctrine (alpha star league)... take the spicy AI stuff from REDGARDEN and ECOWAR especially in ECOWAR the ability to FAST FORWARD fractal commander but not necessary fractal squad commander (no team coordination) use PARENA when possible," then "and we need to save all the snapshots adding them to the league" -> "so each snapshot has the 3 archetypes the normal the exploiter and the league exploiter so for each snapshot it adds 3 to the league" -> "we need to implement elo i guess". Genuinely different architecture from REDGARDEN's own ctypes-headless-sim rl_env.py: the observation/action ARE the literal real UDP wire bytes (NetHeader/UserCmd/NetPlayer, byte-layout self-verified via ctypes.sizeof asserts against a live sizeof/offsetof C probe), so a trained policy is a genuine drop-in bot speaking the real protocol. Found+fixed a real live bug along the way: server_broadcast() never set NetPlayer.jump_count/.hit_stun (uninitialized stack garbage shipped in every real snapshot ever sent). Added --fast-forward to bin/brawlpit_server (ECOWAR's own precedent, honestly scoped down: no --tick-ms, since local_update has no dt parameter to wire it to). New PARENA/stdlib/brawlpit/commander_mod.prn: a real, pure, rule-based single-agent "fractal commander" posture function (REDGARDEN NORTHSTAR §26.3's own precedent, deliberately WITHOUT §26.3's squad-coordination nesting) -- edge-safety beats stock/damage in priority, I32-only FFI boundary. Ported REDGARDEN's own scripts/rl_league.py verbatim (zero REDGARDEN-specific coupling, all 29 original tests pass unmodified) and extended it: real Elo (elo_expected/elo_update/LeagueManager.get_elo/set_elo/record_match_result, kept in separate per-member files so the existing append-only checkpoint registry stays immutable) plus a new register_generation_snapshot() that registers all 3 archetypes (MAIN/MAIN_EXPLOITER/LEAGUE_EXPLOITER) together per generation, all-or-nothing, with Elo inheriting forward per role unless explicitly reset. scripts/rl_env_packet.py's core plumbing (socket handshake, wire encode/decode, observation, reward, commander posture) was run for real against a live, locally-built bin/brawlpit_server in this session -- found a real, honest, named blocker doing so: the server has no network "reset this match" packet (boots into one forever-running local_init_match), so a connecting training client gets thrown into an already-in-progress fight; this is the real next blocker before an actual multi-episode PPO run, not glossed over. 68 new Python tests + 2 new C test binaries (test_net_protocol.c wired into scripts/build.sh for the first time, test_commander.c new) all pass. Real, honest, not done: PACKET_RESET_MATCH, the actual rl_train_packet.py multi-model orchestrator, any real training run, gymnasium/SB3 not installable in this sandbox (same documented limitation REDGARDEN's own rl_env.py already names). |
| MODULAR-BUILDING-NORTH | PAPERCRAFT/docs/NORTHSTAR_MODULAR_BUILDING.md | 1 | 1200 | Founder real-time (routed via `emily observe`, Apple #19494), a real multi-message architecture scoping session growing directly out of SHANKPIT's own just-shipped NOCK level editor: "how can we think about building modular rooms that snap together... i want to build a modest office space... in papercraft geometry has materials that dictates destuctability... the materials wont just be textures it needs to be built into the levels themselves" -> "papercraft is the engine" -> "its a fractal heirarchy... we have to think of the levels themselves as objects... there will also be like a map builder that lets us stitch the levels together into worlds." Core architecture: box (material-tagged, optionally Paper-Engine-destructible) -> room (composed of boxes, itself a placeable object) -> world/map (composed of rooms, WoW-style zone stitching) is the SAME placement+socket+snap mechanism at three scales, configured with a different object type each time, not three separate tools. Checked directly against real code before writing any of this down: materials (`PAPER_MATERIAL_PAPER/WOOD/CONCRETE/METAL`, per-material max HP) already exist and already drive real physical destructibility via the Paper Engine, not a new system to invent; `PcWorldObjectDef` (packages/common/papercraft_worldobjects.h) is the real, existing persisted object format `apps/mapeditor` already edits, CLI-only, no visual tool yet. Named the one real, load-bearing constraint that shapes everything: `PC_WO_MAX_OBJECTS=8`, a hard, MEASURED UDP-wire-budget cap on real-time destructible objects -- resolved into a real two-tier model (cheap unlimited structural/material-tagged boxes vs. a small budgeted set of real Paper Engine destructible objects). Per-tier socket contracts named as the one place the fractal analogy does NOT transfer for free (box = geometric, room = gameplay-gated doorway, world = WoW-shaped zone-stitching). Real, decided mid-session: SHANKPIT and PAPERCRAFT are siblings, not a shared dependency -- each gets its own hand-ported implementation (SHANKPIT proves box-tier UI patterns live now; PAPERCRAFT's future room builder is a real, separate port, with materials as a real v0 requirement THERE unlike SHANKPIT's own deliberate v0 texture cut, "we can build glass and stuff in from day one and not have to go back and add it later"). SHANKPIT confirmed as the actual playable client -- a PAPERCRAFT-authored room is "just geometry" at the box layer, loadable into SHANKPIT before SHANKPIT's own loader is material-aware at all. Movement direction corrected in real time: SHANKPIT has the proven physics tuning, PAPERCRAFT doesn't -- port INTO PAPERCRAFT, not build fresh there -- with an honest caveat that PAPERCRAFT is real, server-authoritative/net-native only (no local mode) while SHANKPIT's own networking isn't fully proven either, so porting tuning numbers isn't the same as porting a network-hardened controller. Real, current status stated plainly: both repos stay needed in parallel, a future full merge is a genuinely open, undecided question, not assumed. Real, honest: nothing in this doc is built yet -- pure architecture, no code changes, real open questions (exact room/world socket schema, whether to raise the object cap, whether room/world get their own IDUNA-backed registries) named, not resolved.
| SHANKPIT-BOT-TRAINING-NORTH | SHANKPIT/docs/BOT_TRAINING_NORTHSTAR.md | 1 | 1200 | Founder real-time (SECTION 459): "hand engineer between 50-100 features for the bots" + "design the reward system for the bots for now these will just be ffa bots we will do new training for teams... please plan for it architecturally... same thing as BP bots - we need to add a timer to the game mode." Found and fixed a real, found-live protocol bug first: emily-bot's PacketSnapshot decode assumed a legacy 18-byte entity at offset 2; the real wire format (verified via a compiled sizeof/offsetof probe) is a 12-byte NetHeader + 1 count byte + 64-byte NetPlayer entries starting at offset 13 -- every peer position/health/weapon the bot ever read had been garbage. Fixed with a byte-exact unit test. Built a real 72-feature observation vector (apps2/emily-bot/observation.go: 23 self + 4x10 nearest-opponent + 9 aggregate, self-relative bearings, no artificial padding) and a 4-tier reward function (reward.go) built around a real, previously-undecoded find: packages/common/physics.h already computes a dense, server-authoritative reward signal (+150/kill, +0.5/damage-point) that reaches the wire as NetPlayer.reward_feedback and was simply never read before. Team rewards architected via a real, typed TeamRewardContext parameter, deliberately a no-op today (FFA only, per the founder's own instruction). Added a real QUEUE round timer (SERVER_QUEUE_FRAG_LIMIT/ROUND_MS, apps/server/src/main.c). Real, honest, still missing: the actual RL training loop (scripts/rl_league.py has nothing to register yet), a round-timer wire field, level-geometry observation features, and the Colab notebook (explicitly gated by the founder on the above). |
| SHANKPIT-ANTICHEAT-NORTH | SHANKPIT/docs/ANTICHEAT_NORTHSTAR.md | 1 | 1200 | Founder real-time, after a real research conversation on hardware-level USB HID input cheats (ATmega32U4/Arduino-based aimbots indistinguishable from a real mouse at the OS level): "ok northstar this for anticheat." Grounds a real, phased plan directly in SHANKPIT's own existing architecture, checked not assumed: fully server-authoritative combat (a hardware cheat can only steer aim, never fabricate damage/kills), raw UserCmd yaw/pitch already flowing through process_user_cmd at real CLIENT_USERCMD_HZ=60Hz (Phase 0 telemetry needs no new wire protocol), the existing real bot-pool architecture (emily-bot/frozen_policy_bot.py, genuine connected UDP clients) meaning a bot-only match is the same match type with different slot fillers, and S459-62's own real is_active_opponent precedent as direct groundwork for routing one specific suspected player. Captures the founder's own real, independently-converged-on design: silent reroute on suspicion (not proof) into a bot-only match; an escalating bot ladder culminating in sub-human-reaction-time "impossible" bots (a real, physically-grounded, near-unfakeable signal -- no human out-tracks a genuine 0ms-reaction opponent); a bounded-cost outcome (never an automatic permaban from this system alone) that makes even a false positive safe to run automatically, matching "if they aren't cheating they have good scrim partners." Real, honest, named-not-solved: screen-reading/CV-only cheats and memory-reading wallhacks are a different threat class this doc doesn't cover; silently diverting one client out of normal matchmaking is real, non-trivial routing work, not a config flag. NORTHSTAR only, no code written. |
| SHANKPIT-STORY-NORTH | SHANKPIT/docs/STORY_SYSTEM_NORTHSTAR.md | 1 | 1400 | Founder real-time, across several turns: "how can we make maps and objects scriptable ideally with parena [...] doors, ladders, in game computer screens" -> "via the nock tools" -> "make a level loading system like half life [...] stories that stitch the levels together [...] same levels to create 2 stories that vary vastly just due to the order of the chapters (novel called crossings)" -> "multiple entrances or exits [...] almost a tree structure but not totally [...] time loops and arbitrary story definition if exit b go here if exit a go here if 2 decisions ago you said a then next time we go c [...] parena scriptable." Scopes three real, coupled systems in real dependency order, grounded directly in SHANKPIT's own code, not assumed: (1) per-level scene identity -- every NOCK custom level currently shares one hardcoded SCENE_CUSTOM_LEVEL id (the same real root cause behind S459-74's spray-decal-leak bug this session), blocking anything that needs to name a specific level; (2) scriptable map objects (doors/ladders/screens/entrance-exit markers) via a narrow, fixed-contract PARENA-to-C pipeline extending PAPERCRAFT's own real, only precedent (level_mod.prn, a plain-C-signature generated function linked into a host) plus SHANKPIT's existing dlopen/dlsym PoC (apps/dynmod_poc); (3) the story engine itself, modeled as a single PARENA decision function `next-chapter(history, exit-taken) -> ChapterRef` over an accumulated choice-history vector -- shown to cover the batteries-included straight-line default, multiple entrances/exits, time loops, "N decisions ago" logic, and the Crossings same-levels-different-order case all as free consequences of one shape, not special-cased. Resolves the C-vs-Java PARENA target question left open from the texture pipeline's own docs: C is the right, not-a-compromise choice here since scripts are hand-authored by a trusted NOCK-admin-gated map designer (not LLM-generated), matching PAPERCRAFT's precedent and the per-tick evaluation cost a 60Hz server loop needs. NOCK authoring surface follows the exact pattern already proven twice this session (S459-77 blank-slate PARENA editor, S144-09 drag-and-drop animation import). NORTHSTAR only, no code written; phased build order named (scene identity -> object system + one kind -> remaining kinds -> story engine). |
| SHANKPIT-HUMANNESS-NORTH | SHANKPIT/docs/HUMANNESS_NORTHSTAR.md | 1 | 1200 | Founder real-time: "I want to build realistic human like ais for games like the soldiers fighting Gordon in half life or like city dwellers in gta3 or like NPCs in Skyrim that travel I want realistic scriptable humans use mishri Minecraft bot humanness features to help guide development of the framework." Real research pass done first: read MISHRI's own HumannessLayer.ts (timing-noise delay/reactionDelay primitives scaled by fatigue/energy, smoothTurn's real bezier-eased overshoot-then-correct, Box-Muller Gaussian imperfectAim, a randomized-look-loop perception model with no FOV cone, an 8-state mood enum feeding every primitive, BehaviorOrchestrator's real scored-utility-AI behavior selection, and MISHRI's own real behavioral-contract test suite) plus SHANKPIT's own two existing NPC systems (story_ai.c's already-real per-role FOV/hearing perception + AIMode combat state machine; local_game.h's bot_think deathmatch heuristic) and this session's own character-tick ambient-NPC design. Scopes a shared, plain-C humanness.h/.c primitives module (reaction delay, aim noise, turn overshoot, mood state) that story_ai.c and character-tick each call INTO rather than being replaced by -- same real architectural shape MISHRI's own managers already use. Explicitly out of scope, named honestly: chat/typo simulation (SHANKPIT has no chat system, checked directly), bot_ai.h's competitive deathmatch bots (fairness vs. humanness are different goals), and Skyrim-style multi-day NPC routines (connects to the Story System's own level-chaining work, not built here). Real, phased build order (primitives+tests -> story_ai.c integration -> character-tick integration -> PARENA-scriptable per-role personality, deferred). NORTHSTAR only, no code written. |
| SHANKPIT-AI-NAV-NORTH | SHANKPIT/docs2/specs/AI_WAYPOINT_NAV_NORTHSTAR.md | 1 | 1200 | Founder real-time, two messages (EMILY/BACKLOG.md S461): a full auto-pilot NPC AI design (sensor layer, FSM, tactical pathing over a tagged cover node network, steering, squad AI), then a direct follow-up asking grid/waypoint vs. polygon navmesh and confirming explicitly: "we are going to need a waypoint system in the levels and maps northstar it." Real audit done first against what SHANKPIT's own story_ai.c already had (sensor layer + FSM were already real and close to the ask) before building anything new. Chose grid/waypoint over navmesh for a real, checked reason: SHANKPIT has no wall-collision data or navmesh-bake pipeline anywhere (grep confirmed empty), only a ground heightfield in terrain.c -- a navmesh has nothing to bake from, a hand-authored graph needs nothing more than story_ai.c's own existing AIPatrolPoint convention already provides. Shipped S461-01: new packages/simulation/ai_nav.c/.h (up to 32 nodes, real A* over authored edges -- small-graph O(n^2), no priority queue needed -- plus a real cover query: nearest is_cover node whose stored direction actually faces away from a given threat, dot-product tested, not just nearest-cover-tile). Wired into story_ai.c's AI_MODE_FLEE, previously a dead enum value with zero behavior: health<30 (matching bot_client's own existing w_retreat convention) AND courage<0.7 (so Brute/Bombardier/Hound's tank identities fight through it) triggers a real A*-to-cover-then-hold, falling back to a plain repulsion vector when no graph is authored for the scene -- a real, expected case, not an error. Deliberately terminal once triggered: no health-regen system exists anywhere in this codebase, so re-engaging would mean walking back into the same low-health fight, named explicitly as intentional v0 behavior, not a missed case. One real graph authored so far (SCENE_VOXWORLD's story encounter, 6-node loop + 1 chord, 2 cover nodes) with its cover_dir values flagged honestly as a first, unverified-against-real-geometry pass. Real, phased follow-up named: per-scene authoring for every other SCENE_* (all currently unauthored, degrade safely), a real NOCK level-editor waypoint-placement tool for SCENE_CUSTOM_LEVEL (none exists), cover_dir verification once any wall-collision query exists, and routing patrol/investigate/search through the same graph (only flee consumes it so far, per the founder's own IsCover-query framing being the one concretely named consumer). Build verified: `make server` and `make lobby` both compile clean with the new module linked; the four hand-copied CI source lists (Makefile x2, release.yml x2, tests.yml x2) all updated in the same pass this time, naming this as the fourth real recurrence of that CI-drift bug class. |
| SHANKPIT-AI-ANIM-NORTH | SHANKPIT/docs2/specs/AI_SCRIPTED_ANIMATION_NORTHSTAR.md | 1 | 1200 | Founder real-time, direct follow-up to the auto-pilot AI design (EMILY/BACKLOG.md S461-04): asked how to compose animations, then a Half-Life scripted_sequence breakdown -- single-character movement-hook-then-locked-animation chaining with On End Sequence handoff, multi-character frame synchronization (matched entity names, wait-then-simultaneous-ignition), realism layering (bone-controller look-at, phoneme/.wav-driven lip sync). Real audit done first, the load-bearing finding of this doc: GSeq (packages/goldenband/gseq.c, a real, already-shipped sequence-stitcher with crossfade -- founder's own prior ask, "stitch animations together like James Bond walk turn raise gun shoot") already covers most of "chain clip A into clip B" mechanically; the real gap, checked directly rather than assumed, is that gband_skel_npc.c (the module that would actually PLAY those clips on an AI-controlled NPC) is a single hardcoded decorative demo -- one fixed mannequin, one call site in apps/lobby/src/main.c, scene-gated, always looping its one clip, zero connection to any story_ai.c AI. Story-mode bots actually render through a completely different, already-real path: the same draw_player_3rd/tyler_body 5-joint rig every human player uses (confirmed by reading the lobby's own per-frame render loop -- bots and players share one PlayerState array and one draw call). Shipped the real, buildable slice: AI_MODE_SCRIPTED (previously a dead enum value, same class of gap AI_MODE_FLEE was before S461-01) now has real behavior -- story_ai_trigger_scripted sends an AI to a marker (S461-02's own arrival steering), a real per-tick guard now skips AI_MODE_SCRIPTED entirely in the mode-decision loop so combat/investigate/flee perception cannot interrupt it (matches "forced into a locked scripted state" exactly), holds for a real duration, then hands back to whatever mode preceded the trigger via the already-existing previous_mode field (On End Sequence). Real, honestly not built, named rather than attempted half-built: which clip actually plays during the hold (no wire field or client-side mode-to-clip mapping exists yet), multi-actor frame synchronization (needs a new marker-gated all-arrived primitive, nothing built toward it), bone-controller look-at (small, self-contained on top of gpose.c's already-real FK, not started), and phoneme/audio-driven lip sync (the largest gap -- no waveform/phoneme analysis exists anywhere in packages/audio, not even design-sketched). `make server`/`make lobby` build clean. Closes EMILY/BACKLOG.md S461's full four-phase plan (all of S461-01 through -04 now have shipped v0s). |
| SHANKPIT-AI-SOLO-NORTH | SHANKPIT/docs2/specs/AI_SOLO_ENEMY_NORTHSTAR.md | 1 | 1200 | Founder real-time, direct follow-up to the now-closed S461 squad-based auto-pilot AI (EMILY/BACKLOG.md S462): non-squad enemies (wild monsters, zombies, lone predators) shift focus from tactical communication onto territorial boundaries, distinct sensory profiles, and physiological drives -- three archetypes (relentless pursuer: direct-line chase; ambush predator: LOS-concealment then charge; territorial beast: radius-anchored home + leash-triggered forced retreat, given as real pseudocode), asymmetric sensory profiles (sound-only echo-locator, scent/blood-trail tracker), steering overrides (sine-wave flank-weaving, wander jitter), a utility-based Hunger/Fear/Fatigue drive system. Shipped three new AIRole values, deliberately never squadded: AI_ROLE_RELENTLESS_PURSUER (ai_combat_pursuer has no preferred_range kiting branch at all -- always closes; courage 1.0 clears S461-01's flee gate outright, this archetype never retreats), AI_ROLE_TERRITORIAL_BEAST (real radius-anchor + leash retreat following the founder's own TerritorialAutoPilot::Update pseudocode closely -- new AI_MODE_LEASH_RETURN checked unconditionally before combat/flee, real stun immunity while returning via the actual existing stunned_until_ms/stun_immune_until_ms fields rather than a new flag, exits only on real arrival home), AI_ROLE_BLIND_STALKER ("Ambush Predator" + "Sightless Echo-Locator" combined -- vision_range 8.0 makes it functionally blind, detecting almost entirely via the already-existing hearing_range path, a real finding that zero new perception code was needed for this part of the ask). Real sine-wave weaving (ai_weave_strafe) for all three, replacing the square-wave alternation every other role uses -- the founder's LeftVector*sin(time*freq)*amplitude collapses to a plain sine on in_strafe in SHANKPIT's 2D top-down movement, same simplification ai_run_search's own orbit math already makes. One real spawn of each in story_ai_seed_voxworld_encounter, placed away from the two S461-03 squads so a lone archetype doesn't share a squad's engagement space. Real, honestly not built, named rather than attempted half-built: the scent/blood-trail breadcrumb tracker (needs a new central array + a "bleeding/sprinting" signal that doesn't exist anywhere in protocol.h today), utility-based Hunger/Fear/Fatigue drives (named as a real FSM-vs-utility-AI architectural fork, not a bolt-on -- humanness.c's existing per-instance energy/fatigue fields flagged as the natural integration point if pursued), and true wander jitter for idle solo enemies (currently fall into the shared generic patrol fallback every role uses; ai_run_search's own cos/sin orbit pattern named as the real, proven reuse candidate). `make server`/`make lobby` build clean. |
| SHANKPIT-STORY-SEQ-NORTH | SHANKPIT/docs2/specs/STORY_LEVEL_SEQUENCING_NORTHSTAR.md | 1 | 1200 | Founder real-time, direct pivot away from VOXWORLD (EMILY/BACKLOG.md S470-S472's own now-legacy Breach Titan work): "lets not work on voxworld this is a legacy world building on it isnt useful ... we need a way to start devining the story as the levels we build in the level editor we dont need the text cutscene ... we need a way to define how the levels fit together and then we need the loading points or whatever the opposite of the spawners is ... string 2 levels together ... then get our waypointing tools set up ... scriptable interactions ... bake in some good defaults for how the npcs behave and interact with extension points for overriding that default behavior via map scripts." Follow-up clarifying the real architectural split, confirmed directly: "like the bot detecting the player that should be baked in" (perception stays engine-owned, never overridable) vs. "the bot waving at the player that should be configurable per map ... may run up to you and start talking then say follow me! then run and kill an enemy who knows" (the REACTION is map-authored content). Two-phase plan: Phase 1 (this session, in progress) -- a level GRAPH as a real chain (next_level_id, not a general branching graph yet), a new is_story_start registry flag mirroring IsDefaultQueue's own exact enforcement shape (MODE_STORY spawns straight into whichever level holds it, STORY_PHASE_CUTSCENE skipped entirely), and a new LevelExit scriptable object (placed trigger volume, the "opposite of a spawner" the founder was reaching for) that reuses the ALREADY-PROVEN-SAFE server_apply_custom_level mid-game level-swap path (the same mechanism server_advance_queue_round already uses every real QUEUE round) to load the next level live and respawn the player at its own Spawners -- no new engine-level-swap primitive needed, a real, checked reuse. Phase 2 (not started, explicitly answered "do both" when asked JSON-vs-PARENA): a native JSON step-list reaction-script runner first (generalizes AI_MODE_SCRIPTED's existing single walk-to+hold primitive into a real multi-step sequence, S470's wave-then-dance becomes its first named built-in script), with PARENA as a real, later, additive escape hatch for genuinely stateful/conditional reactions a flat step-list can't express (the founder's own example: an item pickup granting immunity to a later scripted explosion) -- SHANKPIT has zero PARENA integration today, unlike PAPERCRAFT/ECOWAR/GTA7/DEADWEIGHT's own established convention, named as real, separate, not-yet-scoped work, not a blocker for the JSON v0. |
| SHANKPIT-MESH-NORTH | SHANKPIT/docs2/MESH_EDITING_NORTHSTAR.md | 1 | 800 | Founder real-time: "can we iterate the widget builder? i think this is a decent place to build our little mini version of blender... 4 modes object mode, edit mode (select face, select vertex, select edge)... we really need the move tool added to the level editor too... move scale extrude... V S E." Asked directly box-based-now vs true-arbitrary-mesh-editing; founder chose full arbitrary-mesh as the real destination, then redirected "build box extrude first." Real constraint named: SHANKPIT's native collision represents every wall as an axis-aligned box (center+extents) -- true vertex/edge dragging into non-rectangular shapes needs a new native mesh-collision representation, new wire format, and level migration, a genuinely separate multi-week project (Phase 2+, scoped not started). Phase 1 (this pass, shippable now, forward-compatible): Object mode (whole-wall select/move/scale via three.js's own real TransformControls gizmo) + Edit mode with Face select ONLY (vertex/edge cut, same "if you have to cut it I might not miss it" reasoning the founder applied to edges, extended to vertices) -- click/shift-click multi-select, Alt+E extrudes a face into a new connected box (duplicate-and-chain, not true topology extrusion, but matches the founder's own described hallway-building workflow exactly), Alt+V move / Alt+S scale the selection via the gizmo, Tab toggles Object/Edit, Alt+1/2/3 reserved for Vertex/Edge/Face select-mode (only Alt+3 live in Phase 1). All hotkeys Alt-modified per founder's own "hotkeys can be alt driven if need a modifier" -- deliberate, since bare E already means "fly camera up" (S487).
| SHANKPIT-RAGDOLL-ORIENT-NORTH | SHANKPIT/docs2/RAGDOLL_ORIENTATION_NORTHSTAR.md | 1 | 900 | Founder real-time chain: ragdoll physics -> "so you are telling me that we need to use machine learning via the goldenband format to teach these ragdolls how to walk again?" -> "yes ... 1 first obvi and then 2" -> "we may as well see how far we can push the engine while we are building it it impacts the kinds of stories we can tell" -> "what does it look like to take it all the way to robotics? do we have to build cad in?" -> "if the character falls over theres no way its going to look human like when they get up its going to be horiffic and synthetic." Grounds in 3 real spike iterations (S484/S494/S495): PBD point-mass+distance is stable, per-bone mass + bend constraints verified working (0/63 violated), ground friction measurably tightens spread -- but none of it can ever produce an anatomically plausible pose since none of it has any concept of orientation. Reframes the actual goal: lying flat after falling is correct (real ragdolls do that), the real bug is the symmetric poker-straight "starfish" shape. Real v0 design: swing-axis constraints derived from rest-pose geometry (no new .gskel metadata needed), deferring twist (needs real per-bone orientation state) as v1. Confirms the founder's own "horrific and synthetic" instinct directly: pure task-success RL recovery is a well-documented failure mode, HQ-SPEC-SIM-100's own Reward Compiler (imitation reward against a real reference clip) is the actual fix, not a physics tuning problem -- names two real recovery paths (authored blend, cheap/now vs. trained policy, the real GOLDENBAND destination). Answers the robotics/CAD question directly from SIM-100 §3: real actuator metadata (torque/velocity/backlash), not full CAD, sourced from real hardware specs, with this doc's own joint-limit work as shared groundwork. Phased: major joints first (hips/knees/elbows/shoulders), fingers/spine/twist/collision deferred.
| EMILY-INFRA-PLAY-NORTH | IDUNA/docs/EMILY_INFRASTRUCTURE_PLAY_NORTHSTAR.md | 1 | 900 | Founder real-time: "we want to allow for the possibility of a big infrastructure play" -- three business lines named, graded honestly by real maturity: (1) hardened workstation/"Citrix killer" replacement -- vision-only, real but partial seeds in PITVIPER/DUNG/SAND; (2) zero-trust IAM preventing agents from ever holding real keys -- IDUNA's own already-tracked story, cross-referenced to EMILY_FOR_BUSINESS_NORTHSTAR.md rather than duplicated; (3) "the big one," MS SQL migration for large orgs into cloud/Oracle/on-prem (cloud-focused) -- least product-shaped but most technically real, grounded in this week's actual shipped work (PARENA project-mssql!/tsql projector + database/mssql-util.prn type-transpile, PRRJECT_FATBABY's Redis Streams zero-downtime consumer-group cutover), with the real, largest named gap being no CDC (change-data-capture) mechanism for a live production source yet. Applies critical review to pasted AI-generated pitch content rather than rubber-stamping: corrects a PARENA memory-safety overclaim (region typing prevents use-after-free, not a literal "never touches the stack"/buffer-overflow-immunity claim), declines to restate unverified specific security incidents as fact, and honors the founder's own "(needs check for hallucination)" flag on a more speculative phone-OS idea by treating it as unresolved, not a committed pillar. |
| DEADWEIGHT-VS0 | DEADWEIGHT/docs/VS0_SCOPING.md | 1 | 1200 | VS0 build plan (2026-09-18, S503): card mode first (9 cards, 1v1 simultaneous-lock rounds, hidden hands), Android-first, TCP server-authoritative dw_server, bot pool of 3 on the same queue humans use (never pair the last waiting bot with a bot), fast-forward PFSP training league on the same binary (BRAWLPIT 1-for-1), one PARENA rules source emitted to C+Java with cross-target parity vectors and ZERO FFI added to PARENA Java (firm founder constraint), IDUNA guest accounts + game-scoped checkpoint registry (multi-tenant by default), NOCK art, CI auto-minor-releases from first commit. Backpack battler = VS1. Companion docs: CARD_MODE_RULES.md, WIRE_PROTOCOL.md. |
| DEADWEIGHT-BRAND | DEADWEIGHT/docs/BRAND_STYLE_GUIDE.md | 1 | 900 | First written brand/style guide (2026-09-21, S513) -- founder asked to "reverse-engineer" prior Python art scripts; corrected in the doc itself: no Python art script exists, the real pipeline is Bash+NOCK CLI+ImageMagick (`art/build_art.sh`, `art/recipes/lib.sh`), real hex codes pulled from there. Names a real, live tension: the GUI chrome (`apps/gui/main.c`, flat rects/bitmap font/no gradients) already IS the "brutalist corporate terminal" pitch, but the card-art pipeline (gradients, tier rims) is a different, more illustrated register -- documents both honestly rather than pretending they're unified, flags reconciling them as a real scoped follow-up. Canon lore (Runner infiltrating IDUNA's corporate defense mainframe) and glossary (Network Breach/Proxy/Abort & Extract/Hunter-Killer/Root Access Override/Runner) -- 2 of 6 terms already exact-match shipped copy, 2 named as real follow-up renames. "Do Not Do" list. Future (revenue-gated, not scoped) cutscene/title-screen art direction capturing the founder's own EmilyOS-as-"the Ledger" spacesuit-pilot visual idea. |
| BIG_O-NORTH | BIG_O/NORTHSTAR.md | 1 | 1300 | BIG_O "A SHANKPIT Story" (2026-09-18, S504, founder pivot): hard sci-fi social-stealth management sandbox on SHANKPIT — DAY harvest genetic data in a vector wasteland (SHANKPIT FPS), NIGHT blend into a paranoid corporate night-society (Heat/"canceled" fail state), BASEMENT off-books cloning lab + pheromone-commanded vector army in an async deterministic shadow war. Signature mechanic: Attention/Heat ("if they dont see it, it isnt real"), specced first as a PARENA scalar rules module. Checked capability audit vs SHANKPIT (level chain/doors/REFLUX/NOCK characters exist; humanness NPCs, witness/perception dispatch, time-of-day, lab, swarm command, shadow-war sim do not). Recommended multiplayer-first: shared persistent world, co-op day, social-hub night, async 1v1 shadow war + bots (DEADWEIGHT server/league pattern). V0 = one full day-night-lab turn, 2 players. Genetic content kept at game-abstraction level. 6-phase plan, S504. |
| OPENEXEC-NORTH | OpenExecutive/NORTHSTAR.md | 1 | 1300 | OpenExecutive (2026-09-21, S506): forked SenteLabsAI/OpenExecutive (external, Apache 2.0, real multi-agent virtual-executive system), being wired to IDUNA (M2M provisioning, real + live) and migrated from Anthropic-direct to Google Vertex/Gemini. Filled in the Emily Way process Claude Haiku 4.5's first pass skipped: relocated the repo off a `/tmp` clone, relocated a misplaced integration doc out of DEADWEIGHT, this NORTHSTAR. Critical review found 2 confirmed bugs blocking the Gemini path (wrong JWT key algorithm -- EC vs RSA against IDUNA's real ES256/EC keys; wrong tool-schema shape -- Anthropic input_schema vs an OpenAI-style wrapper, silently dropping every tool) plus zero wiring yet (no config fields, provider not registered, auth middleware applied to no route, deps undeclared) and IDUNA-side process debt (a stray committed empty iduna.db, one commit bundling unrelated changes). Phased next steps: fix the 2 bugs + system-prompt handling, wire config/registry/middleware/deps, add real eval coverage per upstream's own PR bar, then IDUNA cleanup + two founder-level open decisions (upstream tracking, fallback-provider behavior). |
| COMPANY-VALUES | EMILY/docs/COMPANY_VALUES.md | 1 | 800 | EINHORN_INDUSTRIAL's founding vision/mission/motto/core-values, verbatim from the EMILY repo's own first commit (d12864f2, 2026-01-29) -- dug up 2026-09-21 in response to OpenExecutive's own onboarding question ("what are your company values or operating principles?"), since no single doc previously answered it. Includes an honest note: the 24-line-of-business brainstorm is historical context, not a current product map; the mission/motto/core-values read as durable cultural intent. |
